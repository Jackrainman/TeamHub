# 容器内监听端口（compose.yaml 的 environment.HUB_PORT 已硬编码同值、经 compose 部署时会覆盖此行；
# 保留是为脱离 compose、直接 `docker run --env-file` 的部署路径提供同一份真相）。
HUB_PORT=4177
# 归档物（图纸）文件落盘目录（容器内挂卷 hub_artifacts，重启不丢）。server 读的就是这个变量——
# 漏设则上传/下载恒 404。字节不进 git（D-025/D-038：二进制走本地卷，将来换 MinIO 需另加开关）。
TEAMHUB_ARTIFACT_FILES_DIR=/var/lib/teamhub/artifacts
# M11（AUDIT-FIXES）：知识库语料落盘（容器内挂卷，重启不丢、closeout 回灌累积）。
TEAMHUB_KB_DATA_FILE=/var/lib/teamhub/kb/kb-data.json
# v1：治理快照落盘（容器内挂卷，重启不丢——PM 录入 / 图纸提交日志 / 结案知识节点累积）。
# 文件不存在时 FileGovStore seed 真实场景 + 图纸版本日志。
TEAMHUB_GOV_DATA_FILE=/var/lib/teamhub/gov/gov-data.json
# 治理后端（SS3 SQLite 增量迁移）：留空 = JSON（走上面 TEAMHUB_GOV_DATA_FILE，现状零变化）；
# 设 sqlite 才切 SqliteGovStore，须同时配下一行 TEAMHUB_GOV_SQLITE_FILE。
TEAMHUB_GOV_BACKEND=
# 仅 TEAMHUB_GOV_BACKEND=sqlite 时生效的 SQLite 库文件路径（node:sqlite 内置，零原生依赖）。
TEAMHUB_GOV_SQLITE_FILE=/var/lib/teamhub/gov/gov.sqlite
# 库存/BOM 落盘（容器内挂卷，重启不丢——盘点 / 拆装登记 / 一句话快记累积）。
TEAMHUB_INV_DATA_FILE=/var/lib/teamhub/inv/inv-data.json
# 倒排基准线落盘（容器内挂卷，重启不丢——队长手写覆盖 / 验证门过门留痕累积）；独立文件，
# 不进 TEAMHUB_GOV_DATA_FILE（红线：基准线本体不塞 GovernanceSnapshot）。
TEAMHUB_BASELINE_DATA_FILE=/var/lib/teamhub/baseline/baseline-data.json
# 门检查单/欠条落盘（容器内挂卷，重启不丢——现场快记欠条 / 清偿 / 豁免留痕累积，GATE-CHECKLIST-IOU）；
# 独立文件，同 baseline 先例不进 TEAMHUB_GOV_DATA_FILE。
TEAMHUB_CHECKLIST_DATA_FILE=/var/lib/teamhub/checklist/checklist-data.json
# H3（AUDIT-FIXES）：写端点共享密钥。HUB_HOST 非 loopback（compose 用 0.0.0.0）时**必须**设非空，
# 否则 server 拒绝启动。⚠️ 暴露前务必把占位值换成强随机串：openssl rand -hex 32
TEAMHUB_WRITE_TOKEN=change-me-before-exposing
# 反代信任：单端口 4177 反代 / SSH 隧道部署后面须设 true，否则写端点限流塌成**全队共用一个桶**
# （任一客户端可耗尽、DoS 全队写入）。直连暴露时保持 false（否则 X-Forwarded-For 可伪造）。
TEAMHUB_TRUST_PROXY=true
# 空板冷启动：真实团队设 false → 落盘文件首启动 seed 为空板（仅赛季/项目/阶段元信息），不进演示假数据。
# 未设 / 非 false = 落演示场景（8 任务 + 图纸版本日志 + 知识库语料），便于走查。仅影响新建文件。
TEAMHUB_DEMO_SEED=false
# 轻身份登录模式（IDENTITY-LITE，D-083 §4.2）。缺省 anonymous = 现状零变化（共享写口令）；
# 设 identity → 匿名可读一切 + 登录才能写（GET /api/session 选人+可选 PIN，actor 服务端注入）。
TEAMHUB_IDENTITY_MODE=anonymous
# 装配层启用模块（HUB-MODULARIZATION）：留空 = 缺省全 6 模块开；设逗号分隔 ModuleId 子集
# （如 "system,pm-core,knowledge-base"）可收窄，含未知模块 id 会 fail-closed 拒绝启动。
TEAMHUB_TENANT_MODULES=
