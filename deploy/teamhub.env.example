# 容器内监听端口（compose.yaml 的 environment.HUB_PORT 已硬编码同值、经 compose 部署时会覆盖此行；
# 保留是为脱离 compose、直接 `docker run --env-file` 的部署路径提供同一份真相）。
HUB_PORT=4177
# 六个结构化数据域的唯一生产数据库（容器内挂卷 hub_data）。
TEAMHUB_DB_FILE=/var/lib/teamhub/data/teamhub.sqlite
# 归档物（图纸）文件落盘目录（容器内挂卷 hub_artifacts）。字节不进 SQLite/git。
TEAMHUB_ARTIFACT_FILES_DIR=/var/lib/teamhub/artifacts
# H3（AUDIT-FIXES）：写端点共享密钥。HUB_HOST 非 loopback（compose 用 0.0.0.0）时**必须**设非空，
# 否则 server 拒绝启动。⚠️ 暴露前务必把占位值换成强随机串：openssl rand -hex 32
TEAMHUB_WRITE_TOKEN=change-me-before-exposing
# 反代信任：单端口 4177 反代 / SSH 隧道部署后面须设 true，否则写端点限流塌成**全队共用一个桶**
# （任一客户端可耗尽、DoS 全队写入）。直连暴露时保持 false（否则 X-Forwarded-For 可伪造）。
TEAMHUB_TRUST_PROXY=true
# A2 app_settings 落地前的临时配置文件与临时 hub_config 卷；首启动向导写入后重启。
TEAMHUB_CONFIG_FILE=/var/lib/teamhub/config/config.json
# 装配层启用模块（HUB-MODULARIZATION）：留空 = 缺省全 6 模块开；设逗号分隔 ModuleId 子集
# （如 "system,pm-core,knowledge-base"）可收窄，含未知模块 id 会 fail-closed 拒绝启动。
TEAMHUB_TENANT_MODULES=
