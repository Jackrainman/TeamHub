# Now

> 唯一当前战况源。Team Hub 方向已由 D-024 覆盖旧 markdown-only pivot。逐条完成时间线 → `docs/archive/completed-log.md`；pre-pivot 历史快照 → `docs/archive/v0.3-pivot/`。

```yaml
mode: collab_design
stage: "2026-07-11 **产品重定义（D-083）**。开源·防爆肝双主轴·四把刀拍板：①倒排基准线（赛季→学期→阶段类型+里程碑链+验证门+投资类任务防砍示警）②轻身份登录双模式（匿名模式整体保留供选择）③我的视图 ④store 拆分+SQLite。宪法修正：G4 里程碑例外（里程碑有日期、Task 永不带个人 dueDate、快慢派生）+ I0 口径降级（分析对准事不对准人、不做排行榜、登录后本人视图合法）+ AI 排人三红线（事实拼盘不排序/拍板留名归人/只在决策现场）。课表排班砍（伪需求）、在场排班收窄到关键窗口、缺人方向→学习方向、兴趣声明暂不建、游戏包+对外文档+PHASE2 后置。真相 = decisions.md D-083 + docs/design/product-redefine-2026-07.md（解耦审计 wf_66e13d79-814 账单入其 §9）。权威 = decisions.md + docs/archive/completed-log.md + git log。"
stage_goal: "演进留地基、AI 退出治理（治理只做人读说明视图、大三/学长判断），推进战队内部协作工具三支柱——① 战队知识库（规范+资料+调试归档+跨赛季沉淀，复用 growth.ts KnowledgeNode + 移植 Probe_Flash IssueCard→Archive 闭环）② 项目计划表（D-041：任务为核心·全员可见·依赖图+卡住必带原因·无甘特·不按人天数）③ 库存/BOM（P1，自保鲜护栏）。设计北极星 = 比死表省事 / 用着就更新（派生优先）/ AI 只当仓管·转译不下判断 / 人在环 / 小作坊轻量。事实源 = AGENTS.md §1 + docs/design/team-hub-concept.md（canonical）+ decisions.md D-039/D-041。治理 AI 派生整簇（D-032~035）挂起、复活触发 = 未来确认要 AI 参与治理判断。"
current_task: "2026-07-15 **两刀实现轮（D-087 门检查单与欠条 + D-088 挂单认领制）全部落地收口**（前情=阶段0架构体检门 `645b54c`：`wf_031f4f71` 3 读察 + opus 终审判两刀承载力 continue 放行，零 blocked / 全 minor，顺手修 D1/D3/D6 三笔账本）。D-087 四刀 C1-C4（`f2ceffb`..`86bcc3d`，v0.22.0→0.22.3）：GateChecklistItem 独立域（anchor 挂门/自选到期日展平二选一+superRefine 互斥/origin=template|iou/豁免=大三强制理由+留名）+ `deriveChecklistDrift` 并列派生（欠条到期周粒度红黄绿）+ 过门硬闸（检查项全非 pending 才可过、missed 不拦）+ 验收人名单 `Member.gateReviewer` 位 + 鉴权（`authz.ts` `isGateReviewer`）+ console 门检查单卡/全局快记欠条入口/告警区/设置页验收人名单分区；复审注释对齐 `bbb4bc2`（v0.22.4）；偏离回写 `gate-checklist-iou.md` §7。D-088 三刀 T1-T3（`5a60344`..`bfa5d25`，v0.23.0→0.23.2）：Task 八字段留名簇（claimedAt/assignReason/assignedBy/partnerMemberId/crossClaimConfirmedBy/completedBy/reviewedBy/reviewNote，体检 D1 债路径②裁定扩字面「三个」，零新实体不复活 TaskProgressSignal）+ `isBigTask`/`deriveTaskAcceptance`（验收态派生不动 TaskStatus 五态）/`isPostedTask` 结构尺 + 六窄写路由（claim/assign/partner/confirm-cross-claim/complete/review，actor 注入沿 IDENTITY-LITE，review 鉴权复用 `isGateReviewer`——**验收人名单与豁免名单同一张大三名册**，D-087 拍板②语义再次兑现）+ isBig 下沉后端（体检 D5 债）+ console 挂单池视图（滞留分档一键认领/组长指派）/任务卡留名徽章（搭档黄标/跨组待确认/两档验收）/我的视图空态「没把握就去问问学长」/看谁做过事实卡搜索；偏离回写 `task-post-claim.md` §9。**总计**：commit `645b54c`..本 docs 收口共 10 个功能/文档 commit，VERSION 0.21.1→0.23.2；三包 `verify:all` 终态全绿（contracts 263 / server 344 / console 163）；health-check 10 页 0 错 0 白屏（挂单池 tab 已补进覆盖清单）。**两刀全部完成。下一步待定**（候选见下方 frontier 各项，或用户指新方向）。// 上游文档：`arch-checkup-2026-07-15.md`（体检放行）+ `gate-checklist-iou.md` §7 + `task-post-claim.md` §9（实现注记已回写）；★本机 node24 全套 node_modules 可本机验证，禁 WSL/ssh。"
repo_sync: "2026-07-15：两刀实现轮（D-087 门检查单与欠条 + D-088 挂单认领制）+ 阶段0体检门放行（`arch-checkup-2026-07-15.md`）+ 本次收口 planning docs commit（不 bump）——commit `645b54c`..本 docs commit，VERSION 0.21.1→0.23.2；三包 verify:all 终态全绿（hub-contracts 263 测 / hub-server 344 测 / hub-console 163 测）+ health-check 10 页 0 错 0 白屏（挂单池 tab 已补进覆盖清单）+ 活体驱动认领闭环验证（挂单池 2→1 张、写→刷新）。push 走已配 credential helper（共享库 ~/ruolin_huang/.gh-credentials，push 前 git fetch 防分叉）。**遗留注记（收尾清理受限）**：停服 pkill 与删 `~/teamhub-data/*.json` 被安全分类器拒（视 teamhub-data 为持久语料）；demo 服务器仍在 127.0.0.1:4177 后台运行、上一 session seed 的 gov.json（含一次活体测试认领 R1总联调→owner m-visionA）仍在——backup 脚本证实本 session 前该目录为空、属 session scratch 无害，但未能移除；本次两刀实现+复审全程未新增任何 `~/teamhub-data` 污染；下次 demo 若要净种子需手动清 `~/teamhub-data/*.json` 重启。 // 历史 2026-07-12：路线 v4 五把刀（IDENTITY-LITE/MY-VIEW/STORE-SPLIT-SQLITE/LEARN-DIRECTION-REDESIGN/AUDIT-DEBT-2026-07）收口轮——本批（ff35a5b..d1a8aed 共 17 个 commit，VERSION 0.11.5→0.15.5，含 4 次 minor：I1 4a2acd9/M1 6be2545/SS3 244fba3/L1 f98c9d3）+ 本次收口 planning docs commit（不 bump）已**本机全量复核验证**：三包 verify:all 全绿（hub-contracts 214 测 / hub-server 278 测含 e2e-pillars 3 测 / hub-console 125 测，typecheck+build 均过，exit 0）+ `test:local -- e2e-pillars` 独立复跑 3 绿（默认匿名 env，零变化）+ 逐 commit VERSION 核对无漏 bump。本机 node24 全套 node_modules（WSL 免除，用户裁定；本轮全程无 WSL/ssh）。push 走已配 credential helper（共享库 ~/ruolin_huang/.gh-credentials，push 前 git fetch 防分叉）。 // 历史 2026-07-11：BASELINE-CORE 六步 + 收口（d3db6fe..e3a76a9 共 7 个 commit，VERSION 0.10.3→0.11.5，含 S1 一次 minor）三包 verify:all 全绿（197/232/96）+ e2e-pillars 3 绿 + Playwright 首屏走查基准线总览达成。 // 历史 2026-07-03：origin/master=c84f4a9（07-03 FF 合并 feat/plugin-core 六个模块化 commit：6fc32fb→c84f4a9，VERSION=0.9.7）。rainman WSL2 上次同步到 feat/plugin-core 同一 HEAD（07-02 验证全绿：三包 verify:all 432 测 + 迁移脚本 scripts/migrate-robottarget.mjs 真 gov.json 副本零丢失 + Playwright health-check 8 页 0 错等价 master + 真实未迁移 gov.json 新 schema 加载成功）；本轮 07-03 全部 commit 已经**本机全量验证**（WSL 跳过·用户裁定；本机 node24 首次装齐四包 node_modules + playwright chromium）：506 测全绿 + health-check 8 页 0 错 + 新端点冒烟全通。WSL 真机部署验证转为可选项。**部署前必做**：D-080 砍 `requires` 依赖类型 + FileGovStore fail-closed 校验后，旧 gov.json（含 `type:'requires'`）redeploy 直接起不来，须先跑 `scripts/migrate-robottarget.mjs` 或 reset `~/teamhub-data/gov.json`。 // 历史 2026-06-21：origin/master=9cd0b7a（D-078 图纸文件链路 + 表单修缮 + 部署/版本号修复），三包 verify:all 全绿（164/194/44）+ 真机 API 冒烟全过（上传/下载/重传清兄弟/落盘持久化/415·401·404·400）。 // 历史 2026-06-19：origin/master 曾推进到无人值守实现轮（5cb84cb：INV-BOM-CORE + PRESENCE-IMPL 安全增量 + WSL 验收截图 + lark bin 修，4 commit 已 push）。push 凭证走共享库 ~/ruolin_huang/.gh-credentials，按 D-064 默认 commit+push（push 前 git fetch 防分叉）。rainman WSL2 真机已同步到 736f0c3 并起服 4177 跑通 Playwright 验收（loopback，TEAMHUB_INV_DATA_FILE=~/teamhub-data/inventory.json）；注意 WSL 在 ssh 会话关闭后会 reap 所有进程（含 tmux），常驻部署须保持连接或 enable-linger，验收已在单会话内完成。bundle 传 100.78.202.84 实测 ssh BatchMode 可达、字节比对无截断。详见 memory teamhub-remote-access。"
frontier:                                # 本轮(07-11~07-12)=路线 v4 五把刀(IDENTITY-LITE/MY-VIEW/STORE-SPLIT-SQLITE/LEARN-DIRECTION-REDESIGN/AUDIT-DEBT-2026-07)全部落地收口；下一批见下方「后置」
  - TASK-POST-CLAIM                       # ✅ 已落地(2026-07-15，`5a60344`..`bfa5d25`，v0.23.0→0.23.2)：挂单认领制——Task 八字段留名簇(claimedAt/assignReason/assignedBy/partnerMemberId/crossClaimConfirmedBy/completedBy/reviewedBy/reviewNote，体检D1债路径②裁定扩字面「三个」、零新实体不复活TaskProgressSignal)+ isBigTask(挂门||有非waived下游边)/deriveTaskAcceptance(验收态派生不动TaskStatus五态)/isPostedTask 结构尺+ 六窄写路由(claim即生效防孤儿400/assign+partner+confirm-cross-claim走isGroupLeadOf 403/review复用isGateReviewer，与欠条豁免同一张名册)+ GET /api/tasks 扩 q= 看谁做过搜索+isBig下沉后端+console 挂单池视图(滞留分档一键认领/组长指派)+任务卡留名徽章(搭档黄标/跨组待确认/两档验收待验收·已验收·抽查打回)+我的视图空态「没把握就去问问学长」。三包 verify:all 全绿(contracts 263/server 344/console 163)+health-check 10页0错0白屏(挂单池tab补入覆盖清单)；真相文档 task-post-claim.md §9(偏离回写：assign不做状态提升/complete无操作人=owner强校验等)；复审三 nit 已收口(`0926fc1` v0.23.3：assign防孤儿ownerId 400+review done前置判409+reviewNote以本轮为准，server 347测绿)
  - VISUAL-VITALITY                       # ✅ 三批已落地(2026-07-13，9f123b3..HEAD，v0.19.5→0.21.1)，**分支已并 master**(2026-07-15 merge push，`e75506b`)：V0 地基(display字号档+动效token/keyframes+viz原语 Ring/StatusDot/Sparkline/CountUp+12单测)→V1 战情屏总览(T−N周倒计时hero+时间轴64px+指标瓦片仪表化+事件tone点/mono时间戳+里程碑下一站)→V2 收口(页题3xl+panel--hero两处+页级空态构图+tech依赖图节点提亮/被卡脉动)。verify:all 143测全绿+health-check 9页0错+四主题截图 docs/screenshots/visual-vitality/；真相文档 docs/design/visual-vitality.md(§6 偏离注记含 Sparkline 建而未耗的说明)；**不越 DESIGN-LANG B4/B5 硬边界**(散装空态/图标未动)
  - GATE-CHECKLIST-IOU                     # ✅ 已落地(2026-07-15，`f2ceffb`..`86bcc3d`，v0.22.0→0.22.3)：门检查单与欠条——GateChecklistItem 独立域(anchor 挂门/自选到期日展平二选一+superRefine互斥/origin=template\|iou/豁免=大三强制理由+留名)+ deriveChecklistDrift 并列派生(欠条到期周粒度红黄绿)+过门硬闸(检查项全非pending才可过、missed不拦)+验收人名单 Member.gateReviewer 位+鉴权(authz.ts isGateReviewer，豁免两模式统一走名册)+console 门检查单卡(含0项门)/全局快记欠条入口/告警区/设置页验收人名单分区。三包 verify:all 全绿(contracts 235/server 313/console 148)+health-check 9页0错0白屏；真相文档 gate-checklist-iou.md §7(偏离回写：anchor展平/drift并列非扩展/独立checklist.json/事实层带名不剥/409首引入/PUT权限v1/复审补两处轻隐患)
  - BASELINE-DESIGN                       # ✅ 设计已锁(2026-07-11，baseline-design.md v1)：三版车节奏模板(V1实验→G1问题清单收敛→寒假sim2real→G2 V2拼装→G3期中V3→G4整车试跑→调参入场)/验证门(大二证据+大三验收，字节走artifact卷)/投资三维(horizon×value×timeAccumulation)/版次可裁剪/周粒度红黄绿；真实时间线用户赛后回填
  - BASELINE-CORE                         # ✅ 刀①已落地(2026-07-11，d3db6fe..e3a76a9，v0.10.3→0.11.5)：Season 接线(GovernanceSnapshot.seasons+GET /api/seasons)+baseline zod(独立 baseline.ts 域)+独立 baselineStore 两实现+落盘 baseline.json(TEAMHUB_BASELINE_DATA_FILE)+GET/PATCH/pass 三路由(含过门 POST+I0 剥 passedBy)+deriveBaselineDrift/组归因/投资示警周粒度红黄绿+总览 BaselineOverview+三版车模板 v1 seed+投资录入；偏离已回写 baseline-design.md §7
  - IDENTITY-LITE                         # ✅ 刀②已落地(2026-07-11~12，4a2acd9..8978308，v0.12.0→0.12.2)：I1 服务端(轻身份契约+PIN scrypt散列+SessionManager内存会话+actor注入6写路由+双模式main.ts接线)+I2 console(IdentityBar登录UI+queryKey身份维度+PmCreatePanel ownerId自由文本→选人+写门禁用)；匿名模式零变化(env缺省)；偏离回写本文档 §4.2 注记
  - MY-VIEW                               # ✅ 刀③已落地(2026-07-12，6be2545..bb3f70d，v0.12.2→0.13.1)：DepNode加ownerId机读键+我的视图页(MyViewPage，我负责且未被卡住∩被卡分组，客户端过滤GET /api/dep-graph，按session.memberId)；偏离回写本文档 §4.3 注记
  - STORE-SPLIT-SQLITE                    # ✅ 刀④已落地(2026-07-12，47c2f65..1616c4f，v0.13.2→0.14.1)：GovStore拆PmCoreStore/ArtifactStore/ScheduleStore三域+id/clamp抽纯模块+排班域(resourceSessions/relayHandoffs)补落盘(合一schedule-sessions.json)+SqliteGovStore真实现(node:sqlite文档式行存)+迁移脚本+TEAMHUB_GOV_BACKEND opt-in(默认JSON不变)。**增量边界**：gov(pm-core/artifact/schedule三域)已可选迁SQLite；kb.json/inv.json/baseline.json 仍纯JSON、未迁，留后续刀触及各自域时再做；偏离回写本文档 §4.4 注记
  - LEARN-DIRECTION-REDESIGN              # ✅ 刀⑤已落地(2026-07-12，f98c9d3..7841a9c，v0.14.1→0.15.1)：缺人方向页翻转为「学习方向」DirectionPage(跨工种学习地图ROBOTICS_LEARNING_MAP×队内缺口，identity模式按本人groupId个性化排序)+AI边界横切通用资源+sim2real种子缺口；兴趣声明仍不建(D-083)；偏离回写本文档 §5 注记
  - AUDIT-DEBT-2026-07                    # ✅ 六项审计债已清(2026-07-12，538ea9a..d1a8aed，v0.15.2→0.15.5)：①GroupKind闭集→开放非空串 ②convergenceScope收口常量 ③pm-requests.ts解绑robotics硬编码(工厂函数+具体化挪verticals/robotics.ts) ④main.ts接线TenantConfig通道(TEAMHUB_TENANT_MODULES) ⑤工具条刷新按钮归一注册(onRefresh) ⑥词汇覆盖层支持新增key；偏离回写本文档 §9 勾销标注
  - MODULARIZATION-PHASE2                 # 后置(D-083)：游戏包推迟，先把 Robocon 写明白；届时再切垂直包 worktree
  - DEPGRAPH-AI-AUTODRAW                  # AI 自动布大致 DAG + 人拖拽微调（AI 只建议不落库，卡 Hermes 触点）← 跳过
  # 排班收窄(D-083)：在场排班/接力退出日常动线，只服务「人必须凑齐」的关键窗口(联调日/赛前)；课表围绕排班停建(伪需求，MemberAvailability schema/派生保留不扩建)
  # 仍卡外部基建/排期：INV-Hermes 自动对话记账(卡 HUB-HERMES-ADAPTER) / KB-LARK 飞书 wiki·drive→KB 摄入(需落点 schema 定稿) / 正式部署上线(卡用户审批) / Hermes
  # 后置(D-069)：STUDY-BROAD-D039-REVIVAL(HARD-GATED 封存·B1 已拍；D-083 AI 三红线延续其精神)
  # 挂起(D-039 AI 退治理，spec 留、复活触发=未来要 AI 参与治理判断)：GOV-MEMBER-STATUS-DERIVE / GOV-RULES-LAYER-DESIGN + D-032~035 治理派生整簇 + 自动派活 + freeIdle·双写债
  # 历史已落地条目(IA-REFACTOR D-077 / ARTIFACT-FILE-CHAIN D-078 / PRESENCE-RECONCILE D-079 / DAILY-PLAN-PRESETS D-082 / PHASE2-CONSOLE-ASSEMBLY 等)→ docs/archive/completed-log.md + git log
# P1：~~INV-BOM 内核~~(已落地 2026-06-19，自保鲜护栏=对话记账/盘点不再造静态表，Hermes 自动记账仍待 HUB-HERMES-ADAPTER)+飞书 Bitable·sheets 读写+~~修 lark-cli bin bug~~(已修 5cb84cb)；P2：资料/代码批量整理(AI 安全车道)、给老师项目级汇报
blocked: []
open_for_decision:                       # ARCH-PATH(D-028)/提醒(D-026后续→D-037)/资源(D-029)/idle三态+静默(D-032)/受众路由(D-033) 已拍；以下待用户线下细化
  - ARTIFACT-VERSION-SEMANTICS           # 图纸版本语义：v2 已落地最小+回退(D-071)；真实文件上传/存储 = ✅HUB-ARTIFACT-STORE-MECH 本地卷版已落地(D-078，文件+云端链接双存)。仍 open 的进阶语义 = 手动钉旧版 pin(append-only supersede)/按车分支/跨赛季权威指针(D-036，别做完整 PLM)；电路驱动命名规范 = 用户内部待定
  - REMOTE-ACCESS-DEPLOY                 # 远程部署 = 实验室 LAN+隧道方案与鉴权（D-036，§8 审批门后，独立基础设施轨）
  - GITHUB-TO-LOCAL                      # 程序代码 GitHub→本地 Forgejo 迁移 = 考虑中（D-038）；TeamHub 只消费 gitCommit 信号、不改 git 唯一真相
  - PULL-CLOUD-CODE                      # 定期 pull 云端代码/EDA 到本地备份 = 考虑中（D-038，与电路 EDA 云端引用相关）
  # PRESENCE-VIZ-RESOURCE-MODEL 已定稿(D-072，2026-06-19)→移出待定，实现轮 = frontier 的 PRESENCE-IMPL
  # UI-RESTYLE-AURASH 已拍板关闭(D-060 SUPERSEDED-BY D-084，2026-07-12)→移出待定；用户改选科技风，落地=第4主题 tech 并设默认（332c354..04654b3），Aurash 暖纸风提案不再是候选方向
post_pivot_registry:
  - SKILL-PROTOCOL-V1                    # 已落地草稿；作为治理触点层 skill 契约底座保留
  - BRIDGE-01-ROSTER-SCHEMA              # 模型并入治理 Task/progress；数据载体被 D-026 路线 A 反转（系统库做真相）
  - TRAIL-01-VIEWER-DESIGN               # 等治理 event/archive/artifact 原料足够后再设计
frozen:
  - ProbeFlash-v0.3.0                    # 代码已删(git 历史保留)；致命补丁走 git revert
```

## 架构定位（D-026 立魂 → D-037 回中）

四层架构 + 路线 A 详见 `AGENTS.md §1` 与 decisions.md D-026/D-037（不在此重复）。规则协调域是新增核心；已建 Hub 壳子（hub-server/contracts/console/Compose）作为触点/集成 + 展示底座保留。深设计见 `docs/design/team-hub-concept.md`（canonical）。

**产品定义**：定位从「运维 / 观测控制台」→「制度化进度治理系统」(D-026)→ **协作中枢：CASE 工具 + 团队交流中心 + 战队数据库**(D-037，给学长减负 / 给学弟指引 / 项目同步进度表)。系统是大脑 / 飞书是脸、不双写、无硬截止只轻推、**人键只回本人当帮助、第三方只见结构键（核心不变式 I0）**、给被卡的人正名而非抓摸鱼、给新人安全网。**D-039（2026-06-13）**：演进留地基 + **AI 退出治理**；产品 = 三支柱（知识库 / 项管看板 / 库存-BOM）；治理 AI 派生挂起。提醒模型 / AI 边界细则见 `AGENTS.md §2.2` A 原则 + decisions.md D-037（不再依赖 D-026 后续原文，已归档）。

## 阻塞 / 待拍板

- **真实外部 adapter**：Hermes / 小龙虾 / Claude Code 真实接入需用户提供运行方式与权限；AI 当前只能做 mock-first 适配设计。
- **真实服务器写入**：Forgejo/Gitea/bare git 部署、SSH、systemd、80/443、真实数据迁移均需用户白天审批后再做。

## 已冻结

- ProbeFlash v0.3 代码（原 apps/desktop、apps/server、release 流程）：已于 2026-06-09 删除；完整代码留 git 历史，精华见 `docs/archive/v0.3-closeout/PROBEFLASH-V03-ESSENCE.md`；致命补丁走 `git revert`。
- pre-pivot backlog 全部任务（TECH-* / AIREADY-* / REALAI-* / CODECTX-* / DEP-* / DATA-* / UI-* / CORE-* / SEARCH-*）：不再认领；详见 `docs/archive/v0.3-pivot/backlog.md`。
- 原 BRIDGE / TRAIL markdown-only 候选：已被 D-024 Team Hub 架构覆盖，后续只作为 Hub BridgeState / Trail 能力重评。

## 安全边界（pivot 后仍生效）

- 不动 v0.3 server / SQLite / API（致命补丁除外）。
- AI / Skill / Hub adapter 不读 / 打印密钥（`.env` / `*key*` / `*secret*`）。
- 真实 Hermes / 小龙虾 / Claude Code / 飞书 / Git forge smoke 由用户线下配置；AI 只做 mock-first 与只读诊断。
- 不在未审批情况下写真实服务器、SSH、systemd、80/443 或迁移真实数据。

## 最近完成（详见 `git log` 与 `docs/archive/completed-log.md`）

- 2026-07-15 **VISUAL-VITALITY 并入 master + 阶段0体检门 + 两刀实现轮全部落地（`e75506b`→`bfa5d25`，VERSION 0.21.1→0.23.2）**：VISUAL-VITALITY 分支合并 master（`e75506b`，三批视觉升级 V0/V1/V2 定稿）→ 阶段0架构体检门（`645b54c`，`arch-checkup-2026-07-15.md`，3 读察+opus 终审判两刀承载力 continue 放行）→ D-087 门检查单与欠条四刀（`f2ceffb`..`86bcc3d`+复审 `bbb4bc2`，v0.22.0→0.22.4，门检查单容器+欠条+验收人名单）+ D-088 挂单认领制三刀（`5a60344`..`bfa5d25`，v0.23.0→0.23.2，Task 留名字段簇+挂单池+两档验收）全部落地；三包 verify:all 终态全绿（contracts 263/server 344/console 163）+ health-check 10 页 0 错 0 白屏。各自设计文档 §7/§9 为真相（`gate-checklist-iou.md` §7 / `task-post-claim.md` §9）。
- 2026-07-12 **交互三连 + 设计语言统一前半程（`7fa01fe`→`9e1b036`，VERSION 0.17.0→0.19.5，计划经用户批准）**：① `7fa01fe` **星图点击详情面板**（v0.18.0）——点选节点右侧出覆盖式详情（click/drag 以指针位移区分、相机 320ms 缓动对焦、Esc/空白/×关闭、选中关联边常亮、窄屏退化横条卡；按 kind 分内容）；② `32eab65` **SEASON-CREATE 补链路**（v0.19.0）——总览空态"先在设置里建一个赛季"原指向不存在的入口（写口曾注记待身份功能，IDENTITY-LITE 已落地故兑现）：contracts CreateSeasonRequestSchema + 三 store `createSeason`（新赛季恒 active、旧 active 同笔归档=一届一个当前赛季）+ POST /api/seasons + 设置页「赛季」分区，UI 真机走查过；③ **DESIGN-LANG-UNIFY B0–B3+U1**（`391014c`/`32c4278`/`0a22eca`/`6999319`/`9e1b036`）——规范落 `docs/design/design-language.md`（§1-§10 唯一写法+批次真相），9 sonnet UX 扫描 9 页×4 主题（36 张基线截图入库 `design-lang-baseline/`，发现分流 U1-U8 进批次/其余入 backlog `UX-SCAN-BACKLOG-2026-07-12`）；B1 文案两词制（新建+登记，用户拍板）+车→机器人；B2 徽章 8 家族→`.badge`+tone（U3：未配置全主题中性化/空着蓝绿分离）；B3 按钮 12 套→`.btn` 4 变体×2 尺寸（SubmitButton 保 API；rowBtn/画布迷你钮豁免有记）；U1 修真 bug=`.console-nav .nav-item` 特异度压掉三主题侧栏 active（tech 靠 0,3,0 侥幸活）。防石山审计+发布链路设计另落 `docs/planning/rot-audit-2026-07-12.md` + `docs/design/release-pipeline.md`（`0618948`）。**⏸ 硬边界（用户拍板节奏）：B4 卡片/空态 + B5 图标/kb-前缀/CSS 治理待用户看 B3 后截图（`design-lang-after-b3/`）放行**。

- 2026-07-12 **UI 三连（构建分包 + tech 观感打磨批次 B + 学习方向 3D 星图，`1f3ffa8`/`54055df`/`ad12c1b`，VERSION 0.16.1→0.17.0，交互式会话主 agent 亲做）**：① `1f3ffa8` vite `manualChunks` 拆 vendor-react/data/icons，入口 535kB→285kB，500kB 警告消除；② `54055df` tech 观感打磨 8 处（截图评审驱动：侧栏 active 青轨+渐变底 / 工具条渐隐分隔线 / panel 标题水洗 / 行 hover 青染 / seg 选中态青底描边 / 全站细深色滚动条 / 「未配置」主色青→中性灰蓝语义修正 / 空态虚线留位框），全圈 `[data-theme='tech']`；③ `ad12c1b` **学习方向双 UI**（用户拍板）：指南/星图 seg 切换，星图=**自写 SVG 3D**（零依赖——特意不引 three.js，免 500kB 警告在懒加载 chunk 复发+实验室离线友好），四色=四组（电控青/电路绿/机械琥珀/视觉紫）、跨工种知识点=RGB 混色、缺口红环挂本组、AI 边界白点居中连四组、`ITEM_TARGETS` console 侧手工标注条目→关联工种（键=契约原文，改文案安全降级单色）；交互=拖拽旋转/滚轮缩放/节点可拖（视平面位移逆旋转回基础坐标系）/悬停高亮关联边/闲时缓转（reduced-motion 停）；懒加载单开 chunk 8.7kB；6 条纯函数单测。console verify:all 131 测绿 + health-check 9 页 0 错 + 真机 Playwright 走查（自转/悬停 tooltip/双工种色标实证）。截图 `docs/screenshots/ui-2026-07-12/`。**②③是否保留待用户看图定夺**（各自独立 commit 可单独 revert）。
- 2026-07-12 **科技风换肤：第 4 主题 `tech` 落地并设默认（`332c354`→`04654b3`，VERSION 0.16.0→0.16.1）**：新增 `[data-theme='tech']` 深色科技旗舰主题（蓝图网格底垫+基准线遥测轨道发光节点+今天信号青扫描线三角脉冲+等宽数字），设为**默认主题**（`localStorage` 已存偏好用户不被覆盖，沿用 2026-06-16 主题切换器机制）；classic/warm/dark 三套渲染结果零污染，全部结构性改动圈在 tech 选择器下；零外部资产（系统字体栈+CJK 回退 `PingFang SC`/`Microsoft YaHei`/`Noto Sans CJK SC`，等宽 `ui-monospace`/`JetBrains Mono`/`Cascadia Mono`/`Sarasa Mono SC`）；对比度全达标、焦点可见、`prefers-reduced-motion` 沿用既有先例停脉冲。QA 修复 `c6eaa4d`：@xyflow 依赖图第三方控件（缩放条/归属徽标）随 tech 主题去白块。纯视觉轮，业务逻辑/路由/契约零改动。本收口轮（本条）三包 `verify:all` 复核全绿（contracts 214 / server 278 含 e2e-pillars 3 / console 125）+ `health-check` InMemory fixtures 起服复核 9 页 0 错 0 白屏。截图落 `docs/screenshots/tech-restyle/`（18 张，tech 9 页 + 看板 + light/dark 回归 + identity 登录条）。决策：`decisions.md` D-084（新增，用户 2026-07-12 拍板科技风方向）SUPERSEDES D-060（Aurash 暖纸风提案就此关闭，全文移 `docs/archive/decisions-archive.md`，`open_for_decision` 移除 `UI-RESTYLE-AURASH` 条目）。
- 2026-07-12 **路线 v4 五把刀收口（刀②-⑥，`ff35a5b`→`d1a8aed` 共 17 commit + 本收口 docs commit，VERSION 0.11.5→0.15.5）**：刀②`IDENTITY-LITE`（I1 服务端 `4a2acd9`/`ef1892a`：轻身份契约+PIN scrypt散列+会话表SessionManager+服务端actor注入6写路由+双模式main.ts接线；I2 console `e830651`/`8978308`：IdentityBar登录UI+queryKey身份维度+PmCreatePanel ownerId自由文本→选人+写门禁用）；刀③`MY-VIEW`（`6be2545`/`bb3f70d`：DepNode加ownerId机读键+我的视图页，我负责且未被卡住∩被卡分组，按session.memberId客户端过滤）；刀④`STORE-SPLIT-SQLITE`（SS1 `47c2f65`/`ed636eb`：GovStore拆PmCoreStore/ArtifactStore/ScheduleStore三域+id/clamp抽纯模块；SS2 `ddc275b`：排班域resourceSessions/relayHandoffs补落盘合一schedule-sessions.json；SS3 `244fba3`/`1616c4f`：SqliteGovStore真实现node:sqlite文档式行存+PRAGMA版本门+迁移脚本+TEAMHUB_GOV_BACKEND opt-in，默认JSON零变化；**增量边界**=gov三域已可选SQLite，kb/inv/baseline仍JSON留后续）；刀⑤`LEARN-DIRECTION-REDESIGN`（`f98c9d3`/`7841a9c`：跨工种学习地图ROBOTICS_LEARNING_MAP+AI边界横切+sim2real种子缺口，缺人方向页翻转为学习方向，identity模式个性化排序）；刀⑥`AUDIT-DEBT-2026-07`（`538ea9a`/`5831915`/`e3e605b`/`d1a8aed`：GroupKind闭集放宽+convergenceScope收口+pm-requests解绑robotics硬编码+main.ts接线TenantConfig+工具条刷新按钮归一注册+词汇覆盖层支持新增key）。本收口轮三包 verify:all 全绿复核（contracts 214 / server 278 含 e2e-pillars 3 / console 125）+ `test:local -- e2e-pillars` 独立复跑 3 绿 + 版本号逐 commit 核对无漏 bump；红线全守（pinHash 全程未出现在响应/日志/commit、Task 无新增 dueDate、匿名模式零变化、SQLite 默认不启用）；各步偏离详见 `product-redefine-2026-07.md` §4.2/§4.3/§4.4/§5/§9 实现落点注记。
- 2026-07-11 **刀① BASELINE-CORE 实现轮（六步 + 收口，`d3db6fe`→`e3a76a9`，VERSION 0.10.3→0.11.5）**：倒排基准线内核全栈落地——S1 Season 接线（`GovernanceSnapshot.seasons: Season[]` + GET /api/seasons，照 GET /api/groups 直读快照，minor v0.11.0）；S2 baseline zod 独立域文件 `baseline.ts`（SeasonBaseline/BaselineMilestone/TaskInvestment + 三路由契约，Task 加 `milestoneId?`/`investment?` 两 optional、永不加 dueDate）；S3 独立 `baselineStore`（InMemory+File 两实现 + 独立落盘 `baseline.json` + `TEAMHUB_BASELINE_DATA_FILE`，fail-closed）；S4 GET/PATCH/POST-pass 三路由挂 `registerPmCoreRoutes`（过门写鉴权 + I0 剥 `passedBy` 沿 `confirmedBy.omit` 先例 + 证据后缀白名单加 .mp4/.mov/.webm）；S5 `deriveBaselineDrift`（周粒度红黄绿，常量 lookahead=2 周/完成度阈值 0.5）+ `deriveGroupsBehind`（组归因、无 memberId）+ `deriveInvestmentWarnings`（future×high 连续 2 周零进展=正在砍未来）；S6 console 总览 `BaselineOverview`『基准线 vs 实际』首屏 + 三版车模板 v1（正向展开+尾段倒推混合）seed + `PmCreatePanel` 投资三维录入。本机三包 verify:all 全绿（197/232/96）+ e2e-pillars 3 绿 + Playwright 首屏走查。红线全守（Task 无 dueDate / 落后单位=里程碑·组不点人名 / 独立 store 独立落盘 / 证据只存 artifactId / 版次裁剪留痕 `mergedFromVersion`）；实现期偏离全回写 `baseline-design.md` §7。
- 2026-07-11 **产品重定义文档轮（D-083）**：`docs/design/product-redefine-2026-07.md`（DECIDED：开源·防爆肝双主轴·四把刀·宪法修正 G4/I0/AI 三红线·收窄砍推迟清单·解耦审计账单·路线 v4）+ D-083 落 ADR + `core-plugin-architecture.md` PROPOSAL 搁置归档 + backlog 增防爆肝任务簇/翻 07-03 旧状态 + AGENTS 同步注记。解耦审计 = `wf_66e13d79-814`（3×sonnet 逐文件核实四刀阻力 + 坐实 GroupKind/convergenceScope/pm-requests 硬绑 robotics/main.ts 未接 tenantConfig 四处错位）。
- 2026-07-03 **实现五件套终态 `a64e7bc` v0.10.3**：daily-plan-presets（D-082）+ console 装配接线 + L6 入站白名单 + 表单 UX 审计修复 16 条 + 本机 506 测全绿/health-check 8 页 0 错（详 completed-log）。
- 2026-07-01~07-02 **模块化阶段一（feat/plugin-core，6 commit `6fc32fb`→`c84f4a9`）**：剪两条跨域 import 环（growth→governance / pm-requests→relay）+ 装配外壳（`ModuleDescriptor`/`TenantConfig` 装配契约）+ `governance.ts` 拆 `pm-core.ts`+`schedule-infra.ts` + `RobotTarget` 枚举渗透改 optional（Task/Project）+ 迁移脚本 `scripts/migrate-robottarget.mjs` + fixtures 拆 per-module builder + `verticals-robotics` 词汇包成形。07-02 WSL 验证：三包 `verify:all` 432 测全绿 + 迁移脚本真 gov.json 副本零丢失 + Playwright health-check 8 页 0 错等价 master + 真实未迁移 gov.json 新 schema 加载成功。07-03 FF 合并 master（`c84f4a9`，VERSION 0.9.7），已 push origin。设计真相 = `docs/design/modularization-feasibility.md`（已执行）；core-plugin-architecture.md PROPOSAL 已于 07-11 搁置归档（D-083，现在 `docs/archive/`）。详 decisions.md D-081。
- 2026-06-22~24 **批量落地（OOP 三批次 / 视觉打磨 A-H+暗色模式 / Playwright 体检收口 / 库存落盘 / D-080）**：OOP+代码质量路线图批次 A+C1（`882fd56`）+ 批次 B SSOT 收口（`2fd6970`，v0.7.5→0.7.6）+ 补测批次 C2/C6/C7/C8（`21ea803`）+ C3/C4 抽纯模块（`c7d410f`）；视觉打磨 A-H 全批（尺度 token/暖色补全/次级按钮/输入边框/可访问性/文案去 AI 味/暗色模式第三套主题，`54dcfa6`…`657f208`）；Playwright 体检收口进 `hub-console` 自身 devDep（`2231903`/`b4ac8d8`，`e2e/health-check.cjs`+`npm run health-check`，不再借 feiyue-test）；库存落盘接进启动路径（`655bb1e`，`TEAMHUB_INV_DATA_FILE`）；D-080 录入收尾（`3e12e50`/`8b4cfcc`：新建任务抽屉化 + 连依赖/暴露需求 tab 下线 + `requires` 全砍，FileGovStore fail-closed 校验，旧 gov.json 需先迁移）；模块化可行性研究（`4b4ad6d`）；daily-plan-presets 设计稿落档（`8213195`）。
- 2026-06-21 **图纸文件链路收口 + 表单控件修缮 + 部署/版本修复（D-078）**：系统排查需求分析后两类问题——①图纸「半成品」(只登记无上传/存储无定论/下载摆设) ②表单控件错配(适配机器人·赛季窄下拉需手填、编号位过宽)。落地：契约 `ArtifactRef.storedFile`(可选指针，向后兼容)+`POST /api/artifacts/:id/upload`(multipart·50MB·后缀白名单·先验存在避孤儿·继承 H3 鉴权)+`artifact-storage.ts` 存储接缝(本地卷·D-025 不进 git·MinIO 留接口)+`GovStore.setArtifactFile` 三实现；console 上传/替换 UI + 文件/云端链接**双存**(行内双按钮)+条件下载(消灭摆设)；适配机器人/赛季 datalist 组合框(候选+手填，复用台账)+编号位 `.kb-field--narrow` 收窄；修部署 env-var(`ARTIFACT_ROOT`→server 实读的 `TEAMHUB_ARTIFACT_FILES_DIR`，原线上恒 404)；修版本号 lock 漂移(hub-contracts/console lock 停 0.0.1→0.4.1，硬化 `bump-version.sh` 同步 lock + `check-version-bump.sh` 加漂移哨兵)。worktree 隔离开发→rebase 上并发 session 提交(零冲突·保留其未提交 WIP)→FF 合并→push。三包 verify 绿(164/194/44)+真机 API 冒烟全过。
- 2026-06-20 **版本号自动更迭失效修复（D-074）**：对照 feiyue 找根因——AGENTS 从无「要 bump」规则 + 服务端缺 Tampermonkey 那种「不自增不更新」的天然强制函数 + D-052 半拉子修复（只让 `status.ts` 读 package.json、bump 留成没人执行的 TODO）+ monorepo 多包无单一产品版本 + 零 tag。方案落地：根 `VERSION`（产品单一真相）+ `scripts/bump-version.sh`（唯一改版入口，同步三包 package.json）+ `scripts/check-version-bump.sh` 哨兵挂 `pre-commit.sh`（warn，`VERSION_BUMP_STRICT=1` 升硬门）+ AGENTS §7/§2.3 成文。据 git tree 两法收敛**当前版本重定为 v0.4.0**（产品线 v0.3→v0.4 / TeamHub 纪元 0.1→0.4），三包 0.1.0→0.4.0。
- 2026-06-19 **无人值守实现轮**（4 commit `9861850`→`5cb84cb`，逐项 verify 全绿 + push）：① INV-BOM-CORE 第三支柱内核（contracts inventory.ts schema+派生 deriveInventoryLedger/deriveShortfalls+动作语义 applyPartAction / 落盘 FileInvStore / 三端点 / console 零件×车矩阵+一句话快记+缺料告警 / demo seed，照 inv-bom-core.md locked 零猜测）；② PRESENCE-IMPL 安全增量（deriveDisplayCode+SharedResource season/version/displayCode+repair/retired/disassembling 态+canBoardResource 接力释放级联 / RelayChainView 多车接力链+卡片页降级 / 反监视护栏升结构约束 / seed 26R1·26R2）；③ WSL2 真机 Playwright 验收全 PASS（矩阵活体 26R1/26R2 / damage 快记 9→8 刷新 / 零 memberId 泄漏，截图入库 docs/screenshots/wsl-*）；④ lark-toolkit execa bin 修（'lark'→'lark-cli'）。DEFERRED（记设计稿 §7.1 + commit）：PRESENCE fixture 调和+总联调=全组各一人语义、KB-LARK wiki/drive→KB 摄入——均无安全默认/需 schema 定稿。
- 2026-06-19 在场排班优化（D-072）定稿：对抗式设计审查（`wf_2f31074c-523`，55 claims·5 视角·对抗核实）+ 用户多轮拍板 → 设计稿出定稿版（两视图删甘特 / 单层「车=带编号对象」/ 删程序组领任务留汇报视角 / 宏观维修态+注释 / 接力释放「谁可下班」语义 / 预留=扣减归车 / v=代次 / 总联调全组各一人 / 多车并排）；零代码，下一轮 PRESENCE-IMPL 起实现。
- 2026-06-19 差异化排班 S1+S2+S3 + 文案 + 审计长尾（D-069 P1 连续自主批，5 commit `ece2a3b`→`052285d`）：SCHED-WIRE-EXISTING / SCHED-MEMBER-AVAILABILITY / STUDY-NARROW-DERIVE / CONSOLE-COPY-HUMANIZE / AUDIT-FIXES-LONGTAIL，每条独立 workflow + 2-lens 对抗核实，A 档全清。
- 更早完成条目（v2 后硬化 / 图纸档案 v2 / Harness 减负 / …）→ `docs/archive/completed-log.md`（完整时间线，亦见 `git log`）。
