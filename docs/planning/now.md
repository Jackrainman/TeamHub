# Now

> 唯一当前战况源。Team Hub 方向已由 D-024 覆盖旧 markdown-only pivot。pre-pivot 历史快照 → `docs/archive/v0.3-pivot/`。

```yaml
mode: collab_design
stage: 2026-06-15 **v1 能跑产品**（用户以 PM 视角定「先做出来一个能跑的、接口都能接进来」，明确「真实上游 by-design 后置·别纠结没接」+ 用 workflow 连续做掉）：①**治理快照落盘** `FileGovStore`（1:1 镜像 FileKbStore：原子写+writeChain 隔离+seed-on-fresh+fail-closed）+ `main.ts` 接 `TEAMHUB_GOV_DATA_FILE` + compose `hub_gov` 卷——**PM/治理数据重启不丢**（补上 KB 已有、PM 缺的落盘硬伤：原 InMemoryGovStore 重启回演示假数据）②**图纸提交日志**：`ArtifactRef` 加 `mechanism/revision/submittedVia` + 搬进 `GovernanceSnapshot.artifacts` + `GET /api/artifacts` 读快照 + 8 条版本日志种子（底盘 v1/v2/v3·抬升机构·夹爪·视觉固件）+ console 新**图纸档案页**（按机构分组·日期倒序·版本徽章·关联commit·地址）——「某机构几月几号第几版」一眼可查·可存档 ③**删 mock 数据源模式**（删 api/mock/*+数据源开关，单一真实后端，重写 client 测保覆盖，去 8 条「切演示」误导文案）。6 原子 commit `5a2c96d→01d06f4`（feature 分支→ff master→push）。**7-agent workflow `wf_eb55b2ca-8fe`**（design-lock 验架构+出 DoD→后端→删mock→档案页→3-lens 对抗核实[I0护栏/正确性回归/DoD诚实]全 ship·mustFix=0）。**主循环独立核实**：三包 verify:all 绿（hub-contracts 47/hub-server 89[+4 gov-store-persist]/hub-console 6）+ 真机 smoke（4199 单端口，/api/artifacts 返 8 条版本日志、FileGovStore 落盘 15KB 文件 8 artifacts+8 tasks 重启不丢、档案页视觉验收、mock 消失、4177 未碰）。老实定位：真实 status/图纸上游派生仍未接（seam 留 statusSource/submittedVia 枚举 + store? 注入口）·种子=测试数据（空板起步后置 1-flag）·2 条测试断言强度 nit（功能已 smoke 实证）。**权威=decisions.md D-061**。前序（D-044~D-059：三支柱读写/设置页/KB导入/自迭代外环/拖拽连线/录入浮层/集成进设置/文案humanize/审计7必修）见 decisions.md。
stage_goal: 以 D-039 + plan file noble-soaring-gem.md + AGENTS.md 为事实源，演进留地基、AI 退出治理（治理只做人读说明视图、大三/学长判断），推进战队内部协作工具三支柱：① 战队知识库（规范+资料+调试归档+跨赛季沉淀，复用 growth.ts KnowledgeNode + 移植 Probe_Flash IssueCard→Archive 闭环）② 项目计划表（D-041：任务为核心·全员可见·依赖图+卡住必带原因·无甘特·不按人天数；复用 Task/Dep/Need 补 due/优先级 + 依赖录入）③ 库存/BOM（P1，自保鲜护栏）；共享底座（持久层 + real CRUD 路由）先行、一次三根受益。设计北极星=比死表省事/用着就更新(派生优先)/AI 只当仓管·转译不下判断/人在环/小作坊轻量。D-041 构建定调：中心实体=任务（围任务转不围人转）/视图解耦（项目进度 vs 个人成长共享任务底座）/“项目”=标签/先地基后视图/“和人关系”三堆判定尺（事·物=安全｜找谁对接=安全止于找谁｜谁快谁慢=人治封存）。治理 AI 派生整簇（D-032~035）挂起、想法不丢，复活触发=未来确认要 AI 参与治理判断。AI 每轮默认读 AGENTS.md + now.md + git 状态，backlog/decisions/设计文档按条件读取（D-066：agent-state.json/completion-model.yaml 已退役进 archive/legacy-harness，now.md 是唯一活状态源）
current_task: null  # 依赖图运维操作 done(D-063，2026-06-15)：任务状态流转(POST /api/tasks/:id/status，statusSource 钉 console·C5)+连线作废软删除(POST /api/dependencies/:id/waive→waived，toDepGraphView 跳过隐藏·satisfied 仍可见)+console DetailPanel 标完成/重开/全状态下拉(搁置二次确认)+连线点选删除确认条(优先于残留横幅·deleteKey 禁)+横幅 4s 自动消失；受限状态机迁移非通用 CRUD(C3)、POST 子资源(过 H3 鉴权)；contracts48/server101/console7 全绿 + WSL2 真机部署活体验收(127.0.0.1:4177)。+ D-064 commit+push 默认化扩展到交互式会话(AGENTS §6.0 已改)。前序集成模型 done(D-062)。下一步候选见 frontier 或待用户排期(INV/真实上游接入/部署)
repo_sync: "2026-06-16：本机与 origin/master 已同步（D-065 + D-066 harness 全批 + 闭环审计四收口修：backup 读回复用启动加载器当 oracle / pre-commit 密钥正则补 -_ 命中 sk-ant-·sk-proj- / PROTOCOL 死链重指存档 / honesty flag——均已 push）。push 凭证走共享库 ~/ruolin_huang/.gh-credentials（Jackrainman fine-grained PAT·全公开仓+Contents 写），TeamHub local credential.helper 已指向它，后续按 D-064 默认 commit+push 即可（见 memory teamhub-remote-access）。2026-06-18：rainman WSL2 真机经 git bundle 过 SSH 从 D-061 同步到 D-068(230c38e)+verify:all 全绿+4177 起服验收（D-069 验收段），真机现 = D-068、push D-069 后将落后 1 commit、下次演示前再 bundle 同步即可。"
frontier:                                # D-052 提案审查后重排(用户 Q1–Q4 拍板)。低风险批已 done(CONSOLE-LOWRISK-BATCH-D052：版本跟随pkg/删5死导航/Mock文案/重复真相下沉)；下批=依赖图升主舞台+集成进设置
  # ✅ DEPGRAPH-OPS done (D-063, 2026-06-15)：依赖图任务状态流转(标完成/重开/全状态机)+连线作废软删除(waived 隐藏·保留审计)；POST 子资源过 H3 鉴权·statusSource 钉 console(C5)·响应剥 confirmedBy(I0)·删除条优先于横幅·横幅 4s 自动消失；contracts48/server101/console7 全绿 + WSL2 真机活体验收。+ D-064 commit+push 默认化(交互式会话也默认提交，AGENTS §6.0)
  # ✅ DEPGRAPH-ENTRY-OVERLAY done (D-056, 2026-06-15)：依赖图右上角录入按钮→近全屏遮罩浮层承载 PmCreatePanel(点空白退回) + 看板卡片「在依赖图查看」跳转 + I0 ownerLabel 从节点常显降级到 DetailPanel+反排名说明；2-lens 对抗核实(wf_9a77daa8)双 ship/mustFix=0；console verify:all 绿
  # ✅ INTEGRATIONS-TO-SETTINGS done (D-057, 2026-06-15)：用户可见「适配器」→「集成」(zh+en，含 metric/panel/event 标签) + 设置页 IntegrationsSection 只读子节(复用总览 getOverview·同 queryKey 缓存共享) + 总览删 adapter 详情面板改一行「集成已移到设置页」链接(onNavigate)；2-lens 对抗核实(wf_f40f5aea)双 ship/mustFix=0(只渲染 adapters 描述符·无人维度)；顺手删 2 个孤儿 i18n key；console verify:all 绿
  - DEPGRAPH-AI-AUTODRAW                # AI 自动布大致 DAG + 人拖拽微调(Q2 新诉求；AI 只建议不落库，依赖 Hermes 触点、后置)  ← 跳过(Hermes 门)
  # ✅ CONSOLE-COPY-HUMANIZE done (D-058, 2026-06-15)：6 处 console 文案(pm.create.title/subtitle·needDescription·kb.empty·closeout.intro·success.knowledge) + 后端 KB_SIMILAR_NOTE + mock note 去治理黑话(协作真相/词重合/同因/派生/归组不归人)，只改 value 不动 key·zh/en 同步；护栏语义全保留(A4 不断言/C2 不比快慢/A1 按组不点人)；'踩过的坑'前缀保留(测试断言+本就人话)；2-lens 对抗核实(wf_8c5051bf)双 ship/mustFix=0；两包 verify:all 绿
  # ✅ AUDIT-FIXES-2026-06-14 done (D-059, 2026-06-15)：7 部署前必修全做——H1 依赖环(computeCriticalSet visited 守卫+wouldCreateCycle 纯函数+POST /dependencies 拒自环/成环)/H2 FileKbStore 写链失败隔离(catch reset+清 tmp)/H3 写端点 Bearer 鉴权+每 IP 限流+bodyLimit 256KB+main.ts 拒非 loopback 裸暴露/H4 status/statusSource 钳到安全 enum(拒 done/shelved/derived)/M6 create 响应剥 confirmedBy(I0)/H5+M11 compose 删幻影 Postgres+接 KB 持久卷/M9 errorCode 改同日单调序号防碰撞；+11 测(audit-fixes 10+H2 1+cycle-guard 6)；**3-lens 对抗核实(wf_99ea69cb：安全绕过/正确性/回归完整)全 ship·mustFix=0**(无可绕过、7 条全落、无回归、compose 闭环可启动)；三包 verify:all 绿
  # ✅ V1-FOLLOWUPS done (D-067, 2026-06-16)：图纸档案可写一次收口——④POST /api/artifacts(CreateArtifactRequest mechanism/revision 必填·submittedVia=server 钉 console·H3 钩子自动 gate) + appendArtifact 写白名单第五个 append(三 Store：InMemory push/File persist 落盘/Sqlite stub) + console ArchivePage 登记表单(空档也渲染) ③OverviewPage→档案链接 ①硬化 2 测(routes 证 store-sourcing 非 fixture / persist 断言 mechanism·revision·submittedVia round-trip)；②空板起步早于 D-065 已 done。写路径测试补全(pm-routes +2:201 夹带 lark 被 omit·缺字段 400 / client.test M21 补 createArtifact)。4-phase workflow(design-lock 15 接缝→后端→前端→2-lens 对抗核实[I0/反排名 + 正确性/回归/DoD]双 ship·mustFix=0·i0Clean) + 主循环独立核实(三包 verify:all 48/109/9 + 真机 4188 smoke POST→submittedVia=console·GET 8→9·落盘存活)
  # ✅ THEME-SWITCHER done (D-068, 2026-06-16)：设置页运行时风格切换器(经典绿/暖纸 Aurash)——纯 CSS-variable 换肤(:root[data-theme='warm'] 覆盖,全站组件已消费 var(--*) 故零组件改动,不引 Tailwind/Radix)、架构镜像 i18n 语言切换(theme/index.tsx ThemeProvider+localStorage teamhub.theme+dataset.theme+normalizeTheme 纯函数单测)、默认 classic opt-in 暖纸、复用 segClass/.seg 零新 CSS、i18n 4 键 parity；后端零改动；D-060 护栏守住(无 preflight 风险/不碰 I0 三串/未拷 xju-feiyue token)；verify:all 48/109/11 全绿 + 构建产物&4177 活体 serve 核实(dist 含暖纸 token+JS 含 teamhub.theme)。已知限制=dep-graph 连线色(EDGE_COLORS JS 常量)不随主题变；真实视觉切换需用户 4177 浏览器实眼验收
  # ✅ 验收 + D-069 立项 done (2026-06-18)：WSL2 真机同步 D-061→D-068 + 三包 verify:all 全绿 + 主题切换器 Playwright 实测过(SWITCH_WORKS=true)；用户新功能(排班/缺人/学习建议)经 workflow wf_6f935ab0-027 评估 + 两 §5 拍板(A1 组级均衡 + B1 窄义)进 backlog 合宪立项(不复活 D-039) → frontier 重新有可执行项
  # ✅ GAP-DIRECTION (S2 缺人方向) done (D-069, 2026-06-18)：DirectionGapSchema+GroupGapsResponseSchema(对齐 OverloadSignal·零人字段) + deriveDirectionGaps 纯函数(按 providerGroupId 聚合 open/escalated Need·neededSkills 并集·融 rootBlocker 频次定 severity·null 组跳过·无缺口空数组沉默) + GET /api/group-gaps + console「缺人方向」页(组级卡片·severity 徽章·缺的方向 chips·A1 护栏说明常显·永不下钻到人)。**2-lens 对抗核实** wf_f5bd0491-6b5(I0/A1 暴露 + 正确性/DoD)双 ship/mustFix=0——无任何结构性暴露路径(schema 零人字段·derive 不碰 claimedByMemberId/confirmedBy·console 不渲染人·输出组键无法 groupBy 排名)。三包 verify:all 绿(contracts 54[+6]/server 110[+1]/console 11)+活体 curl /api/group-gaps 返组级 gap-grp-program(CAN/RTOS)·JSON grep 无人字段。commit 73373ca→f2ee082
  - SCHED-WIRE-EXISTING             # S1 接出已有 derivePresenceSchedule(组键·零新维度·第一次真正接通死代码)  ← ready (D-069)，frontier 下一项
  # 后置(D-069)：SCHED-MEMBER-AVAILABILITY(S1 课表→组级容量·A1 已清宪法门·依赖 wire) / STUDY-NARROW-DERIVE(S3 窄义纯函数·私下推渠道 gated on Hermes/lark) / STUDY-BROAD-D039-REVIVAL(HARD-GATED 封存·B1 已拍)
  # 仍卡用户排期或外部基建：DEPGRAPH-AI-AUTODRAW(Hermes 门·跳过) / KB-IMPORT-FOLLOWUP nit①② / INV 第三支柱(P1 最后) / 正式部署上线 / Hermes·KB-LARK(hardblock LARK-BIN-PROBE) / 治理派生整簇 D-032~D-035 仍挂起
# P1：INV-BOM-DESIGN(库存/BOM，自保鲜护栏=等 AI 读出车图核数/算余量再做，绝不再造静态表 P13)+飞书 Bitable·sheets 读写+修 lark-cli bin bug；P2：资料/代码批量整理(AI 安全车道)、给老师项目级汇报
# 2026-06-15 小 chore(fb228a5)：console 侧栏加「库存/BOM」灰色「开发中」导航占位(Boxes 图标·复用 page===undefined 灰禁用机制·tooltip nav.inv.soon·zh/en parity)——纯 UI「在做」信号，**不接后端/不建 INV schema·不翻 PILLAR-INV-BOM 的 done**，真正第三支柱仍按 P1 自保鲜护栏后置(completion-model PILLAR-INV-BOM 仍 not-started)
# 挂起(D-039 AI 退治理，spec 留、复活触发=未来要 AI 参与治理判断)：GOV-MEMBER-STATUS-DERIVE / GOV-RULES-LAYER-DESIGN + D-032~035 治理派生整簇(GovernanceCue/silence 分河/give-floor/k-anon/audience 路由/阈值派生) + 自动派活 + freeIdle·双写债
blocked: []
open_for_decision:                       # ARCH-PATH(D-028)/提醒(D-026后续)/资源(D-029)/idle三态+静默(D-032)/受众路由(D-033) 已拍；SCHED-WINDOW/INVITED 排班可视化细项随 D-039 治理挂起(见 decisions.md D-029)；以下待用户线下细化
  - ARTIFACT-VERSION-SEMANTICS           # 图纸版本语义：**v2 已落地最小+回退**(D-071：机械/电路分组+赛季/车/机构/自增版本号 server 派生+最新即权威派生回退，纯 append-only)。**仍 open 的进阶语义**=手动钉旧版 pin（走 append-only supersede·未做）/按车分支/跨赛季权威指针（D-036，别做完整 PLM）；真实文件上传 = HUB-ARTIFACT-STORE-MECH(§8)；电路驱动命名规范 = 用户内部待定
  - REMOTE-ACCESS-DEPLOY                 # 远程部署=实验室 LAN+隧道方案与鉴权（D-036，§8 审批门后，独立基础设施轨）
  - GITHUB-TO-LOCAL                      # 程序代码 GitHub→本地 Forgejo 迁移=考虑中（D-038，2026-06-12）；TeamHub 只消费 gitCommit 信号、不改 git 唯一真相
  - PULL-CLOUD-CODE                      # 定期 pull 云端代码/EDA 到本地备份=考虑中（D-038，与电路 EDA 云端引用相关）
  - UI-RESTYLE-AURASH                    # console 换 Aurash 暖纸风=PILOT-FIRST 已评估、**低优先级延后**（D-060，docs/research/aurash-restyle-assessment.md）；动手须先过决策门，业务逻辑先行
post_pivot_registry:
  - SKILL-PROTOCOL-V1                    # 已落地草稿；作为治理触点层 skill 契约底座保留
  - BRIDGE-01-ROSTER-SCHEMA              # 模型并入治理 Task/progress；数据载体被 D-026 路线 A 反转（系统库做真相）
  - TRAIL-01-VIEWER-DESIGN               # 等治理 event/archive/artifact 原料足够后再设计
frozen:
  - ProbeFlash-v0.3.0                    # 代码已删(git 历史保留)；致命补丁走 git revert
```

## 当前任务

_无。HUB-COMPOSE-SMOKE 已闭环：Docker CLI/Compose 可用后，修复 Hub 镜像 runtime 依赖打包问题并跑通 `scripts/verify-hub-compose.sh`，已完成 Hub + Postgres build/up、health/API/static console smoke 与自动清理。下一步从 frontier 选候选（精简 AGENTS §2 铁律 3：原子单元 + completion gate + 单 commit/push；无 workflow 编排能力的串行轨工具改读 archive/legacy-harness/AGENTS-serial.md）。_

## 架构定位（D-026 立魂 → D-037 回中）

四层架构 + 路线 A 详见 `AGENTS.md §1` 与 D-026/D-037（不在此重复）。规则协调域是新增核心；已建 Hub 壳子（hub-server/contracts/console/Compose）作为触点/集成 + 展示底座保留。深设计见 `docs/design/team-hub-concept.md`（canonical）。

**产品定义（D-026 立魂 → D-037 回中）**：定位从”运维 / 观测控制台”→”制度化进度治理系统”(D-026)→ **协作中枢：CASE 工具 + 团队交流中心 + 战队数据库**(D-037，给学长减负 / 给学弟指引 / 项目同步进度表)。系统是大脑 / 飞书是脸、不双写、无硬截止只轻推、**人键只回本人当帮助、第三方只见结构键（核心不变式 I0）**、给被卡的人正名而非抓摸鱼、给新人安全网。**D-039 第一轮落地细化（2026-06-13）**：演进留地基 + **AI 退出治理**（治理降为人读说明、大三/学长判断）；产品 = 战队内部协作工具三支柱（知识库 / 项管看板 / 库存-BOM）；治理 AI 派生挂起、想法不丢（见 `decisions.md` D-039 + backlog "挂起" 段）。事实源 `docs/design/team-hub-concept.md`（涉及产品形态 / 领域模型 / 中央视图 / 飞书·Git 边界时优先读）。

## 阻塞 / 待拍板

- ~~架构走法（D-026 开放项）~~：**已于 2026-06-11 拍定（D-028）治理为主轴**——治理实体进 hub-contracts 核心域（common/governance/growth/attribution），成长轴落同包独立文件域；已落地。
- ~~提醒可见范围/送达模型~~：**2026-06-10 拍定 + D-037 收窄**（私聊本人、升级的是事不是人、AI 起草不发送/建议不判定/检索不评价；**D-037：人键提醒只回本人 + AI 建议、不上报队长，问责上移废除**）——见 `decisions.md` D-026 后续 + D-037。
- **真实外部 adapter**：Hermes / 小龙虾 / Claude Code 真实接入需要用户提供运行方式与权限；AI 当前只能做 mock-first 适配设计。
- **真实服务器写入**：Forgejo/Gitea/bare git 部署、SSH、systemd、80/443、真实数据迁移均需用户白天审批后再做。

## 已冻结

- ProbeFlash v0.3 代码（原 apps/desktop、apps/server、dev-start.sh、release 流程）：已于 2026-06-09 删除；完整代码留 git 历史，精华见 `docs/archive/v0.3-closeout/PROBEFLASH-V03-ESSENCE.md`；致命补丁走 `git revert`。
- pre-pivot backlog 全部任务（TECH-* / AIREADY-* / REALAI-* / CODECTX-* / DEP-* / DATA-* / UI-* / CORE-* / SEARCH-*）：不再认领；详细见 `docs/archive/v0.3-pivot/backlog.md`。
- **原 BRIDGE / TRAIL markdown-only 候选**：已被 D-024 Team Hub 架构覆盖，后续只作为 Hub BridgeState / Trail 能力重评，不按旧任务直接认领。

## 安全边界（pivot 后仍生效）

- 不动 v0.3 server / SQLite / API（致命补丁除外）。
- AI / Skill / Hub adapter 不读 / 打印密钥（`.env` / `*key*` / `*secret*`）。
- 真实 Hermes / 小龙虾 / Claude Code / 飞书 / Git forge smoke 由用户线下配置；AI 只做 mock-first 与只读诊断。
- 不在未审批情况下写真实服务器、SSH、systemd、80/443 或迁移真实数据。

## 最近完成（详见 `git log`）

- 2026-06-18 图纸档案 v2 机械/电路分组版本库 (HUB-ARTIFACT-ARCHIVE-V2 done, D-071) — 用户 PM 视角讨论后把「通用归档」重塑为「机械/电路分组的图纸版本库」（程序组排除·全在 GitHub）。**数据模型 A 扁平/派生分组**：ArtifactRefSchema 加 5 个 `.optional()` 字段（ownerGroup/season/robotCode/versionNo/subType，词汇对齐 GroupKindSchema，全 optional 故 8 seed+旧 JSON 仍加载）；写契约 `.superRefine`（电路必须有 subType·机械必须无）；**server 派生** versionNo（`nextArtifactVersionNo` 四键 max+1）/kind（`deriveArtifactKind` 电路驱动→firmware·余→report）/revision=`v${n}`，3 个 store 不动（两纯函数落 `artifact-version.ts`·前端版本预览复用同函数零漂移）；console 表单重做（`.seg` 段控 组/R1R2/子类型·赛季 select 默认 guessSeason·机构新增/下拉·版本预览·两级分组 ownerGroup→mechanism +「未分组/历史」桶承接旧 seed·max 版打「当前版」徽章·关联仓库提交仅电路驱动显）。**回退=最新即权威**（派生，纯 append-only）·手动 pin 不做（违 C3，未来走 append-only supersede）·进阶语义 ARTIFACT-VERSION-SEMANTICS 仍 open。**workflow `wf_57c7f730-a0d`**（Contracts/Server[opus]→Frontend[sonnet]→2-lens 对抗核实）裁 i0Clean=true+抓 1 真 bug（空档案首条录入 effectiveMechanism 与渲染条件不一致→按钮永 disabled）主循环收口。三包 verify:all 绿（contracts 62/server 115/console 11）+本地活体 smoke（机械 v1→同键 v2·电路驱动→firmware·缺/多 subType→400·夹带人字段被 strip·落盘 artifact 无人字段）。事实源 D-071 + plan `scalable-noodling-brook.md`。
- 2026-06-18 GAP-DIRECTION 缺人方向 (S2 done, D-069) — 用户「改完之后顺推接下来的任务」→ harness 减负后接 frontier 最 ready 项 S2 缺人方向（D-069 拍 A1 组级均衡·合宪 clean）。**契约层**（commit `73373ca`）：`DirectionGapSchema`+`GroupGapsResponseSchema`（governance.ts，逐字对齐 `OverloadSignal` 范式·组键+factStatement+detectedBy:'derived'·**零 memberId/score/percent**）+ `deriveDirectionGaps(snapshot,now)`（新 `direction-gaps.ts` 纯函数：按 `Need.providerGroupId` 聚合 open+escalated·neededSkills 并集去重排序·融 `deriveBlockAttributions` rootBlockerGroupId 频次定 severity[emerging/pressing]·null 组跳过·无缺口空数组沉默 A4）+ 6 单测。**路由+视图**（commit `b35d1ae`）：`GET /api/group-gaps`（`GroupGapsResponseSchema.parse` fail-closed·clock 注入确定性）+ console「缺人方向」页（组级卡片·severity 徽章·缺的方向 chips·`gaps.note` A1 护栏说明常显·**永不渲染人/不下钻**）+ ConsoleLayout 导航(Compass)+App 路由+`client.getGroupGaps`+i18n 12 键 zh/en parity+gaps CSS。**2-lens 对抗核实** `wf_f5bd0491-6b5`（I0/A1 暴露 + 正确性/DoD，均 opus）双裁 **ship·mustFix=0**——adversarial 构造毒化 fixture 也找不到结构性暴露路径（schema 零人字段·derive 不碰 claimedByMemberId/confirmedBy·console 无人维度·输出组键无法 groupBy 排名·非 A2 派单）；唯一理论通道=上游 free-text(neededSkills/Group.name)→factStatement，本 feature 未引入、同字段早经 /api/needs 公开（记上游 sanitation 待办）。2 nit 收口（commit `f2ee082`：schema docblock 去 memberId 字面使 DoD grep=0 / evidenceNeedIds 排序对称）。**验证**：三包 verify:all 绿（hub-contracts 54[+6]/hub-server 110[+1 路由测:200+响应 grep 无人字段]/hub-console 11+build）+**活体 curl** /api/group-gaps 返组级 `gap-grp-program`(CAN/RTOS·severity emerging)·JSON grep 无 memberId/displayName/confirmedBy 实证。**老实定位**：真实 Need 上游派生（git/lark→缺口）未接、靠现有 fixture/录入；severity 的 rootBlocker 融合依赖 deriveBlockAttributions（owner idle 判定走 Member.status，真实 status 派生未接）。frontier 下一 ready 项=SCHED-WIRE-EXISTING（S1 接出死代码 derivePresenceSchedule）。事实源 D-069 + backlog P1 差异化排班。
- 2026-06-18 Harness 减负·对齐 feiyue 轻量模型 (D-070, T1+T2+T3 done) — 用户「feiyue/feiyue-script 的 harness 很合适、TeamHub 过重，查看怎么减负」→ inventory workflow `wf_782c37b4-193`(6 组件 + 2 feiyue 模型并行盘点)定位重量不在 AGENTS.md(D-066 已精简 64 行)而在周边「长记忆+同步机制+留作追溯的旧脚手架」。**T1 瘦身**：`now.md` `最近完成` 历史 → `docs/archive/completed-log.md`(每轮必读 **61KB→25KB·−58%**，留最新 3 条+指针)；decisions 挂起簇 D-032~D-035 → `governance-suspended-decisions.md`(留 stub)；`workflow-evolution`/`docs/agents/workflow`/`skill-library`/2 份 pre-pivot spec → archive。**T2 拆死同步**：hook(`sync-skills.sh`，实测从未在任何 settings.json 注册)+哨兵(`verify-skills-sync.sh`)删除，skill 收**单一真源 `.agents/skills/`** + feiyue 式 `install.sh` 软链进 `.claude/skills/`(修掉 `.claude/skills/PROTOCOL` 指向 D-066 已删 §9/atomic-task 的漂移、现为软链视图)；AGENTS §4 DoD 改引 install.sh。**T3 账本文化**：`decisions.md` append-only→「活账本+归档」(约定写进顶部 intro)，hard-superseded D-043/D-053 压 3 行 stub+全文 → `decisions-archive.md`；**decisions.md 220KB→195KB**。**守恒**：全 `git mv`/`git rm`·零真相丢失·零代码/数据改动(不碰 `apps/`·`kb.json`/`gov.json`)；承重件不动(AGENTS 64 行+I0/C/A 词汇·`legacy-harness` fallback·5 ops 脚本·`preview:local`)。验证：git diff --check + now.md yaml + grep 无悬挂引用 + 4 skill install.sh 软链生效。事实源 D-070。
- 2026-06-18 验收 D-061→D-068 上真机 + 差异化排班/缺人/学习建议合宪立项 (D-069) — 用户「查看 TeamHub 还有哪些没做 + 是否完成验收(没则在 rainman WSL 验收) + 新功能能否进代办：『不同上课时间+平均排班 / 什么方向缺人 / AI 分析谁去学』」。**验收**：rainman WSL2 常驻部署实测停在 **D-061**(落后本机 7 commit·D-062/D-065/D-066/D-067/D-068 都没上过真机)；git bundle 过 SSH 同步到 `230c38e`(D-068)→三包 verify:all 全绿(contracts 48 / server 109 / console typecheck+测试+build)→4177 真机起服(buildId=230c38e·真实语料 kb 84KB+gov·API smoke /api/artifacts=8·/api/kb/similar 召回 ProbeFlash CAN 卡)→**D-068 主题切换器(唯一挂着的实眼验收)Playwright 实测：`data-theme` 切 warm 后 5 token 全切(pageBg/surface/sidebarBg/text/fontTitle)·`SWITCH_WORKS=true`**。老实定位：经典/暖纸两套配色都浅色、明度接近(冷白↔暖白·深绿↔深棕)+serif 只对拉丁字母生效(WSL 无 CJK 衬线)→**机制正确但视觉差异微弱**(印证 D-068「暖纸调色为起步值待微调」)，留用户 localhost:4177 实眼终验。**新功能评估**(workflow `wf_6f935ab0-027`，4-agent：算法/宪法对抗/资产复用→综合)：高度重合**已有但挂起**的积木(`schedule.ts derivePresenceSchedule` D-029 写完零运行时引用 / `growth.ts` D-027 / `attribution.ts`)；拆 **S2 缺人方向=clean**、**S1 排班=needs-guardrail**、**S3 谁去学=命中 D-039 复活触发**。**用户两 §5 拍板**：**A1 组级均衡**(个人课表 private 自愿录入·聚合成组级 headcount·输出无 memberId·拒 A2 按人均摊出勤) + **B1 窄义**(AI 只建议知识点+只私下回本人·拒 B2 把具体人配对缺口) → 合宪形态进 backlog 立项(`GAP-DIRECTION-*`/`SCHED-WIRE-EXISTING` ready · `SCHED-MEMBER-AVAILABILITY`/`STUDY-NARROW-DERIVE` next/gated · `STUDY-BROAD-D039-REVIVAL` HARD-GATED 封存)，**不复活 D-032~D-035**。纯 planning(decisions D-069 + backlog 新「P1 差异化排班」段 + GOV-SCHED-VIZ-DESIGN 部分解封 + now)。事实源 D-069。
- 2026-06-16 设置页风格切换器·运行时主题 (D-068) — 用户「做一个切换风格的功能，在设置里」（澄清「接下来需要排版的部分」= D-060 `UI-RESTYLE-AURASH` 唯一视觉条目）。**纯 CSS-variable 换肤、不引框架**：把 D-060 Phase 0（换 token，<0.5 天拿 80% 视觉）包装成可回退 opt-in 功能——全站组件早消费 `var(--*)`，第二套 token 挂 `:root[data-theme='warm']`（特异度 0,2,0 > `:root`），切属性即整站换肤、绝大多数组件零改动、无 preflight/无 @xyflow reset 风险/零新依赖。**架构逐行镜像 i18n 语言切换**：新 `theme/index.tsx`（`ThemeProvider`:localStorage `teamhub.theme` + `documentElement.dataset.theme` + useEffect 同步 / `useTheme` 未挂即 throw / 纯函数 `normalizeTheme` 供单测不测 DOM）、`main.tsx` ThemeProvider 包 LanguageProvider、设置页加「外观」section（复用 `segClass`+`.seg` 零新 CSS）、i18n 4 键 zh/en parity。**默认 classic 现行绿、暖纸 opt-in**（无 `data-theme` 即像素一致）；暖纸 = 米色面 `#f7f6f3` + 暖白 `#fffdf9` + 近黑 `#37352f` + 发丝线 + 暖炭侧栏 `#2b2922` + serif 标题（Georgia/Songti/Noto Serif），accent 暂留状态语义色。tokenize 页底/侧栏/标题字体 3 处硬编码色；后端/契约零改动。**D-060 护栏守住**（不引 Tailwind/Radix 无 preflight 风险·不碰 I0 三承重串·zh/en 编译期对称·未拷 xju-feiyue tokens.css·不引 dueDate）。**验证**：hub-console verify:all（typecheck 强制 parity + 11 测[+2 theme] + build）全绿、contracts/server 48/109 零回归；**头无浏览器→构建产物 + 活体 serve 核实**：dist CSS 含暖纸 token（`f7f6f3`/`2b2922`/`37352f`/`Georgia` 实测）+ tokenized `var()`、JS 含 `teamhub.theme`/`dataset.theme`/ThemeProvider 守卫、hub-server 起 4177 托管 `GET /`=200·served CSS 可达 `f7f6f3`·`GET /api/artifacts`=200。**老实定位**：dep-graph 连线色（`EDGE_COLORS` JS 常量）不随主题变 = 已知限制（状态语义色跨主题可接受）；真实视觉切换（整站换色 + 侧栏 + serif + 刷新保持）需用户在 4177 浏览器实眼验收（headless 不可代替）；暖纸调色为起步值待微调。事实源 D-068 + `docs/research/aurash-restyle-assessment.md`。
- 2026-06-16 图纸档案可写·V1-FOLLOWUPS 收尾 (V1-FOLLOWUPS done, D-067) — 用户「teamhub 后续还有什么任务，有的话顺推」→ 盘点确认 frontier 仅剩 V1-FOLLOWUPS(低优先级)可自主做、余皆卡用户/外部基建 → 顺推图纸档案可写一次收口（commit `b7eaf4b`）。**④图纸档案可写**（写白名单第五个 append）：`CreateArtifactRequestSchema = ArtifactRefSchema.omit({id,createdAt,submittedVia}).extend({mechanism,revision: 必填 min(1)})`（base 三字段保 optional 兼容 8 种子+git 录入，仅写侧收紧）+ `GovStore.appendArtifact`/`ArtifactDraft` + 三 Store（InMemory 补 id/createdAt/submittedVia:'console' 后 push、File 写后 persist 落盘累积、Sqlite throw stub）+ `POST /api/artifacts`(safeParse→400/201，**H3 onRequest 钩子自动 gate 所有 POST /api/\***，零旁路) + console `client.createArtifact` + `ArchivePage` 登记表单（机构/版本/名称/地址/类型 + 可选 commit/repo，空档也渲染好录第一条）+ i18n zh/en parity。**③** OverviewPage→图纸档案链接 `onNavigate('archive')`。**①** 硬化 2 测：routes.test 的 GET /api/artifacts 从 length>0 改为注入自定义快照断言原样回出（证 store-sourcing 非 fixture）；gov-store-persist 加 appendArtifact→重读→断言 mechanism/revision/submittedVia 落盘 round-trip。**②** 空板起步早于 D-065 已 done。**写路径测试补全**（对抗核实 correctness lens 标的非阻塞 nit，一并收）：pm-routes +2（POST 201·夹带 submittedVia=lark 被 omit·server 钉 console·落盘 +1 / 缺 mechanism|revision → 400）+ client.test M21 补 createArtifact 用例。**宪法护栏**：I0 图纸日志永无人维度（请求/响应/落盘/读视图全链路无 memberId，对抗探针实测夹带人名被 Zod strip）/C5 submittedVia=server 钉 console/C3 仅 append 不开 update/delete/list 不解 ARTIFACT-VERSION-SEMANTICS 进阶语义/G4 不引 dueDate。**构建**：4-phase workflow `wf_1097920a-e67`（design-lock[opus 抓 15 接缝：修正 schema 落 pm-requests.ts 非 schemas.ts、contracts.ts 间接层、三 Store/index 涟漪]→后端[opus]→前端[sonnet]→2-lens 对抗核实[opus×2：I0/反排名 + 正确性/回归/DoD]双 **ship·mustFix=0·i0Clean**；首跑第 2 verify lens 中断→resume 续跑续上 cache）。**主循环独立核实**：三包 verify:all 全绿（hub-contracts 48 / hub-server **109**[+2 artifact 路由测] / hub-console 9）+ git diff --check 干净 + 真机 4188 smoke（POST 夹带 lark→submittedVia=console、缺字段→400、GET 8→9、落盘文件含新条）。**老实定位**：图纸版本进阶语义（谁 bump/权威版指针/回退）仍 open_for_decision（ARTIFACT-VERSION-SEMANTICS）、本刀只做 append-only 提交日志；真实图纸上游派生（git/lark submittedVia）未接、靠表单录入兜底。**frontier 自此真正见底**——下一步全卡用户排期或外部基建。

- 更早完成条目（D-065 及之前）已归档 → `docs/archive/completed-log.md`（完整时间线，亦见 `git log`）。
