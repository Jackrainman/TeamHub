# TeamHub 自签 HTTPS —— 最终版（过渡期，备案/域名后换真证书+443）
# 本机无公网IP(124.222.136.154 在腾讯云网关)，Caddy 绑内网 10.0.0.5:8445，
# 腾讯云安全组把公网 8445 DNAT 进内网。站点同时接受内网与公网两个 Host。
# 备案后：改成 https://yourdomain.com { tls 自动 }; 删掉 124.222.136.154 与 bind。
{
	email ubuntu@localhost
	admin off
	auto_https disable_redirects
}

https://10.0.0.5:8445, https://124.222.136.154:8445 {
	tls internal
	reverse_proxy 127.0.0.1:4177
}