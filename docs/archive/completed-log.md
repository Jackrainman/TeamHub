# 完成条目归档（completed-log）

> 从 `now.md` §最近完成 + §frontier ✅done 移出的历史完成条目，保留完整时间线。最新 3 条仍留在 `now.md`；全量提交见 `git log`。

- 2026-07-24 **公测补强刀① PIN-DEADLOCK-RECOVERY 已落地（`c23667c`，v0.28.0，minor）** — 「唯一 superAdmin 忘 PIN = 完全死锁」（onboarding-pin-deadlock-2026-07-24.md §2 路径 A 活体复现）的逃生门：`DELETE /api/members/:id/pin` 对 loopback 来源豁免 superAdmin 判定（`isLoopbackOperator`：默认信裸 socket 不吃转发头；trustProxy=true 时退看 request.ip），写门钩子对 loopback 的该路由放过「须有会话」硬门（鉴权收敛在路由）；威胁模型=宿主操作员本可直改 gov.json，不引入新权限面，非 loopback 零变化。测试 +2（server 407→409：403 用例改显式非 loopback remoteAddress；新增 loopback 放行——非 superAdmin 会话/无会话 → 200 pinHash 清除——+trustProxy 两态）；DEPLOY §7.1 改三级（产品通道→loopback curl→手工清文件）。注：hub-contracts/package-lock.json 有他人 WIP 漂移（engines 字段），本刀未动、未入 commit，故该 lock 版本字段暂留 0.27.0。 — repo_sync 验证段：**公测补强刀① PIN-DEADLOCK-RECOVERY（单 commit，v0.27.0→0.28.0 minor）**——server.ts 加 `isLoopbackOperator` + DELETE pin 路由 loopback 豁免 superAdmin + 写门钩子 isPinRecovery 放行（仅 loopback）；authz-route.test.ts +2（15 测）；DEPLOY §7.1 三级化。hub-server verify:all 全绿（45 文件 409 测，typecheck+build exit 0）+ git diff --check 过。contracts/console 未动。

- 2026-07-24 **HARNESS-DIET 已落地（`4a6cb0c`/`dfcc32d`，纯 harness 不 bump）** — 死 skill（debug-checklist/PROTOCOL-v1.0）移 archive/legacy-harness，保留 kb-debug/pre-match-checklist/personal-daily-summary；.debug-archive 双轨砍 tracked README；AGENTS §6 debug 卡规则如实降级为可选 + §4 skills 改「要用才装」；install-hooks.sh 本机已跑。真相=git log `4a6cb0c`/`dfcc32d`。

- 2026-07-23 **PIN-RESET + 余项⑥ nit③ TOCTOU 修复（v0.27.0，minor）** — ✅ 已落地(2026-07-23，公测余项⑦一刀，v0.27.0，minor)：忘 PIN 产品通道——`DELETE /api/members/:id/pin`（身份 only 404 / 须 superAdmin 403）清目标 pinHash，成员回免 PIN 态经既有 firstSetup 流程自行重设（重置口不经手新 PIN 明文，密钥纪律延续）；store `setMemberPin` 签名扩 `string|null`（null=清除，mock/file/sqlite 三实现）；console 设置页「成员与权限」成员行加「重置 PIN」（仅身份模式渲染+二次确认）；DEPLOY §7.1 改产品通道优先、手工清 pinHash 降兜底。同刀顺手修**余项⑥ nit③ TOCTOU**：降级保护判定收进 `setMemberRole` 同一临界区（`guardLastSuperAdmin` 判别联合 SetMemberRoleResult，sqlite 走事务临界区），路由层不再先读后写。测试 +6（server 401→407：authz DELETE pin 4 / member-role-store guard 1 / member-pin-store 清除 1）；三包 verify:all 全绿（contracts 297/server 407/console 165）+ e2e-pillars 3 绿；收口注记入 beta-readiness §6 尾（nit①②④ 维持原裁留档）

- 2026-07-23 **SETUP-WIZARD 三刀已落地（v0.27.0，公测就绪点② tag 已打并推送+GitHub Release）** — （frontier 原文为设计稿待批状态留档，实际随 v0.27.0 三刀落地；设计稿=docs/design/setup-wizard.md，真相=git log）；原文：🖊设计稿待批(2026-07-16，docs/design/setup-wizard.md)：首启动向导+config.json 配置落盘层+设置页部署配置区——用户三点反馈(点开就问测试/安装、初始化时选身份模式、双模式切换太麻烦)。已拍三裁决=**保留双模式**(砍匿名单轴化方案被否，D-083 维持；分叉编目 wf_5ff03910=28 处 24 可塌留档)/**不做安装码**(沿用 loopback 纪律，豁免窗口 nit 继续留档)/**先设计稿后代码**。方案=config.json>env 优先级(env 降为无头通道+显式设值固化跳过向导)+setup 三态启动(不建店不落种子)+exit42 自动重启循环(start 脚本+compose restart:on-failure)+向导二选一(先试试/直接安装→选登录方式)+设置页部署配置区(匿名⇄身份切换/结束试驾转正式单向门=演示数据归档不删)。三刀=配置层+状态机/向导UI/设置页+文档同步；**v2 修订(用户追问"env 还有必要保留吗")=模式类 env 退役**：TEAMHUB_DEMO_SEED/TEAMHUB_IDENTITY_MODE 两 env 同刀删除、config.json 唯一真相、无优先级问题；无头/自动化=预置 config 或 curl init 二选一；运维类 env(端口/路径/密钥/反代/存储后端)保留；升级迁移=v0.25.x 既有部署升级后见一次向导(检测已有数据不动数据提示，dataMode 只影响新建文件误选不清库)；开放问题 3 条见 §10，待用户批稿

- 2026-07-16 **公测文档轮（README 重写+部署指南+队员上手页+AI 部署提示词+备份脚本五域扩展，纯 docs+scripts 不 bump 仍 v0.25.0）** — **公测文档轮（README 重写 + 部署指南 + 队员上手页 + AI 部署提示词 + 备份脚本五域扩展）**。四件套：①README 313→约105 行对外门面（badge 改动态 github tag / 三支柱全部如实标已落地——旧版还写库存『没落地』其实 2026-06-19 就上了 / 页面一览九页表 / 快速开始四命令 / 文档指路），旧 README 全文归档 `docs/archive/readme-pre-beta-2026-07.md` 带过时警示头；②`docs/deploy/DEPLOY.md` 从零部署指南（三个决定=DEMO_SEED/IDENTITY_MODE/暴露方式、身份模式首启动五步、环境变量速查表、compose 路径、升级回滚、§7.1 忘 PIN 手工清 pinHash 步骤）；③`docs/guide/getting-started.md` 队员上手页（登录/每天三动作/三个好习惯/九页表/FAQ，写给非程序员队员）；④`docs/deploy/ai-agent-deploy-prompt.md` 两段式 AI 助手部署提示词（第一段=助手 clone v0.25.0→三包 install→loopback 起服→/health 验证即停；中间步=人经 SSH 隧道做名册导入+管理员初始化——引导豁免窗口决定这个顺序；第二段=初始化完成后才绑 0.0.0.0 暴露；含无 sudo/不碰凭证文件/失败自行重试≤1 次约束）。**顺手修真债**：`scripts/backup-teamhub-data.sh` 原只备 kb/gov 两域，扩到五域（补 inv/baseline/checklist；强校验复用各 FileXStore.create 当 oracle 同口径、弱校验按域适形——baseline 顶层是数组且真实态空板下合法为 []，原『非空对象』断言会误杀合法备份）+ 明示归档物目录不含 + docker 卷等价改六卷 for 循环；本机实测五域备份+读回校验全绿。**核实轮**（3 sonnet 对四文档逐主张核查代码）：1 blocker=『忘 PIN 找队长处理』——产品里其实无任何重置他人 PIN 的通道（PUT /api/members/:id/pin 仅 isSelf||firstSetup，superAdmin 无例外分支，名册重导 pinHash 永不动），文档改如实措辞+DEPLOY §7.1 给手工清步骤，**产品缺口登记公测余项⑦候选刀（superAdmin 重置他人 PIN 写口，公测中大概率真发生）**；3 nit（artifact 漏配=上传 400 非 404 / 备份脚本覆盖面误导×2）全收口。两轮 workflow（事实采集 4 agent+核查 3 agent）底料=九页导航/19 env/三包 file: 链接安装序列/功能落地状态。纯 docs+scripts commit 不 bump，仍 v0.25.0。 — repo_sync 摘要：2026-07-16：**文档轮（公测文档四件套 + 备份脚本五域扩展，单 commit 不 bump，仍 v0.25.0）**——README 重写(313→约105 行，badge 动态 tag、三支柱如实、旧文全文归档 docs/archive/readme-pre-beta-2026-07.md) + docs/deploy/DEPLOY.md 部署指南 + docs/guide/getting-started.md 队员上手页 + docs/deploy/ai-agent-deploy-prompt.md 两段式 AI 部署提示词；scripts/backup-teamhub-data.sh 扩五域(原仅 kb/gov，补 inv/baseline/checklist，弱校验按域适形 baseline 顶层数组，docker 卷等价六卷循环)，本机实测五域备份+读回校验全绿(bash -n 过+弱校验四形状单测过)；核实轮 3 sonnet 逐主张核查出 1 blocker(忘 PIN 无产品通道→文档如实+DEPLOY §7.1 手工步骤+余项⑦候选刀)+3 nit 全收口。

- 2026-07-16 **公测准备二轮收口（K6+K8+K1..K8 复审+push 恢复+仓库首个 tag v0.25.0=公测就绪点①）** — 2026-07-16：**二轮收口**——K6 时钟与空板（`e698ffe`，v0.24.2→0.24.3：真实态注入 RealClock+schedule 真空板，DEMO_SEED 单开关派生）+ K8 名册导入（`94cdfdf`，v0.25.0）+ K1..K8 全量复审（0 blocker/4 nit 入 beta-readiness §6）+ 本 docs 收口 commit（不 bump）。**push 已恢复**（07-16）：根因=旧 fine-grained PAT 30 天到期 + git credential-store 拒后自抹被拒凭证（0 字节之谜），用户重建 PAT 写回共享库；本轮 9 commit（975ea45..本 commit）统一推出 + **仓库首个 tag v0.25.0**（公测就绪点，用户已批）已打并推送。// K8 详情：**K8 名册导入刀**（一原子 commit，VERSION 0.24.3→0.25.0 minor）——名册批量导入 CSV（GET 模板 + POST 导入）解开身份模式空板死锁；新增 hub-contracts `roster-import.ts`（手写零依赖解析器 + 编码探测 + 导入报告契约）、store `importRoster` 三实现、console 设置页名册导入块 + 上一轮 nit 收口（Settings 写控件非 superAdmin 禁用）。三包 verify:all 全绿（contracts 284 / server 379 / console 163，typecheck+build exit 0）+ health-check 10 页 0 错 0 白屏 + Playwright 活体驱动导入 UI 六段报告全渲染。push 按铁律③（共享凭证库 ~/ruolin_huang/.gh-credentials 当前为空、push 401 则跳过待用户补凭证统一推）。 — current_task 承前摘要：**公测准备二轮收口**（K6 时钟与空板 + K8 名册导入 + K1..K8 全量复审 0 blocker/4 nit + push 恢复(旧 PAT 30 天到期+credential-store 拒后自抹=0 字节之谜定案) + 仓库首个 tag v0.25.0 已打并推送=公测就绪点），全部细节见 repo_sync 历史段、frontier 的 K6-CLOCK-EMPTYBOARD/K8-ROSTER-IMPORT/BETA-READINESS/K1/K2 各条目与 `docs/design/beta-readiness-2026-07-16.md`（六刀裁决+复审 4 nit 留档+两次复审 agent 事故记录）。★本机 node24 全套 node_modules 可本机验证，禁 WSL/ssh。

- 2026-07-16 **K6-CLOCK-EMPTYBOARD（`e698ffe`，v0.24.3，patch）** — ✅ 已落地(2026-07-16，K6 一刀，`e698ffe`，v0.24.3，patch)：时钟与空板——空板走查坐实：①时钟全局冻结(RealClock 零调用恒回退 FixedClock 2026-06-11，真实部署新任务时间戳恒 6/11、挂单池滞留全红) ②DEMO_SEED=false 关不掉 schedule 演示种子(两台虚构车+排班窗口)。裁决=演示态(冻结钟+演示锚点)/真实态(真钟+真空板)由 TEAMHUB_DEMO_SEED 单开关派生不新增 env；demoSeed=false 时 main.ts 注入 RealClock 到全部 store 工厂+buildHubServer、schedule 三块种子清空；默认演示态零变化(health-check/既有测试不动)。真相=beta-readiness-2026-07-16.md §5.1

- 2026-07-16 **K8-ROSTER-IMPORT（`94cdfdf`，v0.25.0，minor）** — ✅ 已落地(2026-07-16，K8 一刀，v0.25.0，minor)：名册导入——名册此前无增删通道(唯一来源=demo seed)、身份模式+空板=登录死锁。落点：①GET /api/roster/template(CSV 模板 UTF-8 带 BOM+下载头，列说明放前端) ②POST /api/roster/import(multipart 1MB；手写零依赖解析器在 contracts roster-import.ts=引号/逗号转义/空行跳过+编码探测 UTF-8 BOM→UTF-8→gbk 回退，都失败 400) ③行→Member(年级中文→枚举非法进报告不断批/组名匹配现有否则自动建组 grp-new-N kind=custom/组长真值→groupAdmin/验收人留空默认=大三及以上 true) ④幂等键=displayName(更新 grade·groupId·role·gateReviewer，superAdmin role 保护+pinHash 永不动；库有表无→missingFromSheet 绝不删) ⑤鉴权引导豁免(匿名=写门/身份=须 isSuperAdmin，**名册空时豁免登录**解死锁，全局写门钩子 isRosterBootstrap 放行+路由内自判) ⑥导入报告六段 zod(created/updated/failed/missingFromSheet/createdGroups/autoReviewers，名单事实回显给操作者本人不落聚合，I0 守) ⑦console 设置页「成员与权限」顶部名册导入块(模板下载+上传+六段报告 failed 醒目；空板豁免免锁)+顺手收口上轮留档 nit(Settings/赛季分区非 superAdmin 写控件禁用+title，照 K2 前置资格判)。store importRoster 三实现(members+groups 落 governance.json 原子批量+回滚，新增 memberSeq/groupSeq)。三包 verify:all 全绿(contracts 284/server 379/console 163)+health-check 10 页 0 错+Playwright 活体驱动导入六段报告全渲染；无独立 ADR(延续 K2-K4，真相=commit+now.md+任务设计稿)

- 2026-07-16 **BETA-READINESS 四刀 K1-K4（`975ea45`..`638c46a`，v0.24.0→0.24.2）** — ✅ 已落地(2026-07-16，四刀 K1-K4，`975ea45`..`638c46a`，v0.24.0→0.24.2)：公测准备收口——K1 权限地基(D-089，minor)superAdmin 写口(POST /api/setup/super-admin 身份 only+同笔设 pinHash/PUT /api/members/:id/role+gate-reviewer·role·seasons 三敏感门身份模式须 superAdmin，匿名不变=双模式非对称刻意裁决)+K2 身份体验(patch)匿名徽章/设置身份分区/三处选人器(指派·跨组确认·验收)过滤候选+前置资格判(按钮禁用+title 不隐藏)+K3 部署信息(patch)SystemStatusResponse.deployment(五域 storage 落盘/内存+enabledModules+artifactUploadEnabled+buildId，敏感值不进来)/About 删恒假运行模式行/图纸上传未配禁用+K4 配置面还债(无 apps/hub-*/src 改动不触发 bump，仍 v0.24.2)env.example 清 6 死变量补 7 真变量/compose 补 baseline+checklist 两域挂卷(此前重启静默清零)/冒烟脚本删幻影 postgres。三包 verify:all 终态全绿(contracts 273/server 360/console 163)；四刀架构裁决(双模式非对称/敏感门只身份模式/superAdmin 诞生=setup+PIN 同笔/About 行退役+deployment 口径)+复审留档一处良性「点了才 403」nit(Settings 页角色下拉/验收人复选框/建赛季表单缺 isSuperAdmin 前置判，服务端鉴权本身正确，仅前端可发现性未达 K2 同轮标准，留给下一刀)均入 `docs/design/beta-readiness-2026-07-16.md`；K1 真相=decisions.md D-089 — repo_sync 段：2026-07-16：BETA-READINESS 四刀（K1 权限地基 `975ea45` v0.24.0 / K2 身份体验 `ba48956` v0.24.1 / K3 部署信息 `3e055f5` v0.24.2 / K4 配置面还债 `638c46a` v0.24.2 不变）+ 收口 docs commit（不 bump）——commit `975ea45`..本 docs commit 共 5 个，VERSION 0.23.3→0.24.2；三包 verify:all 终态全绿复核（hub-contracts 273 测 / hub-server 360 测 / hub-console 163 测，typecheck+build 均过，exit 0）；health-check 10 页 0 错 0 白屏为 K1/K2/K3 各刀落地时自报（本轮未重新起服复核，纯 docs commit 无行为面变化）。四刀架构裁决 + 复审发现的一处良性「点了才 403」nit（Settings 页角色下拉/验收人复选框/建赛季表单缺 isSuperAdmin 前置判，服务端鉴权本身正确、仅前端可发现性未达 K2 同轮标准）已回写 `docs/design/beta-readiness-2026-07-16.md`（本轮新增）。push 走已配 credential helper（共享库 ~/ruolin_huang/.gh-credentials，push 前 git fetch 防分叉）。

- 2026-07-16 **K2-IDENTITY-EXPERIENCE（`ba48956`，v0.24.1，patch）** — ✅ 已落地(2026-07-16，K2 一刀，v0.24.1，patch)：身份体验刀(K1 权限地基之上补前端可见性/可发现性)——①IdentityBar 匿名模式不再 return null、渲一枚中性徽章「匿名模式」(无动作) ②设置页新增「身份」分区(只读展示当前登录模式；匿名附「怎么启用登录=服务器启动 env 设 TEAMHUB_IDENTITY_MODE=identity 重启」大白话说明；登录模式显登录状态+重启需重登提示) ③选人器过滤镜像服务端鉴权(指派/跨组确认→本组 role==='groupAdmin' 成员；验收/打回→gateReviewer 成员；候选空显引导「先去设置任命」+禁提交)，修「点了才 403」旧洞(匿名模式旧全员选人器选非组长本会 403) ④身份模式前置资格判(本人无资格的指派/跨组确认/验收按钮禁用+title 说明·不隐藏保可发现性；匿名模式无身份可判不前置判)。console verify:all 全绿(163 测)+health-check 10 页 0 错+双模式 Playwright 活体走查(匿名=徽章/设置分区/挂单指派引导；身份=登录后指派按钮禁用带 title)。红线：I0 守住(无按人聚合/排行)/无 dueDate/MemberRole 枚举零改动 — 2026-07-16 **K2 身份体验刀落地（v0.24.1，patch）**：K1 权限地基之上补前端可见性/可发现性——① IdentityBar 匿名模式渲中性徽章「匿名模式」（此前 `return null` 界面零标识）② 设置页新增「身份」分区（只读展示当前登录模式；匿名附启用说明「服务器启动 env 设 `TEAMHUB_IDENTITY_MODE=identity` 重启」；登录模式显登录状态 + 重启需重登提示）③ 三处选人器过滤镜像服务端鉴权（指派/跨组确认→本组组长 `role==='groupAdmin'`、验收/打回→`gateReviewer`；候选空显引导「先去设置任命」+ 禁提交，修「点了才 403」旧洞）④ 身份模式前置资格判（本人无资格的指派/跨组确认/验收按钮禁用 + title、不隐藏保可发现性；匿名模式不前置判）。console verify:all 全绿（163 测）+ health-check 10 页 0 错 + 双模式 Playwright 活体走查（匿名徽章/设置分区/挂单引导；身份登录后指派禁用带 title）。一个 commit push（真相 = 本轮 StructuredOutput，落点承 `task-post-claim.md`/`gate-checklist-iou.md` 选人器先例）。

- 2026-07-16 **K1-AUTHZ-FOUNDATION（`975ea45`，v0.24.0，minor，D-089）** — ✅ 已落地(2026-07-16，K1 一刀，v0.24.0，minor)：权限地基(D-089)——`MemberRole` 三档久有但全库无路由能改role(挂单指派isGroupLeadOf恒403、敏感设置无门)，用户要"重要设置必须有密码"。落点：①`POST /api/setup/super-admin`(身份only/匿名404，名册无superAdmin时把登录本人升superAdmin+同笔设pinHash，已有则409) ②`PUT /api/members/:id/role`(匿名=写门即可/身份=须isSuperAdmin 403，降级保护=摘最后一个superAdmin 409) ③敏感门收口(身份模式gate-reviewer/role/seasons三条须superAdmin 403，匿名不变) ④`authz.ts isSuperAdmin`(照isGateReviewer形状)+store `setMemberRole`三实现(照setMemberGateReviewer)+`SessionIdentity.gateReviewer`快照(改角色/名单后须重登刷新，服务端门另读实时名册) ⑤console 设置页「验收人名单」扩为「成员与权限」(角色三档下拉+验收人复选框+初始化管理员引导卡+接identity写门修真bug)。三包 verify:all 全绿(contracts 266/server 359/console 163)+health-check 10页0错0白屏+身份模式活体curl链(未登录401→非admin 403→setup 200→admin建赛季201/改角色200→降级409)。红线：MemberRole枚举零改动/无按人聚合/无dueDate/I0守住 — 2026-07-16 **K1 权限地基刀落地（v0.24.0，minor）**：D-089 superAdmin 写口 + 敏感门收口 + 初始化管理员——`POST /api/setup/super-admin`（身份 only，无 superAdmin 时把登录本人升 superAdmin+同笔 pinHash，已有则 409）+ `PUT /api/members/:id/role`（匿名写门即可/身份须 isSuperAdmin，降级保护摘最后一个 superAdmin 409）+ gate-reviewer/role/seasons 三敏感门身份模式须 superAdmin（匿名不变）+ `authz.ts isSuperAdmin` + store `setMemberRole` 三实现 + `SessionIdentity.gateReviewer` 快照 + console 设置页「验收人名单」扩「成员与权限」（角色下拉+初始化管理员卡+接 identity 写门）。三包 verify:all 全绿（contracts 266/server 359/console 163）+ health-check 10 页 0 错 + 身份模式活体 curl 全链通过。一个 commit push（真相 = decisions.md D-089）。

- 2026-07-15 **GATE-CHECKLIST-IOU（`f2ceffb`..`86bcc3d`，v0.22.0→0.22.3，D-087）** — ✅ 已落地(2026-07-15，`f2ceffb`..`86bcc3d`，v0.22.0→0.22.3)：门检查单与欠条——GateChecklistItem 独立域(anchor 挂门/自选到期日展平二选一+superRefine互斥/origin=template\|iou/豁免=大三强制理由+留名)+ deriveChecklistDrift 并列派生(欠条到期周粒度红黄绿)+过门硬闸(检查项全非pending才可过、missed不拦)+验收人名单 Member.gateReviewer 位+鉴权(authz.ts isGateReviewer，豁免两模式统一走名册)+console 门检查单卡(含0项门)/全局快记欠条入口/告警区/设置页验收人名单分区。三包 verify:all 全绿(contracts 235/server 313/console 148)+health-check 9页0错0白屏；真相文档 gate-checklist-iou.md §7(偏离回写：anchor展平/drift并列非扩展/独立checklist.json/事实层带名不剥/409首引入/PUT权限v1/复审补两处轻隐患)

- 2026-07-15 **TASK-POST-CLAIM（`5a60344`..`bfa5d25`，v0.23.0→0.23.2，D-088）** — ✅ 已落地(2026-07-15，`5a60344`..`bfa5d25`，v0.23.0→0.23.2)：挂单认领制——Task 八字段留名簇(claimedAt/assignReason/assignedBy/partnerMemberId/crossClaimConfirmedBy/completedBy/reviewedBy/reviewNote，体检D1债路径②裁定扩字面「三个」、零新实体不复活TaskProgressSignal)+ isBigTask(挂门||有非waived下游边)/deriveTaskAcceptance(验收态派生不动TaskStatus五态)/isPostedTask 结构尺+ 六窄写路由(claim即生效防孤儿400/assign+partner+confirm-cross-claim走isGroupLeadOf 403/review复用isGateReviewer，与欠条豁免同一张名册)+ GET /api/tasks 扩 q= 看谁做过搜索+isBig下沉后端+console 挂单池视图(滞留分档一键认领/组长指派)+任务卡留名徽章(搭档黄标/跨组待确认/两档验收待验收·已验收·抽查打回)+我的视图空态「没把握就去问问学长」。三包 verify:all 全绿(contracts 263/server 344/console 163)+health-check 10页0错0白屏(挂单池tab补入覆盖清单)；真相文档 task-post-claim.md §9(偏离回写：assign不做状态提升/complete无操作人=owner强校验等)；复审三 nit 已收口(`0926fc1` v0.23.3：assign防孤儿ownerId 400+review done前置判409+reviewNote以本轮为准，server 347测绿)

- 2026-07-15 **VISUAL-VITALITY 三批落地+并入 master（`9f123b3`..，v0.19.5→0.21.1，merge `e75506b`）** — ✅ 三批已落地(2026-07-13，9f123b3..HEAD，v0.19.5→0.21.1)，**分支已并 master**(2026-07-15 merge push，`e75506b`)：V0 地基(display字号档+动效token/keyframes+viz原语 Ring/StatusDot/Sparkline/CountUp+12单测)→V1 战情屏总览(T−N周倒计时hero+时间轴64px+指标瓦片仪表化+事件tone点/mono时间戳+里程碑下一站)→V2 收口(页题3xl+panel--hero两处+页级空态构图+tech依赖图节点提亮/被卡脉动)。verify:all 143测全绿+health-check 9页0错+四主题截图 docs/screenshots/visual-vitality/；真相文档 docs/design/visual-vitality.md(§6 偏离注记含 Sparkline 建而未耗的说明)；**不越 DESIGN-LANG B4/B5 硬边界**(散装空态/图标未动) — 2026-07-15 **VISUAL-VITALITY 并入 master + 阶段0体检门 + 两刀实现轮全部落地（`e75506b`→`bfa5d25`，VERSION 0.21.1→0.23.2）**：VISUAL-VITALITY 分支合并 master（`e75506b`，三批视觉升级 V0/V1/V2 定稿）→ 阶段0架构体检门（`645b54c`，`arch-checkup-2026-07-15.md`，3 读察+opus 终审判两刀承载力 continue 放行）→ D-087 门检查单与欠条四刀（`f2ceffb`..`86bcc3d`+复审 `bbb4bc2`，v0.22.0→0.22.4，门检查单容器+欠条+验收人名单）+ D-088 挂单认领制三刀（`5a60344`..`bfa5d25`，v0.23.0→0.23.2，Task 留名字段簇+挂单池+两档验收）全部落地；三包 verify:all 终态全绿（contracts 263/server 344/console 163）+ health-check 10 页 0 错 0 白屏。各自设计文档 §7/§9 为真相（`gate-checklist-iou.md` §7 / `task-post-claim.md` §9）。 — repo_sync 段：2026-07-15：两刀实现轮（D-087 门检查单与欠条 + D-088 挂单认领制）+ 阶段0体检门放行（`arch-checkup-2026-07-15.md`）+ 本次收口 planning docs commit（不 bump）——commit `645b54c`..本 docs commit，VERSION 0.21.1→0.23.2；三包 verify:all 终态全绿（hub-contracts 263 测 / hub-server 344 测 / hub-console 163 测）+ health-check 10 页 0 错 0 白屏（挂单池 tab 已补进覆盖清单）+ 活体驱动认领闭环验证（挂单池 2→1 张、写→刷新）。push 走已配 credential helper（共享库 ~/ruolin_huang/.gh-credentials，push 前 git fetch 防分叉）。**遗留注记（收尾清理受限）**：停服 pkill 与删 `~/teamhub-data/*.json` 被安全分类器拒（视 teamhub-data 为持久语料）；demo 服务器仍在 127.0.0.1:4177 后台运行、上一 session seed 的 gov.json（含一次活体测试认领 R1总联调→owner m-visionA）仍在——backup 脚本证实本 session 前该目录为空、属 session scratch 无害，但未能移除；本次两刀实现+复审全程未新增任何 `~/teamhub-data` 污染；下次 demo 若要净种子需手动清 `~/teamhub-data/*.json` 重启。

- 2026-07-12 **路线 v4 五把刀收口（刀②-⑥，`ff35a5b`→`d1a8aed` 共 17 commit，v0.11.5→0.15.5）** — 2026-07-12 **路线 v4 五把刀收口（刀②-⑥，`ff35a5b`→`d1a8aed` 共 17 commit + 本收口 docs commit，VERSION 0.11.5→0.15.5）**：刀②`IDENTITY-LITE`（I1 服务端 `4a2acd9`/`ef1892a`：轻身份契约+PIN scrypt散列+会话表SessionManager+服务端actor注入6写路由+双模式main.ts接线；I2 console `e830651`/`8978308`：IdentityBar登录UI+queryKey身份维度+PmCreatePanel ownerId自由文本→选人+写门禁用）；刀③`MY-VIEW`（`6be2545`/`bb3f70d`：DepNode加ownerId机读键+我的视图页，我负责且未被卡住∩被卡分组，按session.memberId客户端过滤）；刀④`STORE-SPLIT-SQLITE`（SS1 `47c2f65`/`ed636eb`：GovStore拆PmCoreStore/ArtifactStore/ScheduleStore三域+id/clamp抽纯模块；SS2 `ddc275b`：排班域resourceSessions/relayHandoffs补落盘合一schedule-sessions.json；SS3 `244fba3`/`1616c4f`：SqliteGovStore真实现node:sqlite文档式行存+PRAGMA版本门+迁移脚本+TEAMHUB_GOV_BACKEND opt-in，默认JSON零变化；**增量边界**=gov三域已可选SQLite，kb/inv/baseline仍JSON留后续）；刀⑤`LEARN-DIRECTION-REDESIGN`（`f98c9d3`/`7841a9c`：跨工种学习地图ROBOTICS_LEARNING_MAP+AI边界横切+sim2real种子缺口，缺人方向页翻转为学习方向，identity模式个性化排序）；刀⑥`AUDIT-DEBT-2026-07`（`538ea9a`/`5831915`/`e3e605b`/`d1a8aed`：GroupKind闭集放宽+convergenceScope收口+pm-requests解绑robotics硬编码+main.ts接线TenantConfig+工具条刷新按钮归一注册+词汇覆盖层支持新增key）。本收口轮三包 verify:all 全绿复核（contracts 214 / server 278 含 e2e-pillars 3 / console 125）+ `test:local -- e2e-pillars` 独立复跑 3 绿 + 版本号逐 commit 核对无漏 bump；红线全守（pinHash 全程未出现在响应/日志/commit、Task 无新增 dueDate、匿名模式零变化、SQLite 默认不启用）；各步偏离详见 `product-redefine-2026-07.md` §4.2/§4.3/§4.4/§5/§9 实现落点注记。 — IDENTITY-LITE：✅ 刀②已落地(2026-07-11~12，4a2acd9..8978308，v0.12.0→0.12.2)：I1 服务端(轻身份契约+PIN scrypt散列+SessionManager内存会话+actor注入6写路由+双模式main.ts接线)+I2 console(IdentityBar登录UI+queryKey身份维度+PmCreatePanel ownerId自由文本→选人+写门禁用)；匿名模式零变化(env缺省)；偏离回写本文档 §4.2 注记 — MY-VIEW：✅ 刀③已落地(2026-07-12，6be2545..bb3f70d，v0.12.2→0.13.1)：DepNode加ownerId机读键+我的视图页(MyViewPage，我负责且未被卡住∩被卡分组，客户端过滤GET /api/dep-graph，按session.memberId)；偏离回写本文档 §4.3 注记 — STORE-SPLIT-SQLITE：✅ 刀④已落地(2026-07-12，47c2f65..1616c4f，v0.13.2→0.14.1)：GovStore拆PmCoreStore/ArtifactStore/ScheduleStore三域+id/clamp抽纯模块+排班域(resourceSessions/relayHandoffs)补落盘(合一schedule-sessions.json)+SqliteGovStore真实现(node:sqlite文档式行存)+迁移脚本+TEAMHUB_GOV_BACKEND opt-in(默认JSON不变)。**增量边界**：gov(pm-core/artifact/schedule三域)已可选迁SQLite；kb.json/inv.json/baseline.json 仍纯JSON、未迁，留后续刀触及各自域时再做；偏离回写本文档 §4.4 注记 — LEARN-DIRECTION-REDESIGN：✅ 刀⑤已落地(2026-07-12，f98c9d3..7841a9c，v0.14.1→0.15.1)：缺人方向页翻转为「学习方向」DirectionPage(跨工种学习地图ROBOTICS_LEARNING_MAP×队内缺口，identity模式按本人groupId个性化排序)+AI边界横切通用资源+sim2real种子缺口；兴趣声明仍不建(D-083)；偏离回写本文档 §5 注记 — AUDIT-DEBT-2026-07：✅ 六项审计债已清(2026-07-12，538ea9a..d1a8aed，v0.15.2→0.15.5)：①GroupKind闭集→开放非空串 ②convergenceScope收口常量 ③pm-requests.ts解绑robotics硬编码(工厂函数+具体化挪verticals/robotics.ts) ④main.ts接线TenantConfig通道(TEAMHUB_TENANT_MODULES) ⑤工具条刷新按钮归一注册(onRefresh) ⑥词汇覆盖层支持新增key；偏离回写本文档 §9 勾销标注 — repo_sync 段：2026-07-12：路线 v4 五把刀（IDENTITY-LITE/MY-VIEW/STORE-SPLIT-SQLITE/LEARN-DIRECTION-REDESIGN/AUDIT-DEBT-2026-07）收口轮——本批（ff35a5b..d1a8aed 共 17 个 commit，VERSION 0.11.5→0.15.5，含 4 次 minor：I1 4a2acd9/M1 6be2545/SS3 244fba3/L1 f98c9d3）+ 本次收口 planning docs commit（不 bump）已**本机全量复核验证**：三包 verify:all 全绿（hub-contracts 214 测 / hub-server 278 测含 e2e-pillars 3 测 / hub-console 125 测，typecheck+build 均过，exit 0）+ `test:local -- e2e-pillars` 独立复跑 3 绿（默认匿名 env，零变化）+ 逐 commit VERSION 核对无漏 bump。本机 node24 全套 node_modules（WSL 免除，用户裁定；本轮全程无 WSL/ssh）。push 走已配 credential helper（共享库 ~/ruolin_huang/.gh-credentials，push 前 git fetch 防分叉）。

- 2026-07-12 **交互三连 + 设计语言统一前半程（`7fa01fe`→`9e1b036`，VERSION 0.17.0→0.19.5，计划经用户批准）**：① `7fa01fe` **星图点击详情面板**（v0.18.0）——点选节点右侧出覆盖式详情（click/drag 以指针位移区分、相机 320ms 缓动对焦、Esc/空白/×关闭、选中关联边常亮、窄屏退化横条卡；按 kind 分内容）；② `32eab65` **SEASON-CREATE 补链路**（v0.19.0）——总览空态"先在设置里建一个赛季"原指向不存在的入口（写口曾注记待身份功能，IDENTITY-LITE 已落地故兑现）：contracts CreateSeasonRequestSchema + 三 store `createSeason`（新赛季恒 active、旧 active 同笔归档=一届一个当前赛季）+ POST /api/seasons + 设置页「赛季」分区，UI 真机走查过；③ **DESIGN-LANG-UNIFY B0–B3+U1**（`391014c`/`32c4278`/`0a22eca`/`6999319`/`9e1b036`）——规范落 `docs/design/design-language.md`（§1-§10 唯一写法+批次真相），9 sonnet UX 扫描 9 页×4 主题（36 张基线截图入库 `design-lang-baseline/`，发现分流 U1-U8 进批次/其余入 backlog `UX-SCAN-BACKLOG-2026-07-12`）；B1 文案两词制（新建+登记，用户拍板）+车→机器人；B2 徽章 8 家族→`.badge`+tone（U3：未配置全主题中性化/空着蓝绿分离）；B3 按钮 12 套→`.btn` 4 变体×2 尺寸（SubmitButton 保 API；rowBtn/画布迷你钮豁免有记）；U1 修真 bug=`.console-nav .nav-item` 特异度压掉三主题侧栏 active（tech 靠 0,3,0 侥幸活）。防石山审计+发布链路设计另落 `docs/planning/rot-audit-2026-07-12.md` + `docs/design/release-pipeline.md`（`0618948`）。**⏸ 硬边界（用户拍板节奏）：B4 卡片/空态 + B5 图标/kb-前缀/CSS 治理待用户看 B3 后截图（`design-lang-after-b3/`）放行**。

- 2026-07-12 **UI 三连（构建分包 + tech 观感打磨批次 B + 学习方向 3D 星图，`1f3ffa8`/`54055df`/`ad12c1b`，VERSION 0.16.1→0.17.0，交互式会话主 agent 亲做）**：① `1f3ffa8` vite `manualChunks` 拆 vendor-react/data/icons，入口 535kB→285kB，500kB 警告消除；② `54055df` tech 观感打磨 8 处（截图评审驱动：侧栏 active 青轨+渐变底 / 工具条渐隐分隔线 / panel 标题水洗 / 行 hover 青染 / seg 选中态青底描边 / 全站细深色滚动条 / 「未配置」主色青→中性灰蓝语义修正 / 空态虚线留位框），全圈 `[data-theme='tech']`；③ `ad12c1b` **学习方向双 UI**（用户拍板）：指南/星图 seg 切换，星图=**自写 SVG 3D**（零依赖——特意不引 three.js，免 500kB 警告在懒加载 chunk 复发+实验室离线友好），四色=四组（电控青/电路绿/机械琥珀/视觉紫）、跨工种知识点=RGB 混色、缺口红环挂本组、AI 边界白点居中连四组、`ITEM_TARGETS` console 侧手工标注条目→关联工种（键=契约原文，改文案安全降级单色）；交互=拖拽旋转/滚轮缩放/节点可拖（视平面位移逆旋转回基础坐标系）/悬停高亮关联边/闲时缓转（reduced-motion 停）；懒加载单开 chunk 8.7kB；6 条纯函数单测。console verify:all 131 测绿 + health-check 9 页 0 错 + 真机 Playwright 走查（自转/悬停 tooltip/双工种色标实证）。截图 `docs/screenshots/ui-2026-07-12/`。**②③是否保留待用户看图定夺**（各自独立 commit 可单独 revert）。

- 2026-07-12 **科技风换肤：第 4 主题 `tech` 落地并设默认（`332c354`→`04654b3`，VERSION 0.16.0→0.16.1）**：新增 `[data-theme='tech']` 深色科技旗舰主题（蓝图网格底垫+基准线遥测轨道发光节点+今天信号青扫描线三角脉冲+等宽数字），设为**默认主题**（`localStorage` 已存偏好用户不被覆盖，沿用 2026-06-16 主题切换器机制）；classic/warm/dark 三套渲染结果零污染，全部结构性改动圈在 tech 选择器下；零外部资产（系统字体栈+CJK 回退 `PingFang SC`/`Microsoft YaHei`/`Noto Sans CJK SC`，等宽 `ui-monospace`/`JetBrains Mono`/`Cascadia Mono`/`Sarasa Mono SC`）；对比度全达标、焦点可见、`prefers-reduced-motion` 沿用既有先例停脉冲。QA 修复 `c6eaa4d`：@xyflow 依赖图第三方控件（缩放条/归属徽标）随 tech 主题去白块。纯视觉轮，业务逻辑/路由/契约零改动。本收口轮（本条）三包 `verify:all` 复核全绿（contracts 214 / server 278 含 e2e-pillars 3 / console 125）+ `health-check` InMemory fixtures 起服复核 9 页 0 错 0 白屏。截图落 `docs/screenshots/tech-restyle/`（18 张，tech 9 页 + 看板 + light/dark 回归 + identity 登录条）。决策：`decisions.md` D-084（新增，用户 2026-07-12 拍板科技风方向）SUPERSEDES D-060（Aurash 暖纸风提案就此关闭，全文移 `docs/archive/decisions-archive.md`，`open_for_decision` 移除 `UI-RESTYLE-AURASH` 条目）。

- 2026-07-11 **BASELINE-DESIGN 设计锁 + BASELINE-CORE 刀①实现轮（六步+收口，`d3db6fe`→`e3a76a9`，v0.10.3→0.11.5）** — 2026-07-11 **刀① BASELINE-CORE 实现轮（六步 + 收口，`d3db6fe`→`e3a76a9`，VERSION 0.10.3→0.11.5）**：倒排基准线内核全栈落地——S1 Season 接线（`GovernanceSnapshot.seasons: Season[]` + GET /api/seasons，照 GET /api/groups 直读快照，minor v0.11.0）；S2 baseline zod 独立域文件 `baseline.ts`（SeasonBaseline/BaselineMilestone/TaskInvestment + 三路由契约，Task 加 `milestoneId?`/`investment?` 两 optional、永不加 dueDate）；S3 独立 `baselineStore`（InMemory+File 两实现 + 独立落盘 `baseline.json` + `TEAMHUB_BASELINE_DATA_FILE`，fail-closed）；S4 GET/PATCH/POST-pass 三路由挂 `registerPmCoreRoutes`（过门写鉴权 + I0 剥 `passedBy` 沿 `confirmedBy.omit` 先例 + 证据后缀白名单加 .mp4/.mov/.webm）；S5 `deriveBaselineDrift`（周粒度红黄绿，常量 lookahead=2 周/完成度阈值 0.5）+ `deriveGroupsBehind`（组归因、无 memberId）+ `deriveInvestmentWarnings`（future×high 连续 2 周零进展=正在砍未来）；S6 console 总览 `BaselineOverview`『基准线 vs 实际』首屏 + 三版车模板 v1（正向展开+尾段倒推混合）seed + `PmCreatePanel` 投资三维录入。本机三包 verify:all 全绿（197/232/96）+ e2e-pillars 3 绿 + Playwright 首屏走查。红线全守（Task 无 dueDate / 落后单位=里程碑·组不点人名 / 独立 store 独立落盘 / 证据只存 artifactId / 版次裁剪留痕 `mergedFromVersion`）；实现期偏离全回写 `baseline-design.md` §7。 — BASELINE-DESIGN：✅ 设计已锁(2026-07-11，baseline-design.md v1)：三版车节奏模板(V1实验→G1问题清单收敛→寒假sim2real→G2 V2拼装→G3期中V3→G4整车试跑→调参入场)/验证门(大二证据+大三验收，字节走artifact卷)/投资三维(horizon×value×timeAccumulation)/版次可裁剪/周粒度红黄绿；真实时间线用户赛后回填 — BASELINE-CORE：✅ 刀①已落地(2026-07-11，d3db6fe..e3a76a9，v0.10.3→0.11.5)：Season 接线(GovernanceSnapshot.seasons+GET /api/seasons)+baseline zod(独立 baseline.ts 域)+独立 baselineStore 两实现+落盘 baseline.json(TEAMHUB_BASELINE_DATA_FILE)+GET/PATCH/pass 三路由(含过门 POST+I0 剥 passedBy)+deriveBaselineDrift/组归因/投资示警周粒度红黄绿+总览 BaselineOverview+三版车模板 v1 seed+投资录入；偏离已回写 baseline-design.md §7 — repo_sync 段：2026-07-11：BASELINE-CORE 六步 + 收口（d3db6fe..e3a76a9 共 7 个 commit，VERSION 0.10.3→0.11.5，含 S1 一次 minor）三包 verify:all 全绿（197/232/96）+ e2e-pillars 3 绿 + Playwright 首屏走查基准线总览达成。

- 2026-07-11 **产品重定义文档轮（D-083）**：`docs/design/product-redefine-2026-07.md`（DECIDED：开源·防爆肝双主轴·四把刀·宪法修正 G4/I0/AI 三红线·收窄砍推迟清单·解耦审计账单·路线 v4）+ D-083 落 ADR + `core-plugin-architecture.md` PROPOSAL 搁置归档 + backlog 增防爆肝任务簇/翻 07-03 旧状态 + AGENTS 同步注记。解耦审计 = `wf_66e13d79-814`（3×sonnet 逐文件核实四刀阻力 + 坐实 GroupKind/convergenceScope/pm-requests 硬绑 robotics/main.ts 未接 tenantConfig 四处错位）。

- 2026-07-03 **实现五件套终态 `a64e7bc` v0.10.3** — 2026-07-03 **实现五件套终态 `a64e7bc` v0.10.3**：daily-plan-presets（D-082）+ console 装配接线 + L6 入站白名单 + 表单 UX 审计修复 16 条 + 本机 506 测全绿/health-check 8 页 0 错（详 completed-log）。 — repo_sync 段：2026-07-03：origin/master=c84f4a9（07-03 FF 合并 feat/plugin-core 六个模块化 commit：6fc32fb→c84f4a9，VERSION=0.9.7）。rainman WSL2 上次同步到 feat/plugin-core 同一 HEAD（07-02 验证全绿：三包 verify:all 432 测 + 迁移脚本 scripts/migrate-robottarget.mjs 真 gov.json 副本零丢失 + Playwright health-check 8 页 0 错等价 master + 真实未迁移 gov.json 新 schema 加载成功）；本轮 07-03 全部 commit 已经**本机全量验证**（WSL 跳过·用户裁定；本机 node24 首次装齐四包 node_modules + playwright chromium）：506 测全绿 + health-check 8 页 0 错 + 新端点冒烟全通。WSL 真机部署验证转为可选项。**部署前必做**：D-080 砍 `requires` 依赖类型 + FileGovStore fail-closed 校验后，旧 gov.json（含 `type:'requires'`）redeploy 直接起不来，须先跑 `scripts/migrate-robottarget.mjs` 或 reset `~/teamhub-data/gov.json`。

- 2026-07-01~07-02 **模块化阶段一（feat/plugin-core，6 commit `6fc32fb`→`c84f4a9`）**：剪两条跨域 import 环（growth→governance / pm-requests→relay）+ 装配外壳（`ModuleDescriptor`/`TenantConfig` 装配契约）+ `governance.ts` 拆 `pm-core.ts`+`schedule-infra.ts` + `RobotTarget` 枚举渗透改 optional（Task/Project）+ 迁移脚本 `scripts/migrate-robottarget.mjs` + fixtures 拆 per-module builder + `verticals-robotics` 词汇包成形。07-02 WSL 验证：三包 `verify:all` 432 测全绿 + 迁移脚本真 gov.json 副本零丢失 + Playwright health-check 8 页 0 错等价 master + 真实未迁移 gov.json 新 schema 加载成功。07-03 FF 合并 master（`c84f4a9`，VERSION 0.9.7），已 push origin。设计真相 = `docs/design/modularization-feasibility.md`（已执行）；core-plugin-architecture.md PROPOSAL 已于 07-11 搁置归档（D-083，现在 `docs/archive/`）。详 decisions.md D-081。

- 2026-06-22~24 **批量落地（OOP 三批次 / 视觉打磨 A-H+暗色模式 / Playwright 体检收口 / 库存落盘 / D-080）**：OOP+代码质量路线图批次 A+C1（`882fd56`）+ 批次 B SSOT 收口（`2fd6970`，v0.7.5→0.7.6）+ 补测批次 C2/C6/C7/C8（`21ea803`）+ C3/C4 抽纯模块（`c7d410f`）；视觉打磨 A-H 全批（尺度 token/暖色补全/次级按钮/输入边框/可访问性/文案去 AI 味/暗色模式第三套主题，`54dcfa6`…`657f208`）；Playwright 体检收口进 `hub-console` 自身 devDep（`2231903`/`b4ac8d8`，`e2e/health-check.cjs`+`npm run health-check`，不再借 feiyue-test）；库存落盘接进启动路径（`655bb1e`，`TEAMHUB_INV_DATA_FILE`）；D-080 录入收尾（`3e12e50`/`8b4cfcc`：新建任务抽屉化 + 连依赖/暴露需求 tab 下线 + `requires` 全砍，FileGovStore fail-closed 校验，旧 gov.json 需先迁移）；模块化可行性研究（`4b4ad6d`）；daily-plan-presets 设计稿落档（`8213195`）。

- 2026-06-20 **版本号自动更迭失效修复（D-074）**：对照 feiyue 找根因——AGENTS 从无「要 bump」规则 + 服务端缺 Tampermonkey 那种「不自增不更新」的天然强制函数 + D-052 半拉子修复（只让 `status.ts` 读 package.json、bump 留成没人执行的 TODO）+ monorepo 多包无单一产品版本 + 零 tag。方案落地：根 `VERSION`（产品单一真相）+ `scripts/bump-version.sh`（唯一改版入口，同步三包 package.json）+ `scripts/check-version-bump.sh` 哨兵挂 `pre-commit.sh`（warn，`VERSION_BUMP_STRICT=1` 升硬门）+ AGENTS §7/§2.3 成文。据 git tree 两法收敛**当前版本重定为 v0.4.0**（产品线 v0.3→v0.4 / TeamHub 纪元 0.1→0.4），三包 0.1.0→0.4.0。

- 2026-06-19 在场排班优化（D-072）定稿：对抗式设计审查（`wf_2f31074c-523`，55 claims·5 视角·对抗核实）+ 用户多轮拍板 → 设计稿出定稿版（两视图删甘特 / 单层「车=带编号对象」/ 删程序组领任务留汇报视角 / 宏观维修态+注释 / 接力释放「谁可下班」语义 / 预留=扣减归车 / v=代次 / 总联调全组各一人 / 多车并排）；零代码，下一轮 PRESENCE-IMPL 起实现。

- 2026-06-21 图纸文件链路收口 + 表单控件修缮 + 部署/版本号修复 (D-078，origin/master `0259c18`，2 commit `5e6947b`+`0259c18`，worktree 隔离→rebase 并发 session→FF 合并→push) — 用户「系统排查需求分析半成品」。详见 now.md §最近完成 + decisions.md D-078。要点：①图纸 上传→本地卷→下载 链路打通（`ArtifactRef.storedFile` + `POST /api/artifacts/:id/upload` multipart 50MB·后缀白名单·先验存在避孤儿·H3 鉴权 + `artifact-storage.ts` 本地卷接缝·D-025 不进 git·MinIO 留接口 + `GovStore.setArtifactFile` 三实现）；②console 文件+云端链接**双存**·行内下载/打开双按钮·上传/替换·条件下载（消灭摆设）；③适配机器人/赛季 datalist 组合框（候选+手填·复用台账）+ robotCode 写契约放宽 + 编号位 `.kb-field--narrow` 收窄；④部署 env-var 修复（`ARTIFACT_ROOT`→server 实读 `TEAMHUB_ARTIFACT_FILES_DIR`，原线上恒 404）；⑤版本号 lock 漂移修复（hub-contracts/console lock 0.0.1→0.4.1 + 硬化 `bump-version.sh` 同步 lock + `check-version-bump.sh` 漂移哨兵）。三包 verify 绿（contracts 164 / server 194[+8 upload] / console 44）+ 真机 API 冒烟全过（创建/上传/下载/重传清兄弟/`gov.json` 持久化/415·401·404·400 负路径）。并发：保留另一 session 未提交 WIP `presence-reconcile-lock.md`。WSL2 浏览器走查 = 后续轮。

- 2026-06-19 无人值守实现轮：INV-BOM-CORE + PRESENCE-IMPL 安全增量 + WSL2 验收 + lark bin 修 (4 commit `9861850`→`5cb84cb` 全 push) — `/loop` 无人值守连续实现，遇需拍板/无安全默认即跳过记录、不停。
  - **INV-BOM-CORE** (`9861850`)：库存/BOM 第三支柱 buildable-now 内核，严格照 `docs/design/inv-bom-core.md`(locked) 零猜测。contracts `inventory.ts`：PartType/TrackedPart/PartAction schema + `deriveInventoryLedger`(零件×车矩阵·idle=total−Σ(used+reserved))/`deriveShortfalls` 派生 + `applyPartAction`(7 kind 迁移·单一不变量 idle≥0·InvalidPartActionError) + 读/写契约 + `inventory.test.ts`(13 测)。server：`InvStore` 接口落地(getInventorySnapshot/upsertPartType/recordPartAction/listShortfalls) + `mock-inv-store`(组合 applyPartAction) + `file-inv-store`(tmp+rename 原子写·fail-closed·writeChain H2·回滚) + 三端点(GET /api/inventory·POST part-types/actions) + main.ts 接 `TEAMHUB_INV_DATA_FILE` + `inv-routes.test.ts`(7 测)。console：features/inv(InvPage/InvLedgerTable 矩阵·闲置高亮·缺料行红/InvQuickRecordForm 一句话快记) + 点亮导航 + i18n 中英 + client 三方法 + .inv-* 样式。seed `inventoryScenarioFixture`(GM6020/C620/主控/M4·个体实例·主控板缺料 demo)。**红线**：recordedBy 永无 memberId(I0)·不回写 Bitable(G2)·写白名单仅 upsert/record·个体件拆装只移 currentHolder 不删(C3/D-072§3.4)。诚实偏离 §4 一处：GET /api/inventory 增 actions 字段以兑现 §5④ 记账历史(§4 字段表与 §5④ 取并集)。三包 verify(contracts 111/server 146/console 35)。
  - **PRESENCE-IMPL 安全增量** (`736f0c3`)：照 presence-resource-redesign.md §7 自底向上落 ②部分/③/⑤/⑥。`deriveDisplayCode(season,position,version)`(禁手写→25R1/26R1-v2) + SharedResource 补 optional season/version/displayCode(向后兼容回退 name) + 生命周期态 repair/retired/disassembling(保留 legacy down/upgrading) + `canBoardResource()` 接力释放谓词；derivePresenceSchedule 级联改读 `!canBoardResource(status)`(不再硬编码状态名)。seed res-r1/res-r2 派生 26R1/26R2(库存矩阵车列复用同 displayCode·Task1+2 对齐)。console `RelayChainView`(按车并排接力链·overflow-x:auto·读 /api/schedule+/api/resources·轮到/待命/可下班语义) + SchedulePage 接力链升主视图·卡片网格降级「明细(按组)」 + client.getResources + i18n schedule.relay.* + relay-* 样式。反监视护栏升结构约束 + `resource-display.test.ts`(9 测:PresenceRecommendation/schedule 输出零 memberId)。**DEFERRED**(记 §7.1)：§7① fixture 调和(m-progA→grp-ec/m-progB→grp-vision/grp-program 去领任务)+「总联调=全组各一人」收敛派生语义——无锁定 schema/prompt 未给安全默认/重排共享 demo fixture 波及 ~9 测试文件，应仿 inv-bom-core.md 先出字段/派生级定稿。三包 verify(contracts 120/server 146/console 35)。
  - **WSL2 真机 Playwright 验收** (`d90ca64` 截图入库)：DESKTOP-Jackrainman(rainman@100.78.202.84·node v24·Playwright 1.61) 单端口 4177 起服(loopback·TEAMHUB_INV_DATA_FILE=~/teamhub-data/inventory.json)。增量 git bundle(37bb512..master·字节比对无截断)→git fetch+merge --ff-only→build console+server→Playwright 9 断言全 PASS：库存矩阵活体显示 26R1/26R2·主控板缺料行红+徽章·一句话快记表单·POST damage→201 GM6020 总数 9→8(表格刷新)·接力链 26R1 列「第1棒 R1总联调·在场」+待命+可下班·卡片网格降级·/api/inventory 与 /api/schedule grep 零 memberId/invitedMemberIds。截图 docs/screenshots/wsl-{inv-page,inv-after-damage,schedule-page}.png。**踩坑记**：WSL 在 ssh 会话关闭后 reap 所有进程(含 setsid/nohup/tmux)→验收须在单 ssh 会话内起服+测完；且后台 node 须 `</dev/null` 否则 hold ssh channel 致挂起。
  - **lark-toolkit bin 修** (`5cb84cb`)：cli-bridge.ts execa 二进制名 'lark'→'lark-cli'(@larksuite/cli 装的 bin·旧写法在已装机器仍 ENOENT 误报)，两处 execa + test 两断言同步；WSL 实测 lark-cli 1.0.53·飞书 auth 已配(bot ready)。verify(lark-toolkit 14 测)。**DEFERRED**：「拉飞书 wiki/drive→KB 只读通道+活体」未做——lark-toolkit 是 @probeflash vendored 包未接 hub KB·飞书文档与 KB 调试闭环 schema 不对位·只读通道落点无定稿，需先出字段/落点级定稿。
- 2026-06-19 差异化排班 S1+S2+S3 三诉求 + 文案 + 审计长尾 (D-069 P1 连续自主批，5 commit `ece2a3b`→`052285d` 全 push) — 用户「连续 A 做完→B 能做就做→撞阻塞再演示·多用 workflow」。每条独立 workflow(design-lock→impl→2-lens 对抗核实)+主循环独立 verify。**S1+S2+S3 差异化排班三诉求纯函数/接出全落，A 档全清、B 档 SCHED-MEMBER 提前做掉**。
  - SCHED-WIRE-EXISTING (ece2a3b)：S1 接出已有 derivePresenceSchedule(D-029 写完零运行时引用→第一次真正接出)。GovStore 加 listResources/listResourceSessions/createResourceSession(4 实现：interface+InMemory seed scheduleScenarioFixture+File 委托 inner 不落盘+Sqlite throw 桩) + CreateResourceSessionRequest/Response 契约(剥 confirmedBy 守 M6/I0) + POST /api/resource-sessions(继承 H3 onRequest 鉴权·source 钉 human·clamp) + GET /api/schedule?windowLabel=(组装 ScheduleSnapshot 调纯函数·PresenceScheduleResponseSchema fail-closed) + GET /api/resource-sessions + console SchedulePage(features/schedule·present/onCall/free 组键卡片·free 挂资料·永不渲染人) + App/ConsoleLayout(CalendarClock)/client/i18n 13 键 zh-en。**2-lens 对抗核实**(wf_83bf4434·I0反排名+正确性/回归/DoD 均 opus)双 ship·i0Clean·mustFix=0(毒化 invitedMemberIds 也 0 泄漏)。三包 verify:all 绿(contracts 65/server 136[+12 schedule-route]/console 35)+活体 curl(POST→201 source=human·confirmedBy 剥·再 GET schedule 多一条 present·JSON grep 无 memberId)。**老实定位**：排班窗口不落盘(粗粒度今晚/明天·重启回 seed C1)·真实占用窗上游派生未接(靠队长录入/seed)·录入 UI 留只读 SchedulePage(createResourceSession client 已备)。
  - SCHED-MEMBER-AVAILABILITY (a9b176f)：S1「不同上课时间」→ 组级容量(纯契约+派生·护栏密集·向后兼容)。growth.ts MemberAvailabilitySchema(memberId/recurringBusy[WeeklyMinuteWindow{dayOfWeek,startMin,endMin,label?}]/visibility private|aggregateOnly·镜像 MemberKnowledge 私有范式) + governance.ts WeeklyMinuteWindow 共享基元(refine s<e·半开区间·不支持跨夜)/WindowDef/PresenceFeasibility(ample|tight|conflict) + PresenceRecommendation 加 feasibility .nullable().default(null)(ResourceSession 零改动) + schedule.ts deriveGroupAvailability(单窗·仅组级 headcount 整数·private skip·绝不跨窗按人累计)+ derivePresenceSchedule optional 第4参 ctx。**向后兼容硬证**：三参旧调用不变·14 schedule.test 原封·feasibility 缺省 null·两 fixture(含 resourceDown)派生输出 byte-for-byte 一致。**2-lens 对抗核实**(wf_d9c8f54b·A2 不外露+向后兼容/DoD 均 opus)双 ship/i0Clean/mustFix=0(毒化 private 课表 0 泄漏·deriveGroupAvailability 返回 Map<groupId,int> 无人维度)。hub-contracts verify:all 绿(82 测·schedule 14+availability 17)+下游 server/console 仍绿。**老实定位**：capacity→feasibility 阈值为起步启发式(D-029 open)·feasibility 仅纯函数内算未接路由/console 渲染(留下批)·单人组 capacity=0 隐含信息属 1 人组 headcount 固有性(aggregateOnly 授权前提·非按人排名)。
  - STUDY-NARROW-DERIVE 纯函数 (2b21e29)：S3 窄义学习建议(B1·不复活 D-039)。新 study-suggestions.ts：deriveStudySuggestions(snapshot,now,options?)→{group,selfPrivate} 两数组**物理隔离**(非 union·self 无法泄进 group)。组级=open+escalated Need.neededSkills × KnowledgeNode(node.name 含 skill token·KnowledgeNode 无 skillKey)按 providerGroupId 聚合·**零 memberId/rank/assignee**；私有变体=双门 viewerMemberId&&memberKnowledge·硬过滤 mk.memberId===viewer(绝不读他人 private)·只 interested|learning·钉本人 memberId·audience=selfPrivate·绝不进组/队长聚合。detectedBy 恒 'derived'·不新增 AI 对人拍板路径。**2-lens 对抗核实**(wf_9b81e649·B1反越界+正确性/DoD 均 opus)双 ship/i0Clean/mustFix=0(毒化他人 private 兴趣 0 泄漏·group schema 注入 memberId 被 strip)。hub-contracts verify:all 绿(92 测·study-suggestions 10)+下游绿。**老实定位**：私下推本人(飞书/lark 私聊)仍 gated on HUB-HERMES-ADAPTER/LARK-BIN-PROBE，渠道接通前只能本人页面被动展示。
  - CONSOLE-COPY-HUMANIZE (cb2d120)：sonnet inventory→改写→核实 workflow(wf_792b39b1) 复核 6 目标串=**5 条已自然(D-058 早humanize)保留**、仅 1 条 KB_SIMILAR_NOTE(server contracts.ts)有真 AI 味(混语「reasons」+嵌套书名号「就是同一个原因」)→改「不断言是同一个原因·每条附了相似依据」(A4「不断言」测试锚点保留·只改 value)。「踩过的坑」前缀地道口语+D-058 刻意留→keep。verdict ship/mustFix=0·三包 verify:all 绿(92/136/35)。
  - AUDIT-FIXES-LONGTAIL (052285d)：scope→concern 分组顺序修→2-lens 核实 workflow(wf_70fbdabb)。scope 复核长尾 19 候选→**14 修 / 5 已做(M8/M9/M10/M16/M17主体/L4/L11)或决策门(M14/M20)排除**。**14 修**=M7(getSnapshot/getKbSnapshot 返浅拷贝防绕写白名单·复用 cloneArrayFields·回滚链不动)·M15(nodesDraggable=false)·M18(IssueCardSchema 三串+数组元素 .min(1))·M19(KbCloseoutRequest rootCause/resolution .trim().min(1) 约束上提到 400)·L1(InMemoryGovStore length+1 ID→单调计数器)·L5(static-console realpath 重跑 isWithinRoot 防 symlink 越界)·L7(KbSimilarParams+client 补 projectId)·L8(SimilarIssueMatch.updatedAt→isoDateTimeSchema)·L9(KbCloseoutForm seq→useRef)·L10(TaskForm 冷启动 defaults useEffect 守空回填)·L12(.nvmrc+三 package.json engines>=24)·N1(adapter mode/status z.literal→z.enum 防真 adapter 崩 UI)·N2(rankSimilarIssues tie-break Date.parse)·N3(新 attribution.test.ts 补 sharedResourceBusy 死分支+I0 回归护栏)。**2-lens 核实**(回归/无行为破坏+scope合规/I0 均 opus)双 ship/mustFix=0。三包 verify:all 绿(contracts 95[+3]/server 136/console 35)。**老实定位**：M14/M17/M20 defer·L6 飞书白名单 gated on 真 LLM。
- 2026-06-18 v2 后硬化会话：bug 检修 + 版本 + I0 收口 + 优化（commit `732a2c9`→`b10ba43`，均 push）— 用户「workflow 进行 bug 检修 + 版本号更迭 + 优化整个项目，多用 workflow、自主做无需审批的」。**①bug 检修批**（巡检 wf_e8353745 确认 23 bug → 修 13 clear-correctness：closeout 事务/幂等、FileStore persist 回滚、ArchivePage stale mechanism、DetailPanel pendingShelve、wouldCreateCycle 滤 waived、rateHits 驱逐、内存模式告警等；对抗核实智能跳 3 项=resolved guard/IssueCard min1 会回归、sameDaySeq TOCTOU known-low，`732a2c9`）**②版本 0.0.1→0.1.0**（`4971483`）**③I0 收口**（巡检复核两处无鉴权人名读端点：dep-graph ownerLabel「谁负责」用户拍**保留**=协作信息非监视；Overview 逐人成员状态面板用户拍**隐藏**=与不抓摸鱼冲突+旧脚手架，`4a19569`；未来 LOGIN-PERMISSION-UI 记 backlog）**④优化自主批**（优化 wf_b9b70503 扫 58→自主 41：DRY 提取 utils/Field/MetaRow/MetricTile/constants + 加性 a11y + 类型收紧 + KbCloseout/KbSimilar schema 下沉，net −24 行，3 处伪零变化对抗核实抓出后中性化，`7ed7832`）**⑤C 架构清理**（wouldCreateCycle 前端复用 contracts+测试 / 删死 Adapter* 导出 / Metric accent 收紧，`b10ba43`）。三包 verify:all 215→**224** 全绿。无新 D 号（均 D-071 后续 + 既有 backlog 项）。
- 2026-06-18 图纸档案 v2 机械/电路分组版本库 (HUB-ARTIFACT-ARCHIVE-V2 done, D-071) — 用户 PM 视角讨论后把「通用归档」重塑为「机械/电路分组的图纸版本库」（程序组排除·全在 GitHub）。**数据模型 A 扁平/派生分组**：ArtifactRefSchema 加 5 个 `.optional()` 字段（ownerGroup/season/robotCode/versionNo/subType，词汇对齐 GroupKindSchema，全 optional 故 8 seed+旧 JSON 仍加载）；写契约 `.superRefine`（电路必须有 subType·机械必须无）；**server 派生** versionNo（`nextArtifactVersionNo` 四键 max+1）/kind（`deriveArtifactKind` 电路驱动→firmware·余→report）/revision=`v${n}`，3 个 store 不动（两纯函数落 `artifact-version.ts`·前端版本预览复用同函数零漂移）；console 表单重做（`.seg` 段控 组/R1R2/子类型·赛季 select 默认 guessSeason·机构新增/下拉·版本预览·两级分组 ownerGroup→mechanism +「未分组/历史」桶承接旧 seed·max 版打「当前版」徽章·关联仓库提交仅电路驱动显）。**回退=最新即权威**（派生，纯 append-only）·手动 pin 不做（违 C3，未来走 append-only supersede）·进阶语义 ARTIFACT-VERSION-SEMANTICS 仍 open。**workflow `wf_57c7f730-a0d`**（Contracts/Server[opus]→Frontend[sonnet]→2-lens 对抗核实）裁 i0Clean=true+抓 1 真 bug（空档案首条录入 effectiveMechanism 与渲染条件不一致→按钮永 disabled）主循环收口。三包 verify:all 绿（contracts 62/server 115/console 11）+本地活体 smoke（机械 v1→同键 v2·电路驱动→firmware·缺/多 subType→400·夹带人字段被 strip·落盘 artifact 无人字段）。事实源 D-071 + plan `scalable-noodling-brook.md`。
- 2026-06-18 GAP-DIRECTION 缺人方向 (S2 done, D-069) — 用户「改完之后顺推接下来的任务」→ harness 减负后接 frontier 最 ready 项 S2 缺人方向（D-069 拍 A1 组级均衡·合宪 clean）。**契约层**（commit `73373ca`）：`DirectionGapSchema`+`GroupGapsResponseSchema`（governance.ts，逐字对齐 `OverloadSignal` 范式·组键+factStatement+detectedBy:'derived'·**零 memberId/score/percent**）+ `deriveDirectionGaps(snapshot,now)`（新 `direction-gaps.ts` 纯函数：按 `Need.providerGroupId` 聚合 open+escalated·neededSkills 并集去重排序·融 `deriveBlockAttributions` rootBlockerGroupId 频次定 severity[emerging/pressing]·null 组跳过·无缺口空数组沉默 A4）+ 6 单测。**路由+视图**（commit `b35d1ae`）：`GET /api/group-gaps`（`GroupGapsResponseSchema.parse` fail-closed·clock 注入确定性）+ console「缺人方向」页（组级卡片·severity 徽章·缺的方向 chips·`gaps.note` A1 护栏说明常显·**永不渲染人/不下钻**）+ ConsoleLayout 导航(Compass)+App 路由+`client.getGroupGaps`+i18n 12 键 zh/en parity+gaps CSS。**2-lens 对抗核实** `wf_f5bd0491-6b5`（I0/A1 暴露 + 正确性/DoD，均 opus）双裁 **ship·mustFix=0**——adversarial 构造毒化 fixture 也找不到结构性暴露路径（schema 零人字段·derive 不碰 claimedByMemberId/confirmedBy·console 无人维度·输出组键无法 groupBy 排名·非 A2 派单）。2 nit 收口（commit `f2ee082`）。**验证**：三包 verify:all 绿（hub-contracts 54[+6]/hub-server 110[+1]/hub-console 11+build）+**活体 curl** /api/group-gaps 返组级 `gap-grp-program`(CAN/RTOS)·JSON grep 无 memberId/displayName/confirmedBy。事实源 D-069 + backlog P1 差异化排班。
- 2026-06-18 Harness 减负·对齐 feiyue 轻量模型 (D-070, T1+T2+T3 done) — 用户「feiyue/feiyue-script 的 harness 很合适、TeamHub 过重，查看怎么减负」→ inventory workflow `wf_782c37b4-193`(6 组件 + 2 feiyue 模型并行盘点)定位重量不在 AGENTS.md(D-066 已精简 64 行)而在周边「长记忆+同步机制+留作追溯的旧脚手架」。**T1 瘦身**：`now.md` `最近完成` 历史 → `docs/archive/completed-log.md`(每轮必读 **61KB→25KB·−58%**，留最新 3 条+指针)；decisions 挂起簇 D-032~D-035 → `governance-suspended-decisions.md`(留 stub)；`workflow-evolution`/`docs/agents/workflow`/`skill-library`/2 份 pre-pivot spec → archive。**T2 拆死同步**：hook(`sync-skills.sh`，实测从未在任何 settings.json 注册)+哨兵(`verify-skills-sync.sh`)删除，skill 收**单一真源 `.agents/skills/`** + feiyue 式 `install.sh` 软链进 `.claude/skills/`；AGENTS §4 DoD 改引 install.sh。**T3 账本文化**：`decisions.md` append-only→「活账本+归档」(约定写进顶部 intro)，hard-superseded D-043/D-053 压 3 行 stub+全文 → `decisions-archive.md`；**decisions.md 220KB→195KB**。**守恒**：全 `git mv`/`git rm`·零真相丢失·零代码/数据改动。验证：git diff --check + now.md yaml + grep 无悬挂引用 + 4 skill install.sh 软链生效。事实源 D-070。
- 2026-06-18 验收 D-061→D-068 上真机 + 差异化排班/缺人/学习建议合宪立项 (D-069) — 用户「查看 TeamHub 还有哪些没做 + 是否完成验收(没则在 rainman WSL 验收) + 新功能能否进代办：『不同上课时间+平均排班 / 什么方向缺人 / AI 分析谁去学』」。**验收**：rainman WSL2 常驻部署实测停在 **D-061**(落后本机 7 commit·D-062/D-065/D-066/D-067/D-068 都没上过真机)；git bundle 过 SSH 同步到 `230c38e`(D-068)→三包 verify:all 全绿(contracts 48 / server 109 / console typecheck+测试+build)→4177 真机起服(buildId=230c38e·真实语料 kb 84KB+gov·API smoke /api/artifacts=8·/api/kb/similar 召回 ProbeFlash CAN 卡)→**D-068 主题切换器(唯一挂着的实眼验收)Playwright 实测：`data-theme` 切 warm 后 5 token 全切(pageBg/surface/sidebarBg/text/fontTitle)·`SWITCH_WORKS=true`**。老实定位：经典/暖纸两套配色都浅色、明度接近(冷白↔暖白·深绿↔深棕)+serif 只对拉丁字母生效(WSL 无 CJK 衬线)→**机制正确但视觉差异微弱**(印证 D-068「暖纸调色为起步值待微调」)，留用户 localhost:4177 实眼终验。**新功能评估**(workflow `wf_6f935ab0-027`，4-agent：算法/宪法对抗/资产复用→综合)：高度重合**已有但挂起**的积木(`schedule.ts derivePresenceSchedule` D-029 写完零运行时引用 / `growth.ts` D-027 / `attribution.ts`)；拆 **S2 缺人方向=clean**、**S1 排班=needs-guardrail**、**S3 谁去学=命中 D-039 复活触发**。**用户两 §5 拍板**：**A1 组级均衡**(个人课表 private 自愿录入·聚合成组级 headcount·输出无 memberId·拒 A2 按人均摊出勤) + **B1 窄义**(AI 只建议知识点+只私下回本人·拒 B2 把具体人配对缺口) → 合宪形态进 backlog 立项(`GAP-DIRECTION-*`/`SCHED-WIRE-EXISTING` ready · `SCHED-MEMBER-AVAILABILITY`/`STUDY-NARROW-DERIVE` next/gated · `STUDY-BROAD-D039-REVIVAL` HARD-GATED 封存)，**不复活 D-032~D-035**。纯 planning(decisions D-069 + backlog 新「P1 差异化排班」段 + GOV-SCHED-VIZ-DESIGN 部分解封 + now)。事实源 D-069。
- 2026-06-16 设置页风格切换器·运行时主题 (D-068) — 用户「做一个切换风格的功能，在设置里」（澄清「接下来需要排版的部分」= D-060 `UI-RESTYLE-AURASH` 唯一视觉条目）。**纯 CSS-variable 换肤、不引框架**：把 D-060 Phase 0（换 token，<0.5 天拿 80% 视觉）包装成可回退 opt-in 功能——全站组件早消费 `var(--*)`，第二套 token 挂 `:root[data-theme='warm']`（特异度 0,2,0 > `:root`），切属性即整站换肤、绝大多数组件零改动、无 preflight/无 @xyflow reset 风险/零新依赖。**架构逐行镜像 i18n 语言切换**：新 `theme/index.tsx`（`ThemeProvider`:localStorage `teamhub.theme` + `documentElement.dataset.theme` + useEffect 同步 / `useTheme` 未挂即 throw / 纯函数 `normalizeTheme` 供单测不测 DOM）、`main.tsx` ThemeProvider 包 LanguageProvider、设置页加「外观」section（复用 `segClass`+`.seg` 零新 CSS）、i18n 4 键 zh/en parity。**默认 classic 现行绿、暖纸 opt-in**（无 `data-theme` 即像素一致）；暖纸 = 米色面 `#f7f6f3` + 暖白 `#fffdf9` + 近黑 `#37352f` + 发丝线 + 暖炭侧栏 `#2b2922` + serif 标题（Georgia/Songti/Noto Serif），accent 暂留状态语义色。tokenize 页底/侧栏/标题字体 3 处硬编码色；后端/契约零改动。**D-060 护栏守住**（不引 Tailwind/Radix 无 preflight 风险·不碰 I0 三承重串·zh/en 编译期对称·未拷 xju-feiyue tokens.css·不引 dueDate）。**验证**：hub-console verify:all（typecheck 强制 parity + 11 测[+2 theme] + build）全绿、contracts/server 48/109 零回归；**头无浏览器→构建产物 + 活体 serve 核实**：dist CSS 含暖纸 token（`f7f6f3`/`2b2922`/`37352f`/`Georgia` 实测）+ tokenized `var()`、JS 含 `teamhub.theme`/`dataset.theme`/ThemeProvider 守卫、hub-server 起 4177 托管 `GET /`=200·served CSS 可达 `f7f6f3`·`GET /api/artifacts`=200。**老实定位**：dep-graph 连线色（`EDGE_COLORS` JS 常量）不随主题变 = 已知限制（状态语义色跨主题可接受）；真实视觉切换（整站换色 + 侧栏 + serif + 刷新保持）需用户在 4177 浏览器实眼验收（headless 不可代替）；暖纸调色为起步值待微调。事实源 D-068 + `docs/research/aurash-restyle-assessment.md`。
- 2026-06-16 图纸档案可写·V1-FOLLOWUPS 收尾 (V1-FOLLOWUPS done, D-067) — 用户「teamhub 后续还有什么任务，有的话顺推」→ 盘点确认 frontier 仅剩 V1-FOLLOWUPS(低优先级)可自主做、余皆卡用户/外部基建 → 顺推图纸档案可写一次收口（commit `b7eaf4b`）。**④图纸档案可写**（写白名单第五个 append）：`CreateArtifactRequestSchema = ArtifactRefSchema.omit({id,createdAt,submittedVia}).extend({mechanism,revision: 必填 min(1)})`（base 三字段保 optional 兼容 8 种子+git 录入，仅写侧收紧）+ `GovStore.appendArtifact`/`ArtifactDraft` + 三 Store（InMemory 补 id/createdAt/submittedVia:'console' 后 push、File 写后 persist 落盘累积、Sqlite throw stub）+ `POST /api/artifacts`(safeParse→400/201，**H3 onRequest 钩子自动 gate 所有 POST /api/\***，零旁路) + console `client.createArtifact` + `ArchivePage` 登记表单（机构/版本/名称/地址/类型 + 可选 commit/repo，空档也渲染好录第一条）+ i18n zh/en parity。**③** OverviewPage→图纸档案链接 `onNavigate('archive')`。**①** 硬化 2 测：routes.test 的 GET /api/artifacts 从 length>0 改为注入自定义快照断言原样回出（证 store-sourcing 非 fixture）；gov-store-persist 加 appendArtifact→重读→断言 mechanism/revision/submittedVia 落盘 round-trip。**②** 空板起步早于 D-065 已 done。**写路径测试补全**（对抗核实 correctness lens 标的非阻塞 nit，一并收）：pm-routes +2（POST 201·夹带 submittedVia=lark 被 omit·server 钉 console·落盘 +1 / 缺 mechanism|revision → 400）+ client.test M21 补 createArtifact 用例。**宪法护栏**：I0 图纸日志永无人维度（请求/响应/落盘/读视图全链路无 memberId，对抗探针实测夹带人名被 Zod strip）/C5 submittedVia=server 钉 console/C3 仅 append 不开 update/delete/list 不解 ARTIFACT-VERSION-SEMANTICS 进阶语义/G4 不引 dueDate。**构建**：4-phase workflow `wf_1097920a-e67`（design-lock[opus 抓 15 接缝：修正 schema 落 pm-requests.ts 非 schemas.ts、contracts.ts 间接层、三 Store/index 涟漪]→后端[opus]→前端[sonnet]→2-lens 对抗核实[opus×2：I0/反排名 + 正确性/回归/DoD]双 **ship·mustFix=0·i0Clean**；首跑第 2 verify lens 中断→resume 续跑续上 cache）。**主循环独立核实**：三包 verify:all 全绿（hub-contracts 48 / hub-server **109**[+2 artifact 路由测] / hub-console 9）+ git diff --check 干净 + 真机 4188 smoke（POST 夹带 lark→submittedVia=console、缺字段→400、GET 8→9、落盘文件含新条）。**老实定位**：图纸版本进阶语义（谁 bump/权威版指针/回退）仍 open_for_decision（ARTIFACT-VERSION-SEMANTICS）、本刀只做 append-only 提交日志；真实图纸上游派生（git/lark submittedVia）未接、靠表单录入兜底。**frontier 自此真正见底**——下一步全卡用户排期或外部基建。
- 2026-06-15 审计后 server 硬化 + 写侧正确性批 (POST-AUDIT-HARDENING-D065) — 用户「查看可优化/构建」→ 14-agent 审计 workflow(`wf_2f92f9cc-bd7`：6 finder→对抗核实→综合，38 优化/24 构建)→用户拍板做「写侧正确性小批 + 预写部署代码」合并 pass(无外部门、都改 server.ts)。**写侧正确性(已对抗核实)**：①`POST /api/agent-backends/:id/invoke` 改 `.parse`→`safeParse`+400(M8 旧账：D-062 改了路由名却漏补 safeParse，坏 body 走 500 泄漏 Zod 内部/破坏错误契约——全 POST 路由里唯一一处) ②`POST /api/kb/closeout` 补 `reply.code(201)`(L4，与其余 create 一致) ③`InMemoryGovStore` 构造器补全克隆 groups/members/taskKnowledgeTags(M13，8 数组与 FileGovStore.cloneSnapshot 对齐，防未来写入污染共享 fixture) ④补 console 写侧测试 createTask/createDependency/createNeed/closeoutKb + 400-detail 透出(M21，+2 测) + hub-server invoke-400 回归(+1 测)。**预写部署代码(上线等 §8 审批、代码先就位)**：⑤`SystemStatusResponseSchema.mode` `z.literal('mock-first')`→`z.enum(['mock-first','real','hybrid'])`(否则 real/hybrid 部署 server 自解析自身响应 500、总览页崩)+ `buildSystemStatusResponse` 加 mode 参默认 mock-first ⑥`buildHubServer` 加 `trustProxy?` 透传 Fastify + `TEAMHUB_TRUST_PROXY=true`(4177 反代/隧道后面不开则写限流塌成全队单桶=DoS) ⑦`TEAMHUB_DEMO_SEED=false` 空板起步(V1-FOLLOWUP-2，fresh 落盘文件 seed 空板不进演示假数据，仅影响新建文件) + env.example 补 token 强随机提示/TRUST_PROXY/DEMO_SEED。**验证**：三包 verify:all 全绿(hub-contracts 48 / hub-server 102[+1 invoke-400] / hub-console 9[+2 写侧])+ git diff --check 干净 + 真机 smoke(DEMO_SEED=false → /api/tasks·/api/artifacts 空、season 元信息留；默认 → 8 任务+8 图纸；mode 仍 mock-first 正常解析)。**老实定位**：bridge/members 逐人状态板(唯一确认 I0 违反)用户拍**暂时保留**、标真实部署前必处理；ownerId/ownerLabel 是 D-041 安全堆设计张力非泄漏(待 AGENTS §5 措辞对账)；其余审计长尾(M14/15/16 前端 a11y、M20 workspace 工具、KB schema 双声明等)未动。真实部署/Hermes 全线仍待 §8 审批 + LARK-BIN-PROBE。
- 2026-06-15 集成模型地基重建：扁平 AdapterDescriptor → BotChannel/AgentBackend/DataSource 三分 (D-062) — 旧扁平 `AdapterDescriptor`（kind 是装饰·无人 switch·invoke 契约错配·status 假值·lark 三化身）拆成三个一等公民：**`BotChannel`**(飞书/微信/QQ·连接型 status·receive/reply/push) / **`AgentBackend`**(hermes/openclaw/claude-code·唯一有 invoke/health/capabilities·`backendId`) / **`DataSource`**(git-forge/artifact-store·只读·`sourceRef`，artifact-store=`filebrowser://artifacts` 预留)。路由 `/api/adapters*`→`/api/bot-channels`+`/api/agent-backends`(+`:backendId/health|capabilities|invoke`)+`/api/data-sources`；`AdapterDescriptorSchema` **弃用保留**(lark-gateway/lark-toolkit/pf-skills 仍自描述用)；删 xiaolongxia、pf-skills 移出集成列表；mock-adapters→mock-integrations、mock-ai-adapters→mock-agent-backends；console 设置页集成子节按三类分小节 + i18n zh/en + styles。**认证模型拍定 = A「公共后端、无登录」**(服务端无账号/session/JWT·唯一鉴权全队共用 `TEAMHUB_WRITE_TOKEN` 仅挡非 loopback POST·飞书/微信/QQ 当通知渠道非登录·B 登录推迟)。该重建 prior session 已落地未 commit；本轮主循环独立复核：三包 verify:all 重跑 exit 0(hub-contracts 47/hub-server 91/hub-console 6)+三包 build+活体 curl 三组新端点过 → commit + push。**老实定位**(非阻塞)：`HubEventSourceSchema` 仍混 bot/agent/内部源(枚举清理推迟)·`BotChannel.status` 诚实占位(飞书真实探测推迟)·系统状态 `adapters` 字段语义漂移为 agent 计数(i18n 标签消解)·仍 mock-first。事实源=decisions.md D-062 + memory teamhub-integration-model。
- 2026-06-15 v1 能跑产品：治理快照落盘 + 图纸提交日志 + 删 mock 单后端 (D-061) — 用户 PM 视角定「先做出来一个能跑的、接口都能接进来」+ workflow 连续做。**①治理快照落盘** `FileGovStore`（NEW，1:1 镜像 FileKbStore：原子 tmp+rename·writeChain 失败隔离·`create(file,seed)` 读盘或 seed+persist·`GovernanceSnapshotSchema` fail-closed·写白名单内嵌 InMemoryGovStore 复用 id/时戳/clamp 零漂移·每写后 persist）+ `main.ts` 接 `TEAMHUB_GOV_DATA_FILE`→注入 `buildHubServer({store})` + compose `hub_gov` 卷 + env.example——补上 PM/治理数据落盘硬伤（原 InMemoryGovStore 重启回演示假数据、录的真东西清零）。**②图纸提交日志**：`ArtifactRefSchema` 加可选 `mechanism`(机构)/`revision`(第几版)/`submittedVia`(git/lark/console 来源 seam)；`GovernanceSnapshot`（手写 interface）加 `artifacts: ArtifactRef[]`，`GET /api/artifacts` 改读 `store.getSnapshot().artifacts`（保留 artifactRefFixtures 导出·fixtures.test 未改）；种子 8 条版本日志（底盘 v1/v2/v3·抬升机构 v1/v2·夹爪 v1·视觉固件 v1/v2）；console NEW `features/archive/ArchivePage.tsx` 按机构分组·组内日期倒序·组按最新活跃排·每条名/版本徽章/类型/日期/关联commit/地址 + 真实导航 `nav.archive`(FileStack) + i18n 9 键 zh/en——「某机构几月几号第几版」一眼可查·可存档。**③删 mock 数据源模式**：删 `api/mock/*` + `source:mock|real` 开关（App/ConsoleLayout/SettingsPage）+ HubApiClient 恒真实 + 新增 `getArtifacts()`；重写 client.test.ts 3 个 mock 测为注入 fetcher 真实模式测（保覆盖）；删死 i18n 键 + 8 条「切到演示数据」误导文案改指「检查后端地址/服务」；zh/en `Record<TranslationKey>` parity + 3 条 I0 串原样。**构建**：7-agent workflow `wf_eb55b2ca-8fe`（design-lock[opus 验架构抓 6 处真实接缝:GovernanceSnapshot 是 interface 非 schema/required 涟漪经 spread 继承/apiContractFixtures 消费方/test 路径/DataSource 线程/getArtifacts 需新增]→后端→删mock→档案页→3-lens 对抗核实[I0护栏/正确性回归/DoD诚实]）**全 ship·mustFix=0**。6 原子 commit `5a2c96d→01d06f4`（feature 分支→ff master→push 01d06f4）。**主循环独立验证**：三包 verify:all 重跑 exit 0（hub-contracts 47 测 / hub-server 89 测[+4 gov-store-persist：seed-on-fresh/重启不丢/corrupt fail-closed/H2 写链不中毒] / hub-console 6 测 + build）；`git diff --check` 干净；**真机 smoke**（4199 单端口 console+API+`TEAMHUB_GOV_DATA_FILE`）：`GET /api/artifacts` 返 8 条版本日志、FileGovStore 落盘 15KB 文件含 8 artifacts+8 tasks（重启不丢实证）、Playwright 档案页按机构时间线视觉验收、mock 文案/开关消失、4177 旧实例未碰。**老实定位**（非阻塞）：真实 status/图纸上游派生仍未接（seam 留 statusSource/submittedVia 枚举 + `store?` 注入口，等 Hermes/飞书/Git 触点）；fresh 文件 seed 测试数据·空板起步后置 1-flag；2 条测试断言强度 nit（routes artifacts 只测 length / persist 未断言新字段 round-trip——功能已 smoke 实证）；OverviewPage→档案页链接未加。→ V1-FOLLOWUPS（frontier，低优先级）。

- 2026-06-15 AUDIT-FIXES 7 条部署前必修全做 (AUDIT-FIXES-2026-06-14 done, D-059) — code-audit-2026-06-14.md 的 7 条联网部署前必修一次落地（hub-contracts + hub-server + compose/env，零 console 行为改）。**H1 依赖环 DoS**：`computeCriticalSet` 回溯加 visited 守卫（防已有环死循环卡死 server）+ 新增纯函数 `wouldCreateCycle`（自环 + DFS 可达，自身 DoS 安全）+ `POST /api/dependencies` 落库前拒自环/成环（400）。**H2 写链中毒**：`FileKbStore.persist` 把推进写链版本 `.catch(()=>undefined)` 隔离失败（reset 为 resolved、不再静默跳过后续写）、调用方仍拿真实错误、writeOnce 失败清 .tmp。**H3 写端点零鉴权**：Fastify `bodyLimit:256KB` + onRequest 钩子（仅 POST /api/*）配 `TEAMHUB_WRITE_TOKEN` 则强制 Bearer(401)+每 IP 固定窗口限流(429)；`main.ts` 非 loopback 且未配 token → 拒启动。**H4 字段注入**：`CreateTaskRequestSchema` status 钳到 `enum(pending|inProgress)`、statusSource 钳到 `enum(lark|git|console)`——拒客户端注入 done/shelved（伪造完成）/derived（冒充派生·违 C5）。**M6 I0**：`CreateDependency/NeedResponseSchema` omit `confirmedBy`（创建响应不送 ActorRef 过边界）。**H5/M11**：compose 删幻影 Postgres(服务/depends_on/DATABASE_URL/pg_data) + 接 KB 持久(`TEAMHUB_KB_DATA_FILE`+`hub_kb` 卷)；env.example 补 token+KB 路径。**M9**：`deriveErrorCode` 加 optional 单调序号（路由用「同日既有 ErrorEntry 数+1」），消除哈希 mod 1000 生日碰撞静默覆盖。**测试 +11**（`audit-fixes.test.ts` 10[H1 自环/成环不落库·M6 无 confirmedBy·H4 done/derived→400·H3 401/429/GET 放行] + `kb-store-persist` H2 失败隔离 1 + `cycle-guard.test.ts` 6[传递环 A→B→C+C→A]）+ 更新 kb-closeout 确定性测试为 M9 单调契约。**3-lens 对抗核实** `wf_99ea69cb`（opus 安全绕过 / opus 正确性 / opus 回归完整，并行）**全 ship、mustFix=0**——安全 lens 实证无可绕过（DFS 方向对/visited 守已有环/单写入口/钩子覆盖全 POST /api/*/isLoopback 判定对/bodyLimit 生效/store 无旁路）、正确性 lens 脚本实证 H2 链 reset+M9 不碰撞+H4 拒注入+M6 剥键、回归 lens 实证 7 条全落+现有测试不被鉴权/限流误伤+compose 闭环能启动。验证：hub-contracts 47 测 / hub-server 85 测 / hub-console 7 测+build 全绿；AUDIT-H1/H3 完成度谓词（接缝锚 + hub-server verify:all）重跑 exit 0。**老实定位**：rate-limit key=request.ip 在反代后会塌成单桶（非阻断）、Bearer 非定长比较（时序攻击 out-of-scope）、TEAMHUB_WRITE_TOKEN 占位需运维改强随机串（env 注释已写 `openssl rand -hex 32`）；这些非部署阻断。

- 2026-06-15 console 文案去 AI 味/治理黑话 (CONSOLE-COPY-HUMANIZE done, D-058) — humanizer-zh 原则、只改 value 不动 key、zh/en 同步。**6 处 console i18n**：pm.create.title(协作真相→「全员都看得到」)/subtitle(不记谁快谁慢·不排名→「不比谁快谁慢」)/pm.field.needDescription(归组不归人→「按组，不点人」)/kb.empty(去「词重合度/同因」)/kb.closeout.intro(去「派生知识点」)/kb.closeout.success.knowledge(派生知识点→「存下的知识点」)。**后端** hub-server `KB_SIMILAR_NOTE` 去「词重合」黑话、**保留「不断言」**(kb-similar-route.test.ts:22 断言)；连带对齐 console `mock/kb.ts` 同句。**保留不动**：`deriveKnowledgeNodeFromIssue` 的「踩过的坑：」前缀(gov-store-scaffold.test.ts:66 断言 + 本就是人话)；pm-routes.test/源码注释里的「归组不归人」(非用户可见 copy)。**护栏语义全保**：A4(只列候选·不断言同因·由人选用)/C2(不比快慢·反排名)/A1(缺口按组不点人) 逐条核实未削弱。**2-lens 对抗核实** `wf_8c5051bf`(opus 护栏保全 + sonnet 保真/测试安全)双裁 **ship/mustFix=0**——护栏 lens 逐条确认反监视语义保留无反转、保真 lens 确认零测试断言被撞+zh/en parity+无事实增删。验证：hub-console + hub-server 双 verify:all 绿；完成度谓词(`! grep 归组不归人` + 两包 verify:all)重跑 exit 0。

- 2026-06-15 适配器→集成 + 设置页只读集成子节 + 总览精简 (INTEGRATIONS-TO-SETTINGS done, D-057) — §6.B 连续构建、纯 hub-console。**relabel**：所有用户可见「适配器」→「集成」/「Adapters」→「Integrations」(overview.metric/panel.adapters + enum.event.adapter.health.changed，zh+en)；状态枚举(已启用/已禁用…)保留。**设置页集成子节** `SettingsPage.tsx` 新增 `IntegrationsSection`：`useQuery(['hub-overview',source])` **复用总览 getOverview 数据(同 queryKey 缓存共享、不双取)**，只读渲染 `adapters.adapters`(displayName/capabilities/status pill)，isLoading/error/empty 三态降级。**总览精简** `OverviewPage.tsx`：删 adapter 详情 `panel-wide` 区 + 连带删 `AdapterRow`/`StatusPill`/`ADAPTER_STATUS_KEY`/`AdapterDescriptor` import(全转移到设置页)，改成一行「集成对接状态已移到设置页 →」链接(`onNavigate('settings')`，App 传 setPage)；summary-strip「集成」指标 tile 保留。语义=连飞书/Hermes/git 外部应用，不引入真实触点(仍 mock-first)。**2-lens 对抗核实** `wf_f40f5aea`(opus I0 暴露面 + opus React/TS 正确性)双裁 **ship/mustFix=0**——I0 实证 IntegrationsSection 只渲染 adapters 外部应用描述符(无 memberId/bridgeMembers/排名)、relabel 未动含人字段；正确性实证 queryKey 共享无双取、删的 helper 零残留引用、4 状态枚举全覆盖、5 新键 zh/en parity。**顺手收**核实指出的 2 个孤儿 i18n key(overview.panel.adapters/meta.unconfigured 删 panel 后无消费方)。i18n +5 键 −2 孤儿。验证：hub-console verify:all(typecheck/7 测/build) 绿；完成度谓词(grep integration + console verify:all)重跑 exit 0。老实定位：仍 mock-first，真实飞书/Hermes 触点接入后置(Hermes 门)。

- 2026-06-15 依赖图录入浮层 + 看板↔依赖图互通 + I0 负责人降级 (DEPGRAPH-ENTRY-OVERLAY done, D-056) — §6.B 连续构建、纯 hub-console。**录入浮层**：依赖图右上角「录入」按钮 → `position:fixed` 近全屏遮罩浮层(backdrop role=presentation 点空白关 / drawer role=dialog+aria-modal stopPropagation)承载 `PmCreatePanel`；依赖图补调 `getTasks`(与看板同 queryKey `['tasks',source]` 缓存共享)填依赖/需求下拉；`onCreated`→**同时失效 `['tasks',source]`+`['dep-graph',source]` 两查询**才即时重绘。**看板↔依赖图互通**：`PmTaskCard` 加「在依赖图查看」按钮 → `App.focusTaskId` 暂存 task.id → 切 dep-graph → `DepGraphPage` useEffect 图加载后选中该节点并消费 focus(早返回守卫防循环)。**I0 护栏**：节点卡片 `dag-node__owner` 去掉 `ownerLabel`、只留结构键(组·车)，`ownerLabel` 降级到 DetailPanel + 新增「负责人只表分工·不代表进度快慢」反排名说明；topbar 加「图上只显任务/组/卡点·不排个人」。**2-lens 对抗核实** `wf_9a77daa8`(opus I0 暴露面 + opus React/TS 正确性)双裁 **ship/mustFix=0**——I0 实证净改善(从画布常显移除一项人维度+反排名说明、focusTaskId 传结构键 task.id 非人 id、PmCreatePanel confirmedBy 写侧不回显)、正确性核实 useEffect 无无限循环(focusTaskId 置 null 早返回)、queryKey 共享无双取、空 tasks 各表单降级不崩。i18n +6 键 zh/en parity。验证：hub-console verify:all(typecheck/7 测/build) 绿；完成度谓词(grep PmCreatePanel + console verify:all)重跑 exit 0。老实定位：DEPGRAPH-AI-AUTODRAW(AI 自动布 DAG)仍后置(Hermes 门)；真实 status 派生上游未接通。

- 2026-06-14 画布拖拽连线建依赖 (DEPGRAPH-DRAG-CONNECT done, D-053 续) — 用户拍 Option A(固定 type='blocks' + 重取重绘)。`DepGraphPage.tsx`：`nodesConnectable={true}` + `onConnect`(源底 handle→目标顶 handle = 源阻塞目标)→`createDependency({projectId:graph.projectId, fromTaskId:源, toTaskId:目标, type:'blocks', source:'human', confirmedBy:合成 console actor 使边显红})`→`invalidate(['dep-graph',source])` 重取、dagre 重布局。**关键安全**：后端 POST /dependencies 零语义校验(AUDIT H1 未修)，故 onConnect 在 POST 前客户端守 自edge(from===to)/重边(edges.some)/成环(`wouldCreateCycle` 模块级 DFS：to 能否回到 from)，任一拒则轻提示横幅不 POST——挡掉「拖出成环边→下次 getDepGraph 死循环卡死服务器」。+i18n 6 键 zh/en + `.dep-graph-banner` 反馈条。**I0 已核**(重绘走 toDepGraphView 不外露 confirmedBy/人维度，合成 console actor 第三方读视图见不到)。验证：hub-console verify:all(typecheck 守 ActorRef 形状+i18n parity / 7 测 / build) 绿。**自审夯实**：跑 completion-model 全 13 谓词，DEPGRAPH-DRAG-CONNECT→PASS done-flip(重跑谓词 exit 0 才翻)，并抓出 AUDIT-H1-CYCLE-GUARD 谓词含 console 路径+泛词 visited 被前端守卫误判 PASS→收口为「只测后端 hub-server/hub-contracts 环检测」诚实 gap(后端拒环仍部署前必修)。设计=`docs/design/depgraph-drag-connect.md`(status→ready→done)。
- 2026-06-14 自迭代外环 §6.C + 完成度模型 + 拖拽连线设计 (SELF-ITERATE-SKELETON done, D-053) — 用户「搭自迭代骨架(没任务时查完成度→不够则自动设大目标→workflow 推进；atom-task 只提正确率+拆子 agent)+push+准备拖拽连线」。先 push 5 commit 到 origin(0a639d9)。**自迭代外环**：化解「自动设大目标」与 §6.0 M1 候选池闭口的张力——**materialize-before-action 逃生阀**：合成的 epic 先写成真 backlog 行(带 M2 谓词、状态 pending)+追 ADR+进 frontier 并 commit，唯此后才作普通 in-backlog 候选驱动(只合规往闭池加、不绕过)。落 `.agents/skills/self-iterate/SKILL.md`(8 硬化步)+`AGENTS §6.C`(单一源)+`docs/planning/completion-model.yaml`(每 deliverable 一条机器谓词，gap=谓词失败/not-started，priority 排，gaps[0]=下一大目标)。**3-opus 设计→对抗红队 `wf_3845c9c0-aa2` 裁「ship-able ONLY after guards」**，补三处致命缺陷：①§5 宪法门缺失(opus 合成的目标可由 roadmap 措辞、紧邻挂起治理簇 D-032~035 而合成出违 I0/A1/C2 的 epic)→§5 门作合成第 0 子步+挂起簇硬封 must-stop(自迭代永不复活)②epic cap 缺失(双重耗尽非真终止、逃生阀重填池)→每 invocation ≤1 合成+人审 checkpoint③completion-model 自著可伪造 done→交叉对账 pending 行+done-flip 重跑谓词+禁 haiku 写 done。保守默认 `audited:false`→合成只 propose-and-stop，人审 completion-model 一次后 `audited:true` 才自动驱动。**老实定位**：外环在单次 invocation 内连续自迭代、非跨进程永动机；首版合成 propose-and-stop(待人审谓词)，「全自动设目标+驱动」在 audited:true 后一行翻转。**DRAG-CONNECT 设计就绪**(`docs/design/depgraph-drag-connect.md`)：已核实 DepNode.id===Task.id/`fromTaskId 阻塞 toTaskId`/只 invalidate ['dep-graph',source]/红边需非 null console confirmedBy；**关键安全**：后端 POST /dependencies 零语义校验(AUDIT H1 未修)，前端 onConnect 必须客户端守自edge+重边+成环(否则成环边卡死服务器)；I0 已核重绘不外露人维度；待用户拍 1 条 UX(固定 type='blocks' Option A 推荐 vs 落点弹 type 选择器)即实现。验证：git diff --check 干净 + now.md/completion-model.yaml yaml 可解析 + verify-skills-sync(self-iterate 镜像一致)。纯 docs/skill/planning，零代码。
- 2026-06-14 提案审查 + 低风险收尾批 (CONSOLE-LOWRISK-BATCH-D052 done, D-052) — 用户一批诉求（5 灰导航语义/适配器改名/看板≈依赖图合并/暴露需求/全项目石山/skill 自迭代/版本不更新/mock 是什么），**先用 9-agent workflow `wf_def55d4d-916` 探明合理性**（5 survey[sonnet]+3 石山审计[opus]→1 opus 综合，~62 万 token）产逐条裁决+22 条石山 finding+自迭代方案 → 问 Q1–Q4、用户拍板。**纠偏**：5 灰项非空壳（后端路由+总览面板都在，死的只是侧栏禁用按钮）。**拍板**：①适配器→**集成/Integrations 归设置**②看板/依赖图**不合并**，依赖图升主舞台·录入做近全屏遮罩浮层·AI 自动画 DAG+人微调③"连线"=**画布拖拽建依赖**非改按钮名④暴露需求=制度化替你开口(A2 反派单)，建议改「登记缺口」⑤自迭代引擎已在(§6.B/continuous-build)，停因 frontier 空+安全门+方向待拍。**低风险批 3 commit done**（`8ab93cf`/`44b7fcc`/`8ea6579`）：版本跟随 package.json(createRequire) / 删 5 死导航+unused 图标+nav 键+Mock 文案白话化("Mock 数据"→"演示数据") / 重复真相下沉 hub-contracts 单一源(deriveErrorCode·Health·SystemStatus·Create\*Request 三组跨包去重，re-export 保路径零行为变化)。验证：hub-contracts 41 测/hub-server 74 测/hub-console typecheck(noUnusedLocals)+7 测+build 全绿；git diff --check 干净。新功能立项 backlog（DEPGRAPH-ENTRY-OVERLAY/DRAG-CONNECT/AI-AUTODRAW + INTEGRATIONS-TO-SETTINGS）已补 frontier。老实定位：命名进设置/依赖图浮层/拖拽连线/AI 布图 均未实现；石山①③④ 未修。
- 2026-06-14 KB-IMPORT 独立二次对抗审计 + 正确性硬化 (KB-IMPORT-FOLLOWUP 部分 done, D-051) — D-050 落地后另起**独立**多维对抗审计 `wf_74dee37d-59b`（4 finder 按 CLAUDE.md 分档[parser/schema-recall/I0=opus、cli-io=sonnet]→每条候选 3 票 majority，61 agent/1.99M token）对**已提交**的 KB-IMPORT 代码做第二意见，抓出 D-050 那轮（`wf_a52195b7-44e`）**漏的 3 条真实正确性/§10 缺陷**：① **`fileNameToSlug` 撞 slug→静默丢档**（`ascii≥3 返回前缀`+`.slice(0,48)`+非 ascii 丢字使 `CAN问题归档甲/乙`、`26R2历史Bug归档-CAN甲/乙`、`a_b/a-b` 都→同 issueId→第二份被当「已导入」`skipped-existing` 静默丢弃、无 warning/退出码 0）→ 拼**全名哈希后缀单射**；② **`toIsoDateTime` 不查日历**（`2026-02-30`/`04-31` 返回非法 ISO→下游 Zod 拒→整档 `failed` 而非兜底）→ `Date.UTC` 反查；③ **`readFile`/`readdir` EISDIR 崩整批**（`.md` 子目录/不可读文件）→ `withFileTypes`+`isFile()`+`readFile` try/catch。顺手收 `SKIP_FILES` 大小写（nit④）/`extractSection` `**` 残留（nit③）/死代码 `if(!parsed)throw`/`isCli` 正则。**I0 探针重跑 CLEAN**（无 memberId/ownerId/confirmedBy 外露）；**驳回项**（majority 未过）：markdownContent/fileName 偶含人名外露（条件性 by-design 自由文本回显）、IMPORT_FORCE 无去重（仅磁盘膨胀、召回零影响）。验证：hub-server `verify:all`（typecheck+**74 测，+9 新**[日历边界/撞 slug 消歧 4 例/runImport IO 子目录跳过·README 大小写·撞 slug 两卡·重跑幂等]+build）全过；6 真实档案重跑 5 导入 0 失败、slug 哈希后缀化、debug-checklist 日期由文件名正确解析、CAN/UART 召回实证。剩 nit ①IMPORT_FORCE 去重 ②汇总 rootCause `；`串接（非阻塞、二审确认召回无损）。

- 2026-06-14 KB-IMPORT-PROBEFLASH·ProbeFlash 调试归档一次性导入 (frontier#1 三支柱最后一项 done, D-050) — 纯 hub-server、零 contracts/console 改动。**纯解析器** `src/import/parse-debug-archive.ts`（零 IO 可单测）：frontmatter 拆分 + 文件名→ascii slug + 历史日期派生（frontmatter→文件名→正文「生成于」→兜底）+ TAG_VOCAB 客观标签 + best-effort 抽根因/修复/预防段；**一文件=一张卡**（汇总文档无可靠 bug 边界，recall-over-precision，rawInput 存全文）。**导入 CLI** `src/import/import-debug-archive.ts`：组 IssueCard(status=resolved)→canonical `buildCloseoutFromIssue`**注入历史时戳**(errorCode `DBG-<历史日期>-NNN` 非当前钟)→`FileKbStore.appendCloseout`(同 server 落盘文件)，skip-existing 幂等，README 跳过。`deriveErrorCode` 抽到 `src/kb/error-code.ts`(CLI/server DRY 共用)。`npm kb:import` 脚本。**C2/I0 无人维度**(generatedBy=hybrid/主键 issue·errorCode/客观标签，绝不写 memberId)、**§10**(抽不到给诚实兜底+warning 不静默)、**G2**(只 append 不双写)。**3-lens 对抗核实** `wf_a52195b7-44e`(I0/C2 + 解析保真·写安全 + 诚实·DoD → 综合)两技术 lens 均 ship、裁 block 仅 DoD/流程(未提交/无 ADR/无脚本，三项已闭)；**H2 FileKbStore writeChain 判 stays-deferred**(顺序 one-shot CLI + skip-existing 咬不到，留 AUDIT-FIXES)。验证：hub-server verify:all(typecheck+65 测含 23 新解析测+build) 全过；**6 真实档案实跑** 5 导入/0 失败、errorCode 历史化、4 代表查询(FreeRTOS HardFault/夹爪抬升/串口 IDLE DMA/达妙上电)均召回 iss-pf-* 实证。**真实语料写入=部署机 operator 一次性跑**(`npm run kb:import -- <archive> <permanent-dataFile>` + `TEAMHUB_KB_DATA_FILE` 起 server)。4 nit 入 backlog `KB-IMPORT-FOLLOWUP`(IMPORT_FORCE 去重/汇总文档 rootCause 串接/extractSection markdown 残留/SKIP_FILES 大小写，非阻塞)。

- 2026-06-14 PM/KB 写侧 web 表单·console 录入口 (frontier#1 PM-KB-WRITE-FORMS done, D-048) — §6.B 连续构建、纯 hub-console 写侧零后端改动。**PM 录入面板** `features/pm/PmCreatePanel.tsx`：段控切换 布置任务/连依赖/暴露需求 三表单；依赖/需求 from·to·onTask 走 **live 任务下拉**；成功 `invalidateQueries(['tasks',source])` 看板即刷；自edge 守卫；冷启动空板引导（`PmBoardPage` `pm-coldstart`）。**KB 结案表单** `features/kb/KbCloseoutForm.tsx` + `KbSearchPage` 加 检索/结案 标签：最小人本字段合成 IssueCard（status=resolved）+ rootCause/resolution 必填；成功显 errorCode/归档文件/派生知识点 + `invalidateQueries(['kb-similar',source])`（D-047 回灌后刷新）。**API client 写侧** `api/client.ts`+新 `api/schemas/pm.ts`：四 mutation，请求 schema 与后端同法从 hub-contracts 派生（不手抄）；mock 闭包可变任务表（无后端也即时反映）+ mock closeout 复用 canonical `buildCloseoutFromIssue` 纯函数。**汉化** ~75 键 zh/en parity（`Record<TranslationKey,string>` 类型强制）。**I0 命门实证**：confirmedBy 只在写表单收集 + POST 入参 + 回建边本人，第三方读视图/UI 零渲染——POST `confirmedBy={id:m-secret-leaker,displayName:SECRET_NAME_LEAK}` 后 `GET /api/dep-graph`+`/api/tasks` 干净；代码自审 confirmedBy 仅注释+请求构造。**本地真机端到端**（hub-server:4177 真实后端 + Playwright real）：建任务→看板出现 / 连依赖→成功 / 暴露需求→201 / 结案→errorCode+归档+知识点 → **切检索同症状召回刚归档卡（closeout→corpus→similar 闭环实证）**；四 POST 路由 curl 往返全过。**2-lens 对抗核实** `wf_af4c88df-309`（opus I0 + sonnet 正确性 → opus 综合）裁 **ship/i0Clean=true/mustFix=0**，2 nit（死 source prop + 结案未失效 kb-similar 缓存）合并修。老实定位：mock task 表切源即重置、真实 status 派生上游未接通（`statusSource=console` 兜底，不宣称解 C1/C5）、ProbeFlash 导入后置。验证：hub-console verify:all（typecheck/7 测/build）+ git diff --check 全过。设计=`docs/design/pm-board.md`（status→implemented）。

- 2026-06-14 PM 项目计划表·后端录入簇+读视图 (frontier#1 PM 后端 done, D-045) — §6.B 连续构建承接 base 收口刀录入簇（原 throw 全落地）：`createTask`(补 id/时间戳+默认 status=pending/statusSource=console)+`createDependency`(clamp status=active)+`createNeed`(clamp open/openedAt=now/escalatedAt=null/**claimedByMemberId=null A2 反派单**) 写实现 + `POST /api/tasks`·`/api/dependencies`·`/api/needs`(201/400) + `GET /api/tasks` 读视图。**§8 设计闸 + 用户拍板**：confirmedBy 在 schema 是 ActorRef(含 memberId) vs D-042「timestamp 非 memberId」冲突涉 I0 核 → AskUserQuestion：Q1=**ActorRef 作内部凭证**(I0 靠永不经读视图暴露/排名守，非 schema 去人 id)、Q2=**本轮后端+读视图、console UI 下一轮**。**2-opus 对抗核实**(`wf_86ad9d6b-45a`，152K token：I0 暴露面+写实现健全)裁 **ship、mustFix=0**——**对抗探针实证**：POST `confirmedBy={id:m-secret-leaker,displayName:SECRET_NAME_LEAK}` 后 `GET /api/dep-graph`+`/api/tasks` 响应体均无泄露标记 → I0「永不暴露」守住；2 nit(死代码失真注释/创建可派单 claimedByMemberId)已 PM-cleanup 收口(后者硬化 A2)。设计+落地=`docs/design/pm-board.md`(I0 读写边界/触发表/死表基线)。**老实定位**：console UI 未做(用户 Q2 定下一轮)、真实 status 派生上游未接通、不宣称已解 C1/C5。验证：hub-server verify:all 37 测/git diff --check/skills-sync 全过。commit `7218a67`+`6cb38c8`+`3bbf919`。

- 2026-06-14 KB-CORE 战队知识库·核心 (frontier#1 done, D-044) — §6.B continuous-build 连续构建 7 原子单元(U1~U6b，各自 verify+单独 commit+push `45bbeaf`→`226e838`)：① `kb.ts` 移植 Probe_Flash `IssueCard→InvestigationRecord→ErrorEntry→ArchiveDocument` Zod 链(保留 normalizedSummary/relatedFiles/relatedCommits·去 repoSnapshot·isoDateTime 统一·verbatimModuleSyntax) + `kbScenarioFixture`(跨赛季 CAN/3508/MicroROS) ② `kb-similar.ts` 移植 `rankSimilarIssues` 纯函数(逐字等价打分；findSimilarIssuesForIssue 的 StorageRepository IO 不移植) ③ `kb-closeout.ts` 移植 `buildCloseoutFromIssue`(now/id/errorCode opts 注入保持纯) + 新增 `deriveKnowledgeNodeFromIssue` ④ kbStore 类型 `GovStore`→独立 `KbStore`(兑现 base 收口刀对抗核实 deferToNextKnife：IssueCard 不在 GovernanceSnapshot)+`InMemoryKbStore` ⑤ `GET /api/kb/similar`(症状→top-N，A4 护栏 `note` 焊进响应) ⑥ `POST /api/kb/closeout` + `InMemoryGovStore.closeoutKbNode` 写实现(I0 安全：补 id/createdAt clock 注入·克隆 knowledgeNodes 不污染 fixture·无人维度·errorCode clock+issue.id 确定性派生·缺 rootCause→422 不伪造完成)。**4-opus 对抗核实**(`wf_fc3f1282-bbf`，231K token：3 lens[移植保真+TS/宪法 §5/路由·Store 集成]→1 综合)逐条 grep+typecheck+test 实证裁 **ship、mustFix=0**(3 nit 均诚实标注或顺手收口 U6b)。设计+落地说明=`docs/design/kb-core.md`(触发表/死表基线/findability/护栏)。**老实定位**：录入交互未接通(等 Hermes 统一触点)、不宣称已解 C1。验证：hub-contracts verify:all 41 测/hub-server verify:all 28 测/git diff --check/skills-sync 全过。

- 2026-06-14 D-043 构建纪律双轨化（宪法）— 化解"atomic-task 串行 STOP 是全员硬律、拖累 Claude Code workflow"的张力（甲方：还用弱工具→必须双轨、要物理隔离但怕漂移）。**`AGENTS §6` 重写为双轨三段**：§6.0 共享底座（工具无关单一源，吸收 completion gate + 提交授权 + M1 候选池闭口 / M2 DoD 谓词 / M3 误提交自检 + DoD 对照表）+ §6.A 串行轨（无编排能力工具 Codex/OpenCode：一次一个→STOP→重入）+ §6.B 连续/编排轨（具 workflow 能力如 Claude Code：拆原子单元喂 workflow 连续构建·不强制 STOP·每单元仍各自验证+单独 commit·小改动直接做不强起 workflow）。**分档按能力**(非工具名)。**物理隔离两 skill**：`atomic-task` 收窄为 §6.A 串行（M1/M2/M3/DoD 表外移引用 §6.0）+ 新建 `continuous-build` §6.B（引用 §6.0、与 atomic-task 互不依赖→不漂移）。`workflow-evolution.md` 标 superseded-by D-043（当年无 workflow 故保留 STOP 的旧立场被现实推翻）。Explore 全仓扫交叉引用定改动面。纯 docs/planning/skills、零代码。验证：git diff --check + now.md yaml + agent-state json + verify-skills-sync（新/改 skill 镜像）+ grep 无悬挂引用。

- 2026-06-13 HUB-SERVER-GOV-SCAFFOLD base 收口刀 (frontier#1 done, D-042 决策 5①) — 化解"四次重建底座"违 C3：① `gov-store.ts` GovStore 加写方法白名单签名 `createTask/createDependency/createNeed/closeoutKbNode`（draft 入参 = `Omit<Entity, server 生成字段>` 派生，**仅签名、实现后置=throw**，C3 不一把梭不实现写入簇/路由）+ reserved `InvStore` 接口（INV 唯一需扩 schema 的根，PartStock 本刀不建）② `server.ts BuildHubServerOptions` 加 `kbStore?: GovStore`/`invStore?: InvStore` 扩展点（KB 复用同快照不扩 interface）③ 新增 `SqliteGovStore` 持久化切换合约 stub（同 GovStore 接口全 throw not-implemented，证 InMemory→SQLite 不必一次性重建）④ `InMemoryGovStore` 写方法 throw "实现后置"（读路径 getSnapshot 不变）。**workflow 实现 → 4-opus 对抗核实**(run `wf_fe26249b-4a1`，190K token：3 lens[ts 健全/宪法 C2·G2·I0·C3/sqlite·INV·KB 三方可扩展不重建]→1 综合)裁 **ship、mustFix=0**：宪法四闸接口层全守(白名单无 memberId 横比/无 setTaskBlockedBy 双写/closeoutKbNode 不引入人维度/A1 缺口归组)、ts 健全(Omit draft 正确·InvStore weak-type 拒乱注入·throw stub 返回类型相容)、sqlite/INV 扩展真成立。核实抓出我注释 2 处过度声称(KB `/api/kb/similar` 语料 IssueCard 不在快照、kbStore 类型过早收窄)已修注释为诚实标注(守 §10)，对应实现转 deferToNextKnife(承接 KB-CORE/PM/部署刀，已记 backlog + frontier)。验证：hub-server verify:all 18 测(+4 新 scaffold)/hub-contracts verify:all 29 测零回归/git diff --check/skills-sync/now.md yaml/agent-state json 全过。

- 2026-06-13 D-042 需求分析(闸门)+需求可行性分析+构建定基调 — 20-agent 闸门式 workflow(`wf_0ef0d4cc-4c8`，1.26M token：5 分析器需求闸门[宪法=opus]→opus 裁定→5 haiku 实证→4 sonnet 评估→4 opus 对抗核实→1 opus 综合)。**需求分析闸门 proceed/0 阻断**(14 条遗留全 major/minor)；可行性裁定 base/kb/pm=conditional、INV 经甲方决策改写。甲方五条定基调：①**冲突取最新版**(D-041 优先→PM 删 Member.status/freeIdle 展示通道) ②**Hermes/openclaw=统一触点能力·最后做·先搭壳子**(新需求=项目需具备调用 hermes 能力) ③**库存不冻结·留着排最后·对话记账防死**+一次盘点建底+缺口汇报+"大概账"定位 ④**base 补收口刀**(GovStore 写方法白名单+扩展点) ⑤**KB 拆 CORE/LARK、PM 结构键、dueDate 不引入**。分析记录=`docs/design/three-pillar-feasibility.md`(D-042)。对抗核实抓出初稿 3 处幻觉(8 GET 一把梭/INV 不存在的 DoD/PM 改名方案)已纠正。纯 docs/planning。验证：git diff --check + now.md yaml + agent-state json + verify:all 零回归。

- 更早条目（**D-041** 三支柱构建前设计定调(中心实体=任务/项管看板→项目计划表全员可见/视图解耦/三堆判定尺)、**HUB-SERVER-GOV-SCAFFOLD 首刀** GET /api/dep-graph(D-040)、**D-040** 三支柱需求设计分析(14-agent)+破冰序 base→kb→pm→inv+首任务收敛 GET /api/dep-graph、**D-039** AI 退出治理+演进留地基+三支柱(知识库/项管/库存)+知识根合并+死表头号约束、**D-038** 目标结构最终确认(真相分域/飞书纯被动脸/图纸按组分治/只自建四样)、**D-037** 定位回中(CASE+交流+数据库)+核心不变式 I0+silence 纯自指、**D-036** 数据河 build 轨方向 + **D-035** give-floor+修正测量四段 + **D-034** 数据生命线分组化(silence 按组分河) + **D-033** 受众路由/角色模型(captainMemberId/leadMemberId/observerMemberIds) + **D-032** 治理提示层 GovernanceCue 统一 + Member.status 全派生 + 私下 silence（**D-037 收窄为纯自指**）、**D-031** 概念调研 9 agent + frontier 重排（数据生命线命门比录入更深）、**GOV-C4-FIX**(`17316cc`) 修两处 C4 破口 + 锚定测、**D-030** 文档瘦身 −46%、GOV-SCHED-MODEL/**D-029** 差异化在场排班 + `derivePresenceSchedule`、GOV-DATAMODEL-VIZ-ARCHPATH/**D-028** 治理为主轴 + 依赖链归因视图、GOV-REMIND-AXIS-DECIDE/**D-027** 成长轴 + 提醒模型、PF-V03-CLEANUP / HUB-COMPOSE-SMOKE 等）见 `git log` 与 `decisions.md`——已裁剪到 5 条（AGENTS §6）。
