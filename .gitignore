# Personal AI assistant / agent tool traces (local-only, not project assets)
.claude/
.codex
.codex/
.cursor/
.continue/
.aider*
.mcp.json

# Local IDE workspace state. If shared project editor settings are needed,
# commit an explicit template/rules file instead of local workspace state.
.vscode
.idea

# Local backend runtime data
apps/server/.runtime/

# Local secrets and API keys. Never commit real provider credentials.
.env
.env.*
.secrets/
*.key
deepseek.env

# Dependency directories
node_modules/

# Generated local outputs
apps/desktop/dist/
apps/hub-contracts/dist/
apps/hub-server/dist/
tmp/
*.sqlite
*.sqlite-*

# Local reference projects copied for comparison; do not commit into ProbeFlash.
xju-feiyue/
docs/se-assignment/

# Release build outputs; tracked historical release assets stay tracked until convention changes.
.release/

# Skill / Bridge 本地写入区(灰区:活跃但不入库)
.debug-archive/
.bridge/
